# Codex Review: monitoring_p10_protocol_v9_visible_scope_postpositive_corrective_20260801

Date: 2026-08-01
Delegated-agent output: `runs/pi_monitoring_p10_protocol_v9_visible_scope_postpositive_corrective_20260801.md`

## Verdict

**PASS for offline pause, with one recorded P2 pre-canary test gap.**

This is not runtime, canary, project, candidate, release or activation
acceptance.

## Boundary Check

- Initial Pi pass edited only the five governed implementation/test files.
- Same-session follow-up edited only
  `services/api/app/monitoring_ai_service.py` and
  `tests/test_monitoring_ai_service.py`.
- Runner-owned reports were persisted under `runs/`; task/conference records
  were written by Codex.
- No service, browser, provider, runtime DB, real project or candidate workflow
  was started. Ports 8911/5174 remained stopped.
- Parallel medical-writing implementation was treated as read-only.

## Codex Verification

- Reproduced the initial defect: `…完成时必须改期/应重新安排` incorrectly
  returned `reschedule + schedule`.
- Reviewed the same-session fix: a normative modal must precede its schedule
  predicate inside the clause; no global reschedule precedence was added.
- Exact withheld matrix passed:
  - post-trigger modal governing `改期/重新安排`: reschedule-only;
  - modal-before-predicate, independent completion obligation and numeric window:
    mixed/fail-closed;
  - exact postpositive title: reschedule-only.
- Python compilation passed for both governed implementation modules.
- Three governed test files: `403 passed in 11.26s`.
- Full monitoring standard selector was collection-blocked by the unrelated
  `test_medical_writing_dynamic_section_matrix.py` private-symbol import.
- With only that known collection blocker ignored:
  `1398 passed, 4285 deselected, 27 warnings in 639.65s`.
- Adjacent five medical-writing contract files: `200 passed in 2.48s`.
- Independent Luna review found no P0/P1 contradiction.

## Delegated-Agent Output Review

- Pi correctly added v9 identity, v8 terminal legacy cutover, initial/repair
  visible-field prohibitions, postpositive semantic-role handling and exact
  regression surfaces.
- The initial Pi pass missed one modal-governance counterexample. Codex
  reproduced it before acceptance and used exactly one consolidated
  same-session follow-up; the corrected code and tests passed all rechecks.
- No provider prose was accepted as proof, no boundary gate was weakened, and
  no canary/runtime claim was made.
- Luna independently confirmed full-boundary inclusion of uncertainty/user
  action, one-repair/whole-response atomicity, exact v8 failure surfaces and
  fail-closed mixed-family guardrails.

## Hermes Workflow Review

- The finite-code worker ran through the guarded Hermes workflow route as
  Pi/deepseek/deepseek-v4-flash/max.
- Prompt preflight passed before both the initial pass and the one consolidated
  same-session follow-up.
- The same session was reused; there was no duplicate dispatch, provider
  fallback or fixed-interval controller polling.
- Runner reports were treated as evidence and independently checked by Codex.

## Residual Risk

- P2: direct tests do not yet cover failed-v8 visibility, `RUNNING` v8
  exclusion/retirement, and same-revision queued-v8 no-claim/no-reuse as one
  execution-level cutover matrix. This must be closed before a future canary.
- Modal/predicate association remains intentionally lexical and may
  conservatively reject later `为/记录` constructions. This is a fail-closed
  false-positive risk, not unsafe acceptance.
- Standard repository monitoring selection remains blocked by unrelated
  medical-writing test drift; this slice did not repair or revert it.
- Offline tests do not prove provider compliance, startup retirement, runtime
  persistence, or canary success.
