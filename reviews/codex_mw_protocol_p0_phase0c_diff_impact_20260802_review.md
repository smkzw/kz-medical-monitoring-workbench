# Codex Review: mw_protocol_p0_phase0c_diff_impact_20260802

Date: 2026-08-02 CST
Delegated-agent output: `runs/codex_mw_protocol_p0_phase0c_diff_impact_20260802.md`

## Verdict

`READY_FOR_BOUNDED_PHASE0C_CONTINUATION; NOT_READY_FOR_PROTOCOL_RELEASE`

The independent challenge returned READY with no remaining P0-P4 findings.
This is acceptance of the bounded diff/impact increment only, not a Protocol
release or runtime/provider-test approval.

## Boundary Check

- The implementation stayed within the declared contracts, API, frontend,
  tests, and task-record paths. No r42/v36 row, upstream research/OCR/
  translation/download state, or medical-monitoring file was changed.
- No service, browser, real provider/model, OCR, translation, or clone runtime
  was started. The immutable r42 checkpoint stayed byte-identical:
  SHA-256 `d354eb0b4f8b98225c831d76f752b346315949e0e78983f24c6392c91d255130`,
  mtime `2026-07-31 16:11:13 +0800`, size `6516` bytes. Task ports 18905 and
  18906 had no listener.
- Normal frontend `dist/` output was regenerated by Vite; no unrelated
  product path or runtime database was modified.

## Codex Verification

- `tests/test_medical_writing_revision_diff.py`: `7 passed`.
- Sync rewrite race and adjacent rewrite regressions: `3 passed`.
- Final focused integration command: `150 passed, 106 deselected in
  130.69s`.
- Python `compileall` passed for all changed Python modules.
- Frontend `npm run build` passed (`1914 modules transformed`).
- Independent challenge reported `228 passed in 118.15s`, confirmed canonical
  diff replay, paired-hash fail-closed behavior, unreadable sibling impact
  handling, all four generation/persistence paths, and the sync rewrite race.
- Source inspection confirms the frontend only displays the new diff/impact
  data; existing accept/reject/rewrite actions remain unchanged.
- Browser/Word/DOCX visual acceptance and real-provider testing were not run
  because they are explicitly outside this bounded Phase 0C slice.

## Delegated-Agent Output Review

The challenge was read-only and evidence-linked. It initially identified three
P1 paths and was kept in the same session while Codex repaired them:

1. A non-empty `diff_segments` list could be supplied without paired hashes;
   the model now requires segments and both hashes to appear together.
2. Correct hashes and per-segment lengths did not prove a complete canonical
   sequence; `RevisionThread` now recomputes and compares the entire sequence.
3. An unreadable sibling section could fall back to an empty lazy list and
   report `known`; the repository now emits
   `dependent_section_index_unavailable` and returns `unresolved`.
4. Sync rewrite could calculate impact on a newer working-copy revision after
   AI returned; it now revalidates the bound snapshot before and after
   projection, with a deterministic race regression.

The final independent pass found no unsupported completion claim or
over-scope change. It explicitly preserved legacy cold-load compatibility and
confirmed the four real generation/persistence paths.

## Residual Risk

- `source_fact_ids` is the only currently trusted downstream propagation edge;
  unindexed cross-document claims, citations, Synopsis, and CSR relations
  remain `unresolved` by design.
- Full protected-token editing, undo/impact acceptance, same-snapshot DOCX/
  Word gates, browser visual acceptance, security/deployment controls, and
  the serial multi-provider/two-role release loop remain later gates.
- No claim is made that Protocol is release-ready; the next step must be a
  separately tracked Phase 0C slice with its own review gate.

## Hermes Workflow Review Gate

- Task: `mw_protocol_p0_phase0c_diff_impact_20260802`
- Risk: `high`
- Route: Codex main implementation with same-session independent challenge
- Verification: focused tests, compileall, frontend build, frozen checkpoint
  fingerprint, no-listener check, and independent READY review recorded above
- Final decision: `READY_FOR_BOUNDED_PHASE0C_CONTINUATION`; do not infer
  Protocol release readiness or start the later runtime/provider loop from this
  record.
