# Codex Review: medical_monitoring_frontend_principal_gate_20260803

Date: 2026-08-03 CST
Review type: direct Codex; no delegated route

## Verdict

**PASS for the bounded offline frontend gate; runtime identity integration remains a P1 release gap.**

The Assurance panel now reads only an explicit `monitoring_principal` envelope,
requires `server_verified=true`, validates schema/role/scope/hash/time-window and
re-evaluates expiry while the panel remains open. Creation requires that principal
in addition to frozen identity and authority. The current application does not
return a principal, so the visible state remains “未接入” and creation stays blocked.

## Boundary Check

- Only the requested workbench frontend module/test/panel and static contract test were changed;
  derived `frontend/dist` was regenerated by the build. No medical-writing source or real-project
  data was changed.
- The route record was initialized for traceability but no external/delegated process was launched.

## Codex Verification

- Principal focused Node: 15 passed.
- Complete discovered frontend Node suite: 29 subtests/files passed, 0 failed.
- `tests/test_frontend_monitoring_contract.py`: 28 passed; Ruff passed.
- `npm run build`: 1927 modules transformed; existing >500 kB chunk advisory only.
- B6/C14 JSON unchanged (`pending_review` / `blocked_pending_b6_review`, no write permission);
  8911 and 5174 had no listeners. No service, browser, provider or API login ran.

## Delegated-Agent Output Review

- The gate is intentionally a UX/shape contract and does not claim authentication, RBAC, audit,
  route authorization or commercial acceptance. Its `server_verified` marker must be supplied by
  a future server adapter, not trusted from a client actor.

## Hermes workflow review

Hermes workflow guard initialization is recorded for traceability. No Hermes or external agent
was dispatched; Codex completed the bounded implementation and acceptance directly.

## Residual Risk

- Actual provider/session bootstrap, server route dependency, server-derived actor payload, audit,
  Playwright role evidence and B6-approved real-project LOOP remain open.
