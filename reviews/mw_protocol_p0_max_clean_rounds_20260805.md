# Protocol P0 max clean-round review — round 9 and pause

## Codex review verdict

Engineer round 9 is **not clean** and receives zero clean-round credit. The fixed configuration gate passed and the visible flow was real, but the clean clone stopped during staged preparation admission. No substantive full draft or Word artifact exists for this round; the clean streak remains 0.

## Evidence-backed findings

1. P0: full Protocol Word path was not reached. Final state is `awaiting_preparation_admission` / `preparation_partial_failure_stage_admission_required` with 39 deferred items.
2. P1: one triage chunk returned no top-level `results` array. The visible retry recovered 26/26 without duplicate retain rows; fail-closed handling is correct.
3. P1: four Paddle OCR failures are safely excluded but collapse transport/unusable-text causes to `paddle_ocr_outcome_unknown`; status/content-type/poll-count/body-hash observability remains a follow-up.
4. P1/P2: repeated visible stage-admission clicks are truthful and idempotent but high-friction for 87 retained documents.
5. P2: initial Playwright event/deep-link assumptions were harness defects and were corrected; ChinaDrugTrials `reachable_no_api` is an evidence-source limitation, not a fabricated success.

## Pause review

The active one-shot Grok process could not accept injected stdin. After the user-authorized interrupt, the fallback runner/helper and isolated services were stopped. The attempted same-session resume/pause message failed at Grok settings retrieval (`cli-chat-proxy.grok.com`, three network attempts) before any model response. The durable role report, clone evidence, and Grok session transcript remain, so the task is paused without deleting or rewriting evidence; the literal pause marker was not observed and must not be claimed.

## Acceptance boundary and next action

Do not start the senior-medical-monitor role or broaden the tester matrix in this paused state. On explicit resume, re-check the max receipt and continue the isolated engineer clone from visible preparation admission; only after a complete evidence-bound full draft, formal Word, structural/TOC/reference-link/render checks, and P0–P4 review may any clean-round claim be made.

The controller log also shows later repeated settings retry groups (about 05:05 and 05:11) ending in the same network failure and exit code 130. They did not advance the product workflow and do not change the round verdict.

## Codex review — follow-up4

Follow-up4 is **NOT_CLEAN**. The discovery-projection readiness repair is accepted by live UI evidence: the pre-PICOS gate is truthful and covers four requirements without mutating final corpus triage. The 16 approved briefs and prior immutable rows were unchanged.

The new 409 is a real concurrency defect, not a provider/schema acceptance: a prefill external call was dispatched against revision 6 while delayed gate reconciliation advanced the journey to revision 7; the event could not persist and the reservation correctly became unknown-outcome. The fail-closed reservation behavior is correct, but the UI allowed a stale-revision call. The authoritative prefill refresh is the minimum repair; a single explicit force retry is the only permitted validation. P0 framing remains because no evidence-bound candidates for design pattern, population intent, or intrinsic objectives are available; no Word claim is allowed.

## Codex review — follow-up5

Follow-up5 is **NOT_CLEAN**. It correctly stopped before any second AI call when the new stale-revision repair itself raised a prop-name `ReferenceError`. This is a P0 product defect introduced by the repair (`onJourneyChange` versus declared `onJourneyChanged`), now fixed and locally tested. The old unknown-outcome reservation and all completed evidence remain unchanged. Follow-up6 may perform exactly one visible force retry after the fix; no clean-round credit is possible until framing, PICOS, full substantive draft, and Word evidence exist.
## Codex review — follow-up6

Follow-up6 is **NOT_CLEAN**. The fresh-authoritative-revision repair passed in the real UI and correctly prevented the stale revision-6 dispatch. The remaining timeout is reproducible from immutable evidence: the new request took 300 s at the configured DeepSeek provider transport limit plus the 15 s local wait grace, then persisted a single unknown-outcome reservation/fallback event. There is no evidence of an HTTP response, model identity, or malformed schema, so the proper classification is provider latency/wait-budget exhaustion rather than a successful/failed model result.

The bounded route-specific timeout repair is accepted for this continuation: it leaves role model/effort, evidence binding, one-attempt reservation, and no-redispatch semantics unchanged, while making the provider and local wait budgets agree at 900 s by default (explicit override bounded to 60–1800 s). Offline evidence is adequate for the contract; live acceptance still requires one new visible force request. The original two unknown reservations remain immutable and must not be cleared or replayed.

P0 remains: no evidence-bound framing/PICOS recommendations, no full substantive draft, no Word artifact or downstream structural/visual checks. Follow-up7 is limited to one visible force request on revision 8 under the new budget.

## Codex review — follow-up7

Follow-up7 is **NOT_CLEAN**. The route fallback and harness evidence are explicit: Grok was unauthenticated; the previous Cursor continuation was Ask-mode and produced no action; a new Cursor Agent session completed one real visible force request. The Vite/API mismatch was diagnosed and corrected at the Vite-only layer without restarting the API or changing stable data.

The timeout repair passed live: one visible request at revision 8 returned HTTP 200 in 298.3 s under the 900 s budget, created exactly one completed reservation/event, and advanced the journey to revision 9. The two previous unknown-outcome reservations remained unchanged. This is a valid transport/reservation result, not a clean substantive-writing result.

The current blocker is now evidence/adoption semantics, not transport. The returned package is partial; `framing.design_pattern` has no evidence-bound candidate and no intrinsic-objectives candidate exists. Bound registry observations for `picos.design_archetype` are field-scope `manual_only` and `partially_supported`/`pending_decision`; the single-candidate endpoint correctly fails closed. Treating these observations as current project facts would be a P0 evidence-integrity error. No adoption was attempted. The next review must inspect the visible composite per-path override/skip flow and its audit receipt; only a user-explicit, evidence-labeled override or skip may be considered, and it must not be used to fabricate protocol facts.

## Codex review — follow-up8 and repair

Follow-up8 is **NOT_CLEAN**. The visible UI diagnosis confirms the backend policy is faithfully surfaced: no safe composite candidate existed; the design scaffold displayed 13 explicit override/skip rows; before decisions the adopt button was disabled, and the agent did not submit the skip-only action. The two supported project-title candidates were exposed only as field-scope `manual_only` cards with disabled `采用` and `修改后采用`, leaving a confirmed identity fact impossible to adopt without retyping. This is the first broken product contract for a minimal-input AI-first flow.

The bounded repair is accepted at source/test level, with a strict safety predicate: only complete, all-`current_project_fact`, supported, gap-free non-package candidates become `batch_allowed`; competitor/corpus/mixed/partial/pending candidates and every composite package remain manual-only. Existing backend live verification and idempotency gates are unchanged. New regression and the complete prefill suite pass (`2`, `237`, and `575` respectively); Vite build passes. The old revision-9 package was not regenerated, so live runtime proof of the new mode remains outstanding.

The clean streak remains zero and no Protocol Word claim is allowed. The next acceptance pass must use a fresh isolated runtime and a single visible prefill generation, then prove one project-fact single adoption and one competitor fail-closed path without touching the existing clone’s immutable reservations.

## Codex review — follow-up9 and deterministic identity repair

Follow-up9 is **NOT_CLEAN**. The fresh visible pass proved the runtime was
clean and the user could create a minimal project, but the product's automatic
create sequence generated a deterministic package before attaching its 697-row
search snapshot. The resulting title candidates had only a synthetic
`creation_minimum` reference, no catalog bindings, and `manual_only` controls;
no adoption POST occurred. This is a backend adoption-contract gap, not an
AI/provider failure. The same pass also exposed a deterministic design-pattern
candidate incorrectly left `batch_allowed` while `insufficient` and unbound.

The bounded repair is accepted at source/test level: exact title templates are
now rebound to all three immutable current-project facts and a catalog hash,
while phase-only design prose is forced to pending/manual-only. This preserves
fail-closed competitor/mixed/partial/pending/composite semantics. Live proof is
not yet claimed because the r10 package predates the repair. The next review
must use a new runtime, allow the automatic snapshot attach to settle, perform
one visible update against that snapshot, then verify one title adoption receipt
and one fail-closed design/competitor path. Clean streak stays 0; no Word claim.

## Codex review — follow-up10 build-align recovery

The r11 continuation is **focused-contract PASS / overall NOT_CLEAN**. The
initial Vite/API mismatch was a harness/runtime race; aligning a temporary
Vite snapshot to the already-imported API was the smallest reversible action,
and the API was not restarted. The same Cursor session then used the existing
project and real visible UI: one automatic prefill, one snapshot attachment,
one `更新建议`, and one actual `采用推荐` adoption.

The source repair is live-proven: all three deterministic title variants are
supported, batch-allowed, and bound to the same three immutable project-fact
catalog entries; the adopted title has a revision-5 CAS/audit receipt. Design,
PICOS, and competitor-like candidates stay manual-only/insufficient with no
bindings. Zero reservations and unchanged r9/r10 evidence support the
idempotency/isolation claim.

The pass also exposed an independent issue that must not be hidden: the
automatic search pipeline emitted triage progress and ended
`competitor_triage_stale` without a user triage action. This is not a successful
competitor result; classify it as an automatic pipeline/harness contract
boundary and diagnose its first transition before relying on a full Protocol
route. No clean-round credit, substantive Protocol, or Word claim is allowed.

## Codex review — follow-up11 stale-triage repair

The stale result has a concrete first-principles cause: a journey-wide CAS
revision was mistakenly part of the triage material-facts hash. Adopting a
title derived from immutable product/indication/phase facts therefore
invalidated the same snapshot/run even though no relevance fact changed. The
SQLite run payload and deterministic chunk reasons distinguish this from a
failed registry search or an AI response.

The repair is appropriately narrow and fail-closed: compare material facts at
the run revision only when the revision differs; actual relevance-driving
field changes still produce a stale run. Existing stale tests, recovery tests,
and the full triage/durable suite pass (`32`, `29`, and `438` respectively).
This is source/offline acceptance only; the old r11 stale row remains
immutable and no live clean credit is assigned. Fresh r12 is required before
using the path for the full Protocol route. 

## Codex review — follow-up12 live stale-revision proof

The repaired stale predicate is **live accepted for its focused contract**.
In fresh r12, the same `review_ready` triage run survived a title adoption that
advanced the journey CAS from revision 4 to 5; the run stayed on journey
revision 3 with unchanged snapshot/material-facts hash and empty
`stale_reason`. The title was supported, batch-allowed, and fully bound to
current project facts; no unsupported design fact was adopted.

Overall r12 remains **NOT_CLEAN** because the visible shepherd created a second
debug project after a form-selector timeout. That is a harness cleanliness
finding and is preserved as evidence, not erased. No full Protocol/Word path
was attempted, so no clean-round credit or senior-monitor dispatch is allowed.

## Codex review — follow-up13–15 and planner repair

The waiting-stage source repair is focused-contract accepted: a completed
durable preparation continuation no longer leaves the parent in `preparing`
without a user CTA. r14 live evidence showed the missing
`awaiting_stage_admission` reconciliation branch; the repair and `101` focused
tests close that projection defect without touching immutable rows.

r15 is **NOT_CLEAN**. The live engineer route genuinely reached the corpus
gate with real source counts, PaddleOCR-VL-1.6 receipts, and 18 Hy-MT2 chunks,
but the final Protocol/Word gate was not reached. The one failed document is
not a ClinicalTrials.gov or model-connectivity no-result: the audit event and
item payload show a fail-closed document-plan validation error. First-principles
reproduction against the exact OCR spans found the deterministic fallback's
case-sensitive title dedupe as the cause of the `REFERENCES`/`References`
collision. The repair is minimal, deterministic, and regression-tested; it
does not accept malformed provider output or rewrite the failed immutable
attempt.

The same Cursor session was resumed once with a bounded prompt permitting only
the failed-item retry, then source-grounded medical/PICOS admission and full
Word verification. Until its runner returns a terminal report, do not count a
clean round, start the senior role, or claim readiness. Any unexpected result
must be classified by UI action, persisted state, provider/contract, data, or
harness cause rather than worked around.

## Codex review — follow-up16–17 semantic span route

The semantic ranking repair is accepted as a bounded source/offline change:
explicit M11 anchor headings now outrank unrelated long neighbors, while
reference/TOC, generic cross-reference, heading-only, table-noise, and
oversized candidates are penalized. The focused readiness (`12`), translation
batch/chapter (`100%`), and research-pipeline (`58`) evidence supports the
repair, but it is not a live clean-round claim.

r17 is **NOT_CLEAN**. The real headful route proved the runtime contract,
AI-role receipts, ClinicalTrials.gov search, visible triage retry, basket
locking, PaddleOCR preparation, Hy-MT2 translation, and translation UI. It
also exposed the remaining first-principles issue: semantic heading preference
alone still admitted an eligibility introduction without criteria and a
heading-only objectives candidate, while complete alternatives were blocked by
fidelity checks. The final batch was `5` ready / `11` fidelity-blocked /
`4` excluded, and the pipeline stopped at `awaiting_corpus_admission`.

The medical UI initially rejected the incomplete eligibility item, but the
visible one-click batch-confirm path subsequently admitted five records,
including that thin brief. This is a P1/P2 admission-contract defect: a
candidate carrying `incomplete_eligibility_criterion` must not be batch
admissible merely because its machine fidelity status is ready. The result
does not support PICOS, Protocol, or Word acceptance. The visible `409` on
`更新建议` is an in-flight idempotency outcome, and the reachable China
registry with no stable API is a bounded integration limitation, not proof of
zero studies.

The runner ended normally with no fallback or failure. Isolated API/Vite were
stopped after evidence collection. No senior-monitor role was started and no
additional rounds were dispatched. Preserve the current clean streak at `0`
and resume only from the r17 report/evidence, first closing substantive span
and medical-admission gates, then re-running a fresh visible route through a
real populated Protocol Word before any clean-round credit.
