# Codex Review: medical_monitoring_acceptance_contract_audit_20260803

Date: 2026-08-03
Delegated-agent output: `runs/codex_medical_monitoring_acceptance_contract_audit_20260803.md`

## Verdict

Pass for the bounded structural acceptance-contract audit through the Hermes workflow guard review gate; no source patch was justified. The real Playwright/scientific LOOP remains not started and cannot be represented by the fixture.

## Boundary Check

- Codex must confirm the delegated agent stayed inside allowed paths.
- Codex must confirm only the requested output file was written.

## Codex Verification

- Readiness planning artifact: `blocked`, 3 canonical projects, 24 planned scenarios, 10 issues, no provider/runtime/write permission. Explicit blockers include insufficient eligible full batches, B6/approved-input/source-token/CAS/runtime gates.
- Acceptance planning contract: five requested tester routes, two roles, five candidate projects, two consecutive clean rounds, frozen prompt manifest, Playwright-only login and all authority flags false.
- Persisted acceptance revalidation: `blocked`, no persisted Playwright/scientific acceptance JSON declared, zero tester/role/run counts, no authority or release flag.
- Focused deterministic suite: `54 passed in 0.17s` across acceptance, revalidation, readiness, execution and prompt-manifest contracts.
- Negative probes blocked missing browser/scientific refs, API/backend login and non-contiguous rounds. The complete 20-row fixture can pass only as explicitly structural in-memory evidence.
- No service, provider, browser, API login, real project, SQLite or runtime write was started; 8911/5174/8910/4173 remain stopped.

## Direct Audit Review

- `monitoring_real_loop_acceptance.py` freezes all five tester IDs, both roles and five candidate project IDs; rejects narrowed caller matrices, duplicate runs/prompts, API login, missing browser/scientific evidence refs, route-window drift for Pi routes, overlapping sessions, dirty rounds without repair evidence, and missing final two-round clean streaks.
- `monitoring_real_loop_acceptance_revalidation.py` reconstructs the persisted matrix and compares the stored report with a canonical recomputation; it fail-closes on missing payload/report, matrix drift, strict-boolean drift and file byte/SHA mismatch, and never grants release/medical/runtime authority.
- `monitoring_real_loop_readiness.py` remains planning/preflight only and keeps execution/provider/write flags false until B6, source, batch and runtime prerequisites are explicitly proven.
- The contract intentionally stores opaque evidence references rather than reopening screenshot/session/scientific files. This is acceptable for the current planning-only fixture but is a mandatory residual gate before commercial acceptance: the controlled runner must supply independently reviewable, hash-bound artifacts and route-policy evidence; synthetic refs must never be backfilled.
- No fail-open defect in the structural contract was found, so no source-code change was made.

## Residual Risk

No real tester result exists. Before release, the future controlled run must bind each Playwright session, browser screenshot/trace, scientific review record, issue/repair artifact and route-policy observation to immutable bytes/SHA and a real project/source-admission snapshot. B6 formal outcomes, approved-input closure, source-token/CAS replay, controlled runtime identity, live service/provider execution and senior medical review remain open.
