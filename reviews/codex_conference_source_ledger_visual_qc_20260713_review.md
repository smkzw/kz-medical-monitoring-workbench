# Codex Conference Review: source_ledger_visual_qc_20260713

Date: 2026-07-13

## Verdict

Pass with a reduced delegated panel, one accepted polish item, and Codex-owned final visual acceptance.

## Boundary Compliance

- Qwen stayed within the bounded read list, performed three same-session rounds, and did not edit production files.
- MiniMax and Kimi produced no first-round output or resumable session after the 20-minute hard wait and one same-route retry. Both are explicitly excluded with preserved evidence; no silent provider substitution occurred.
- The visual route had no Hermes chair. Codex retained final source, browser, and screenshot authority.

## Participant Outputs Reviewed

- `opencode-go/qwen3.7-plus`: substantive three-round output reviewed. Round 2 returned nonzero, but round 3 resumed the same session and produced a complete corrected pass.
- `aishuo/MiniMax-M3`: excluded after hard wait plus controlled same-route retry without output.
- `buddy/kimi-k2.7-code`: excluded after hard wait plus controlled same-route retry without output; Qwen supplied the configured fallback perspective.

## Hermes Sub-Venue Review

Not applicable for this Codex-led visual conference; no sub-venue chair was assigned.

## Main-Venue Codex Review

- Accepted P1 in substance. The legacy numeric title was fixed upstream to a safe business filename before final acceptance, which is stronger than appending a generic UI kind label. The final MY009 screenshot no longer contains the numeric-only title.
- Accepted P3 as a future-safe empty state and added `尚无历史核验记录。`. The real MY009/RUX rows each currently have two history records, so Qwen's bare-header observation was a viewport inference rather than a true empty-history state.
- Rejected P2 two-line clamping. At 1600 px the visible listing title already retains project, data-listing role, and date; full text remains in the native title tooltip. Raising every row to two lines would reduce scan density without demonstrated ambiguity in the final screenshot.
- Rejected any right-panel width reduction or return to card layout; the master-detail architecture is the established scalable desktop pattern.

## Codex Independent Verification

- Final focused source-registry regression: 23 passed.
- Frontend production build passed; only the existing Vite large-chunk advisory remains.
- Isolated Google Chrome QC passed for MY009 at 1600x1000 and RUX at 1920x1080 with `failures=[]`; one first rerun timed out during project switching, services were independently healthy, and the unchanged script passed on immediate rerun.
- Verified three status axes, confirmation disabled until all warnings plus a 10-character reason are supplied, historical versions hidden by default, module navigation, no horizontal overflow, and no path/hash/security-scan copy.
- Codex inspected the final original-resolution screenshots after the last build.

## Final Decision

Accept the source-ledger desktop slice. Preserve one-line dense source rows with tooltip disclosure. Continue downstream Safety/PV and monitoring real-project coverage; do not reopen this visual slice unless later source volumes reveal a measured scanning defect.
