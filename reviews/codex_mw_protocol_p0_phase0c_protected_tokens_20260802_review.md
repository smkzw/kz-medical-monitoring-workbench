# Codex Review: mw_protocol_p0_phase0c_protected_tokens_20260802

Date: 2026-08-02 CST  
Delegated-agent output: `runs/codex_mw_protocol_p0_phase0c_protected_tokens_20260802.md`

## Verdict

`READY_FOR_BOUNDED_PHASE0C_CONTINUATION; NOT_READY_FOR_PROTOCOL_RELEASE`

The independent same-session challenge returned `READY` with no remaining
P0-P4 or fail-open findings within the declared slice. This verdict is limited
to the protected-token and atomic-boundary increment.

## Boundary Check

- Changes remained within the declared contracts, API service/repository,
  frontend revision ledger, deterministic tests, and task-record paths.
- No r42/v36 row, upstream research/OCR/translation/download state, immutable
  item, or medical-monitoring file changed.
- No service process, browser, Word/LibreOffice, real provider/model, OCR,
  translation, or upstream task was started.
- The old real-project two-step API tests were intentionally updated to assert
  the new 410 retirement contract; the explicit demo route remains covered.
- The immutable r42 checkpoint stayed byte-identical:
  SHA-256 `d354eb0b4f8b98225c831d76f752b346315949e0e78983f24c6392c91d255130`,
  mtime `2026-07-31 16:11:13 +0800`, size `6516` bytes. Ports 18905/18906 had
  no listener.

## Codex Verification

- Final affected deterministic/API suite: `128 passed, 2 deselected` with only
  existing SWIG/FastAPI deprecation warnings.
- Protected-token unit suite: `14 passed`.
- Python `compileall` passed for changed Python modules.
- Frontend Vite build passed (`1918 modules transformed`).
- Direct probes confirmed fail-closed changes for numeric/unit values,
  comparators, Chinese dosage forms, Greek/alphanumeric terms, mixed-case
  metric thresholds, citations, cross-references, HGVS/cytobands, and legacy
  route gating.
- Browser/PPT/PDF/Word visual and real-provider checks were not run because
  they are explicitly outside this bounded Phase 0C task.

## Delegated-Agent Output Review

The read-only challenge was kept in the same session while Codex addressed its
findings. It verified 48 focused tests, ten mutation probes and four
normalization probes, including:

1. Greek suffix digits, compact alphanumeric Greek terms, Phase/Arm/Cohort/
   Group/Part labels, Chinese trailing-half and percent syntax.
2. Conservative HGVS/cytoband and approximation/comparator identity.
3. p/P/n and mixed-case clinical metric comparator expressions with units,
   spaces and ASCII/Unicode comparator normalization.
4. The atomic accept/apply path, same-key race persistence, legacy compatibility,
   real-project 410 gates, and frontend atomic endpoint contract.

The reviewer did not edit files or invoke runtime services. Codex independently
checked the final filesystem and reran the affected suite.

## Residual Risk

- `unresolved` additive protected tokens are shown for medical review; this
  slice does not claim a complete clinical ontology or automatic source
  adjudication.
- Full semantic graph propagation, undo/impact acceptance, DOCX/Word runtime,
  deployment/security controls, browser visual acceptance, real provider calls,
  and the serial multi-provider/two-role release loop remain later gates.
- This record must not be interpreted as Protocol, Synopsis, or CSR release
  readiness.

## Hermes Workflow Review Gate

- Task: `mw_protocol_p0_phase0c_protected_tokens_20260802`
- Risk: `high`
- Route: Codex main implementation with same-session independent challenge
- Verification: focused tests, compileall, frontend build, r42 fingerprint,
  no-listener check, and independent READY review
- Final decision: proceed only to the next separately tracked Phase 0C
  increment; keep Goal active.
