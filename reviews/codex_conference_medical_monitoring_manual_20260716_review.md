# 医学监查子系统说明书会商与终审记录

日期：2026-07-16

## 结论

通过。本次交付为说明书 V1.0，不代表目标规范中的全部生产能力已经上线。正文已明确区分目标规范、已实现能力、项目配置、候选判断和尚待生产验证能力。

## 路由与接管

- Hermes/aishuo/MiniMax-M3 按用户指定作为首选主笔。第一次运行受并行任务上下文污染，第二次隔离运行停滞且未形成可用稿件，两次均排除。
- 按用户最新指令，由 DeepSeek V4 Pro 接管主笔，形成完整初稿：`runs/conference/medical_monitoring_manual_20260716/deepseek_v4_pro_primary_manuscript.md`。
- Reasonix CLI `deepseek-pro` 完成独立审阅。审阅文件头中的模型自报名称不准确，实际命令路由为 DeepSeek V4 Pro；最终记录以执行路由为准。
- Codex 负责来源核验、医学与工程终审、正文重构、电子书实现和浏览器验收。

## 审阅意见处理

采纳：运行模式显式切换；基线规则禁止通用兜底；语义覆盖按项目配置时间窗口；API 幂等、并发与冲突契约；跨表规则细化；非药物治疗边界；空白与未评价的 CS/NCS 区分；GCP 生效日期说明；统一风险状态机。

拒绝或改写：通用基线中位数；固定 7 天语义覆盖窗口；跨项目通用正负 3 天或 30 天窗口；通用四分位距中心阈值；把方案、规则和来源内容混入同一个内容修订号；用总体测试通过率掩盖关键失败。

## Humanizer-ZH 校订

- 删除模型主笔、过程稿等不应进入正式说明书的元叙事。
- 清理公式化过渡语、营销化词汇、表情符号和伪精确评分。
- 保留法规、规则、接口和医学判断的限定条件，不为追求口语化压缩关键逻辑。
- 正文示例统一使用斜体“示例”标记，并增加真实资料来源、脱敏和平移说明。

## Codex 独立验证

- Markdown 审计：160,584 bytes，44 个二级章节，22 幅 Mermaid，72 张表，26 个正文示例；第 8–24 章全部包含就地示例。
- 规则边界：CM 与 IP 变更严格分域；非 CTCAE 参数必须项目批准；中心聚集、时间窗口和阈值无跨项目硬编码。
- 电子书：单文件离线 HTML，官方 CMS Logo，左侧目录、右侧连续正文、全文检索、定位、打印、返回顶部。
- Mermaid：22/22 预渲染为内联 SVG，最终 HTML 不携带运行时 Mermaid 脚本。
- 浏览器：1920×1080 与 1440×900 均无页面级横向溢出；Logo 加载、目录跳转、全文搜索和目标章节定位通过；无页面错误。

## 剩余边界

锁库前全量重算、核查前 CFDI 场景演练及至少三个真实项目的跨项目泛化，仍属于目标规范中的待生产验证项。说明书不得被解释为这些能力已经全部完成生产验收。
