[pytest]
# W05-J1/J2运行兼容（20260926）：medical_monitoring子集以
# `-c implementation/workbench/pytest.ini`引用配置时，该相对路径按调用方
# cwd解析——从项目父目录执行指向本文件的本体（implementation/workbench），
# 从workbench根执行则解析到本副本。内容与根pytest.ini保持一致；
# 子集命令显式传test路径，testpaths仅为无参兜底。
testpaths = tests
