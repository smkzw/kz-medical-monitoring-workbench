# 独立审阅与主线程取舍
2026-09-22；声明角色general_single_object，实时C01批准路线CodeBuddy/deepseek-v4.1-flash:max。

首轮runner完成但读取工具全部被拒，输出只是阻塞说明；不计有效审阅、不冒称全仓独立通过。
没有改全局权限/绕过限制。第二轮在同一session内由主线程提供带行号的冻结原文、专家边界、拟议工作包与实际探针结果；审阅者不调用工具，仅独立挑战这些材料。实际runner回执为完成、无fallback。独立性局限：源码选段由主线程选取，探针不是审阅者重跑；不是浏览器/临床独立验收。

## 采纳
- W01统一“身份与真实性”读取策略，禁止manifest/binding损坏回退；真实来源内容和hash优先。
- public_findings_meta贯穿API→normalize→UI；空数组不能直接显示无风险。主线程已查publication_routes:756注入meta而前端无对应消费。
- W05正式UI前，W01明确事件/来源目标契约；窗口选择必须容纳被定位事件。
- W03先移除无证据疾病风险/版本投票、让unknown真实，再实现正式知识通道；该删除不得清空旧历史工件。
- 明确四层能力：原始浏览可用、已确认部分旅程可用、缺文档基础医学分析未默认授权、依赖方案/IB判断缺依赖不可评估。
- 计数与低风险/聚合入口自洽，前后端同一契约，按根因集中修复。

## 修正/不采纳
- 审阅者称未知快照“回落默认”：精确行为是将当前事实贴上请求的未知snapshot标签；只在空值时用默认。以实际探针和R11为准。
- 来源修复不是“两行换真实hash”即可：必须结合源文件版本、物化血缘、冻结context和真实位置，避免修字符串留下假凭证。
- “onSource完全未用”不成立：Workspace:1928旧风险卡已有按钮；AI claim仍只显示证据条数，真正缺口限定AI Finding公开链。
- 缺失candidate_id为null导致不可提交是条件性风险，当前生产_user_choices_for_unresolved明确返回空字符串；不当作已复现P1。仍在D03验证schema/默认选择。
- R06不是探针自相矛盾：先_effective_user_selections写盘，再_apply_user_role_selections拒绝，最后读取仍存无效选择；该时序正是缺陷。
- 不采纳删掉事件定位功能以求收口，它是用户核心目标；补通契约及明确无锚点状态。
- 不采纳扩词频负向表来修医学一致性；必须命题/证据核验。
- 不采纳全项目只有一次E2E、所有真实模型等W07才跑、每个发现必须另写测试。保留W02入口、W05用户链、W07完整验收三个实质集成批次和首个MG小范围→全量验证；避免最后才发现链断，同时禁止逐编辑测试。
- 不采纳现在顺手改产品两行：本轮用户要review与执行包，实施留给fork，当前goal仍paused。

## 实际关闭范围
独立计划挑战完成并已整合；14类工程问题仍待实施。原文净化副本在evidence/independent-review.md，运行回执摘要在evidence/independent-review-receipt.json。本文件不签署产品验收。

