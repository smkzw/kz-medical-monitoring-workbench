# W07 五项目泛化、恢复启动与最终验收
状态以../status.json为准；本文件为执行合同，不是完成记录。

依赖：W04、W05、W06。

## 进入时只读这些
PRD、CONTRACTS、EXECUTION_RULES、REVIEW中的相关R项，及以下当前文件的完整受影响定义与其消费者：
- scripts
- tests
- frontend
- packages/medical_monitoring
- .trellis/tasks/09-22-mm-delivery-replan/acceptance
路径是起点，若当前仓库文件迁移，先rg定位符号，不凭不存在路径新建平行实现。

## 整组实施步骤
1. 使用W00五项目固定清单逐项目真实全量；已有完整且等价的可验收结果可复用，未处理域必须补齐。每次实际模型路线/输入/版本/范围冻结，失败只重试对应单元。
2. 每项目检查兼容双快照，有则真实增量；无则留下资料盘点证据和not_applicable，不伪造第二临床快照。某项目资料缺失保留blocked，不用CSU/PSO替换。
3. 完成所有专家A01–28和新增D项的指定证据层级；UI用ego作为真实医学监查人员从新项目导入至资料输出，并核对正常数据、风险、反证、来源。需要独立视觉审阅按实时路线能力派发，不把静态截图当交互通过。
4. 一次集中全仓基线，按簇对账96历史失败，不盲放宽；与当前改动无关但影响交付的失败仍需处理或明确真实阻塞。
5. 使用脱敏/隔离备份在另一目录真正恢复、重启同结果、暂停与发布状态；一键启动核验正确产品、端口、前后端版本，不能误开入排应用。
6. 记录加载首屏/筛选/旅程/来源/导入/模型吞吐、P50/P95可测值、峰值内存/请求数/token可得值/工件体积。用同硬件同数据比较，性能不达标再定向优化，不先造缓存框架。
7. 清理被替代脚本与可重建缓存前核查引用和留存；交付可运行版本、恢复包、用户短指南、限制清单和真实验收记录。用户未确认项明确待验收，不能自签。

## 允许修改与边界
仅本包上述业务链及直接schema/测试/必要接线、Trellis状态。扩到共享基础合同需说明原因并加相邻验证；无权改临床原件/用户决定/旧结果。不得替换批准模型路线、清空运行库或重构无关写作。

## 产物
五项目验收矩阵、全回归分类、可恢复发布包、性能基线、用户交付清单。保存最小证据：提交、输入版本、运行配置引用、实际结果及未覆盖项；不保存密钥/多份全量日志。

## 集中验证
最终集成一轮全回归＋ego真实场景＋恢复演练；仅因实际失败/后续修复补对应验证，不反复全量复跑。
验收ID：0922V2-A28、D28、D29、D30、D31、D32、D33。完整场景见acceptance/cases.json，执行证据写status.json。
测试命令由实际发现的suite登记在当前journal；先用现有tests，新增真实反例只写一次，避免复制生产实现为probe。

## 完成条件
全部required证据绑定最终HEAD与数据版本，五项目/适用增量/报告/恢复/性能完整，无未解释P0/P1，用户验收如实。
每个未完成条件保持明确状态；仅部分验证不能把本包标completed。达标合并记录并直接进入下一就绪包，不暂停等用户说继续。

