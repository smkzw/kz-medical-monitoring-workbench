# W01 项目、结果、事实与来源的不可变读取
状态以../status.json为准；本文件为执行合同，不是完成记录。

依赖：W00。

## 进入时只读这些
PRD、CONTRACTS、EXECUTION_RULES、REVIEW中的相关R项，及以下当前文件的完整受影响定义与其消费者：
- services/api/app/main.py
- packages/medical_monitoring/projections/facts_publication.py
- packages/medical_monitoring/api/r7_product/facts_mode_outputs.py
- packages/medical_monitoring/api/r7_product/publication_routes.py
- services/api/app/medical_monitoring_r7_product_router.py
路径是起点，若当前仓库文件迁移，先rg定位符号，不凭不存在路径新建平行实现。

## 整组实施步骤
1. 追踪setup/freshness/adapter/mode/publication/result/evidence全部调用，定义请求作用域的ResolvedResultContext（或复用已有类型），不要只修一个lazy字典。
2. result token绑定真实project/run/snapshot/cutoff/fact manifest/mapping/knowledge/findings hash。latest入口可以选当前，但一旦解析后下游不重新读active。
3. 物化阶段产出manifest并原子发布；查询禁止首次写manifest、损坏重建、任意snapshot贴标签、first-provider和默认SAR。行与meta使用同一已校验工件。
4. 明确legacy迁移清单及版本边界：只无新binding且登记为旧schema时读旧；坏binding、缺目标、hash错、未知snapshot均明确不可用，不生成备用医学结果。
5. 来源定位保存真实源内容版本、sheet/page/row/region，删除占位hash/伪created_at/伪验收hash。缓存按真实内容与版本，不用max mtime或单workspace键。
6. 在一个完整切片同时改生产者与全部消费者，保留必要旧schema只读兼容；旧结果无法重建的标限制，不能偷偷重写成新结果。

## 允许修改与边界
仅本包上述业务链及直接schema/测试/必要接线、Trellis状态。扩到共享基础合同需说明原因并加相邻验证；无权改临床原件/用户决定/旧结果。不得替换批准模型路线、清空运行库或重构无关写作。

## 产物
版本化context及manifest读写协议、迁移说明、公开结果读取证据。保存最小证据：提交、输入版本、运行配置引用、实际结果及未覆盖项；不保存密钥/多份全量日志。

## 集中验证
整组改完集中运行facts manifest/mode/source-digest/API publication相关用例；同进程空库→项目A→项目B、旧结果A→active B、并发行/meta和坏绑定必须走实际应用factory/公开API（隔离runtime），不能只调用helper。
验收ID：0922V2-A01、0922V2-A02、0922V2-A03、0922V2-A04、0922V2-A05、0922V2-A06、0922V2-A07、0922V2-A08、0922V2-A25、D04、D05。完整场景见acceptance/cases.json，执行证据写status.json。
测试命令由实际发现的suite登记在当前journal；先用现有tests，新增真实反例只写一次，避免复制生产实现为probe。

## 完成条件
同号双项目不串；旧token不变；请求未知快照/缺工件明确失败；GET零写入，source显示真实源版本。
每个未完成条件保持明确状态；仅部分验证不能把本包标completed。达标合并记录并直接进入下一就绪包，不暂停等用户说继续。


## 前置用户可见真实性
本包同时贯通read_state/meta到公开envelope，给W05提供统一空态及事件/来源契约；不等界面改版才区分失败与零发现。W03删除无依据知识先于任何新医学运行。
