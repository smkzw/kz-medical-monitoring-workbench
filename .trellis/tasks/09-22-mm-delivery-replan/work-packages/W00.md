# W00 接管基线、路线与可复用证据
状态以../status.json为准；本文件为执行合同，不是完成记录。

依赖：无；首包。

## 进入时只读这些
PRD、CONTRACTS、EXECUTION_RULES、REVIEW中的相关R项，及以下当前文件的完整受影响定义与其消费者：
- scripts/test_round6/HANDOFF_ROUND6.md
- .trellis/tasks/09-06-mm-product-rebaseline/implement.md
- services/api/app/main.py
- services/api/app/ai_runtime_settings.py
路径是起点，若当前仓库文件迁移，先rg定位符号，不凭不存在路径新建平行实现。

## 整组实施步骤
1. 记录实际HEAD/dirty/remote、运行进程/端口/队列状态；保留活项目和所有暂停/未终态工作，先不resume。当前本包HEAD未变则复用143项基线。
2. 只读有效角色绑定、provider配置与最近脱敏attempt回执；逐阶段登记requested、observed、允许别名、视觉能力、fallback、预算与来源。不得查看密钥正文；旧goal/交接冲突不靠猜。
3. 按冻结project/source/mapping/knowledge/prompt/strategy/model身份盘点已有成功作业可复用范围；旧804条只记历史发布，不直接做新真值。
4. 冻结五项目名单与来源清单：MG-K10-SAR、RUX、MY009、MY008-302、MY008-301；以既有项目manifest核实全名、研究号、原件/隔离路径、资料角色版本和兼容双快照。合成CSU/PSO为挑战样本，不替代五真实项目。
5. 登记历史失败簇及未能找到的原始证据；把仍在/tmp的必要报告摘要/hash收进隔离证据目录。检查当前任务输入与实际能力，不重做旧A/B、G门。

## 允许修改与边界
仅本包上述业务链及直接schema/测试/必要接线、Trellis状态。扩到共享基础合同需说明原因并加相邻验证；无权改临床原件/用户决定/旧结果。不得替换批准模型路线、清空运行库或重构无关写作。

## 产物
route-lock.json、project-materials.json和reuse-inventory.json（脱敏、只存必要定位）。保存最小证据：提交、输入版本、运行配置引用、实际结果及未覆盖项；不保存密钥/多份全量日志。

## 集中验证
只读清单/schema/来源路径与hash核对；不调用模型。HEAD变化才选变化组验证。
验收ID：0922V2-A21、D01。完整场景见acceptance/cases.json，执行证据写status.json。
测试命令由实际发现的suite登记在当前journal；先用现有tests，新增真实反例只写一次，避免复制生产实现为probe。

## 完成条件
每条新运行路线可定位到有效设置与允许身份；五项目资料清单明确available/missing/not_applicable；旧paused队列未误恢复。
每个未完成条件保持明确状态；仅部分验证不能把本包标completed。达标合并记录并直接进入下一就绪包，不暂停等用户说继续。

