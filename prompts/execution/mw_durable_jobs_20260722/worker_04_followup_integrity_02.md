Continue the same `mw_durable_jobs_20260722 / worker_04` session. Fully reread `/Users/smkzw/.hermes/SOUL.md`, workspace `AGENTS.md`, your prior report, and the current source. Do not redo already accepted work.

Hard boundaries remain unchanged: write only `services/api/app/writing_reference_translation_batch.py`, `tests/test_writing_reference_translation_batch.py`, and `tests/test_writing_reference_translation_durable_jobs.py`. Do not edit shared durable core, `main.py`, frontend, contracts, OCR/Hy-MT2/Flash pipeline implementations, corpus admission, competitor triage, or revision services. Do not substitute your model for product AI.

Runner-managed report path: `runs/execution/mw_durable_jobs_20260722/worker_04_followup_integrity_02.md`. Never invoke write/edit tools on this report; return it in final text.

Codex independently reproduced 119 focused passing tests, but source review found P0 integrity gaps that those tests do not exercise. Fix all with deterministic adapter-level tests:

1. **Live-owner theft:** `__init__()` unconditionally calls `_recover_interrupted_items()`, which changes every business item in `running` to `failed_retryable` without consulting the durable job lease. This steals an unexpired live owner. Remove blanket constructor recovery. Recovery of a running business item must happen only after the shared durable store has established expiry/takeover for the specific job/batch (for example, inside the newly claimed executor for that exact persisted batch). Test two service instances against the same SQLite DB while owner A has an unexpired lease: B must not mutate the running item. After expiry and successful durable takeover, only the new owner may reset/resume it.
2. **Old-owner business writes:** `_run_items()` checks cancel only before an item. `_process_claimed_item()` and the composite/legacy paths can persist document-plan/translation/item state after expensive product AI returns without a second ownership check. Thread `cancel_check` through the actual persistence boundaries and check before and after expensive calls and immediately before every business commit. After lease loss/cancel during a slow product-AI fake, assert no translation revision, chapter integration, item terminal state or completion audit from the old owner is persisted. Item-attempt CAS alone is insufficient if other translation tables were already written.
3. **Heartbeat ownership ignored:** `_run_items()` currently ignores a `False` heartbeat and catches every exception. A `False` result means claim loss and must stop; do not convert ownership loss into success. Unexpected heartbeat errors must fail retryably or stop without terminal business writes. Add a deterministic false-heartbeat test.
4. **False durable success:** `TranslationBatchDurableExecutor.execute()` always returns a success result after `_run_items`, even when cancellation/claim loss stopped work with pending/running/failed-retryable items. Recheck ownership and aggregate batch state before returning. Cancel/lost claim must not complete the durable job; retryable failures must return an error/retryable result, and only genuinely terminal batch state may return success.
5. **Retry ordering/orphan state:** `retry()` commits batch `running`/attempt/idempotency before creating the retry durable job. If durable creation fails or no-ops, the business batch is orphaned as running. Create/reuse the stable retry job successfully before the business mutation, using the request idempotency key plus a frozen ordered failed-item set (or equivalent stable identity). Duplicate retry must reuse the same job; durable failure must leave batch/items/idempotency unchanged; live-running retry must reject without mutation.
6. `recover_pending_batches()` currently queries only `pending,running`, but the current constructor first converts interrupted work to `failed_retryable`; this means recovered failed items are not actually scheduled. After fixing item 1, prove startup recovery returns/schedules pending and legitimately taken-over failed-retryable batches without stealing live running work.

The prior report's claim that the missing `threading` import was stale bytecode was incorrect; it was a concurrent W2 source defect and has since been assigned to W2. Do not change W2 files or rely on cache deletion as a fix.

Rerun legacy translation batch, durable translation adapter and shared durable core tests. Finish with `WORKER_04_TRANSLATION_INTEGRITY_COMPLETE` only when all six source-level gaps are closed and proven.
