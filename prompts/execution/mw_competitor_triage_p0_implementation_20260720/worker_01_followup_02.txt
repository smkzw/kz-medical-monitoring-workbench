Continue the SAME competitor-triage implementation session
`20260720_160430_449bf3`. This is a recovery continuation after Codex manually
soft-paused the runner during Followup 01. Do not start a new conceptual task.
Reread the current files because the interrupted pass already wrote partial
changes. Do not undo any current correct work.

Before working, fully reread the current global
`/Users/smkzw/.codex/AGENTS.md` and `/Users/smkzw/.hermes/SOUL.md`, then the
workspace files listed below.

Hard boundaries:
- Work only in the current workbench workspace.
- Preserve current partial changes and unrelated user/worker edits.
- Do not modify frontend, harness, stable runtime, real project data, user
  originals, configuration, credentials, or the runner-managed report.
- Product code may call only the direct configured independent AI.

Read these files only:
- `AGENTS.md`
- `prompts/execution/mw_competitor_triage_p0_implementation_20260720/worker_01_followup_01.txt`
- `records/active_slices/medical_writing_final_release_e2e_20260720/SOFT_PAUSE_RESUME.md`
- `plans/codex_mw_pre_picos_evidence_loop_p0_20260720.md`
- `packages/contracts/workbench_contracts/models.py`
- `packages/contracts/workbench_contracts/__init__.py`
- `services/api/app/medical_writing_competitor_triage.py`
- `services/api/app/medical_writing_authoring_journey.py`
- `services/api/app/writing_reference_repository.py`
- `services/api/app/writing_reference_preparation_batch.py`
- `services/api/app/writing_reference_translation_batch.py`
- `services/api/app/main.py`
- `tests/test_medical_writing_competitor_triage.py`

This list permits directly causal adjacent tests and their imported source
files when required to verify the same state machine. Record each addition.

Codex direct recovery evidence
------------------------------
All edited Python files compile. Direct focused test command:

`python3 -m pytest -q tests/test_medical_writing_competitor_triage.py`

currently gives `2 failed, 26 passed`:

1. `TestBasketConfirmAndIdempotency.test_duplicate_confirmation_is_idempotent`
   - the second identical confirmation reaches the `run.status ==
     REVIEW_READY` guard after the first call changed status to `confirmed`;
   - it raises instead of returning the original confirmation.
   - Fix true idempotency before mutable-state rejection: an exact replay of
     the same idempotency key and canonical request must return the same
     confirmation without another projection, decision write or user action.
     Reuse of the idempotency key with a different canonical request must fail
     closed. Do not weaken the requirement that a new confirmation can only
     originate from a fully REVIEW_READY run.

2. `TestProjectionRetry.test_projection_pending_then_retry_succeeds`
   - discovery projection first fails as intended;
   - retry uses stale `expected_journey_revision=3` after the first discovery
     projection advanced the journey to revision 4;
   - corpus projection then fails stale and the confirmation becomes
     `deferred_until_picos` while the test expects `discovery_projected`.
   - Correct the state machine rather than just changing the assertion.
     Discovery projection success before PICOS must be represented as
     discovery-projected/deferred-for-final-corpus with one unambiguous typed
     meaning. The stored/replayed journey revision must be refreshed from the
     successful discovery projection before any idempotent final-corpus
     attempt. A pre-PICOS retry must not fabricate a final-corpus failure.
     Align the typed contract, service and tests with the accepted sequence:
     basket_confirmed -> discovery_projected -> sources_prepared ->
     design_adopted -> picos_complete -> corpus_projected.

Required remaining acceptance
-----------------------------
- Complete every requirement in Followup 01, including wrong configured and
  wrong response model fail-closed tests, exact partition tests, partial run
  rejection, pre-PICOS preparation, replayable projection, override
  reconciliation, post-PICOS idempotent corpus projection, and legacy
  compatibility.
- Do not implement the 3-5 evidence-grounded design candidate service yet.
- Do not touch frontend, 12-lane harness, stable runtime, real project data,
  user originals, credentials, or unrelated files.
- Product code must call only the configured direct independent AI, never an
  agent runtime.

Run:
1. Python compile checks for all changed files.
2. `tests/test_medical_writing_competitor_triage.py`.
3. All adjacent authoring journey, reference repository, preparation and
   translation batch, prefill, corpus readiness and main route tests selected
   by direct causal dependency.

Return a compact loop trace and exact changed-file/test/migration/residual-risk
evidence. The runner owns the report path; do not write it with tools.

Runner-managed output path:
`runs/execution/mw_competitor_triage_p0_implementation_20260720/worker_01_followup_02.md`
