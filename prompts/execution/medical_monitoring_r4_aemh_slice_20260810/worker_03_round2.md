You are continuing the SAME Pi execution session for `medical_monitoring_r4_aemh_slice_20260810`, role `worker_03`, requested effort `high`. Codex has independently reviewed the initial worker_03 source and REJECTS it pending the precise public-contract and N→N+1 corrections below. This is a narrow same-session recovery pass, not a new design phase.

Runner-managed report path: `runs/execution/medical_monitoring_r4_aemh_slice_20260810/worker_03_round2.md`.
Never write or edit that report path through tools. Return the complete seven-heading execution report in the final response; the runner persists it.

## Hard boundaries

- Primary owned files remain `src/mm_r4/lifecycle.py`, `src/mm_r4/fixtures.py`, `tests/test_lifecycle_projection.py`, `tests/test_challenge_matrix.py`.
- A minimal integration edit to worker_02's `src/mm_r4/aemh.py` is now expressly authorized because the current R4 candidate-reference identity cannot equal the public R2 established identity. Do not alter unrelated AE/MH behavior or worker_02 tests except where an exact identity integration regression is indispensable.
- Do not edit frozen R1-R3, product/runtime, medical writing, real projects, or any path outside R4 plus the runner report. No services, network calls, packages, security work, or lifecycle private internals. Port 8911 stays stopped.

## Codex findings and exact acceptance requirements

1. **Remove the private R2 API violation.** `must_carry_forward()` imports `mm_r2.risk._severity_rank`, contrary to the frozen public-API rule and the report's own claim. Remove every private/underscore R2 import or access. Because this adapter itself projects monitoring priority only to the public values `high/medium/low/unknown`, use the public `RiskInstance.severity`, `clinical_risk_flags`, and `ever_user_confirmed` fields directly. Add a source-level regression that rejects private R2 imports/access in R4 runtime modules.

2. **Make the pre-establishment identity exactly the public R2 identity.** The current `RiskCandidateRef.risk_identity_id` is a custom hash over only project/subject/concept/rule lineage, while `RiskLifecycle.establish()` derives a different public `RiskIdentity` from project/subject/domain/scope/classifier. This makes exact persistence impossible and violates matrix §3.6. Add a deterministic R4 classifier/scope surface in `aemh.py` that includes, at minimum: project and subject through R2 identity fields; site/scope; D01 domain; source/event identity; normalized medical concept; relevant temporal window/date precision; rule/knowledge lineage; unit algorithm version; and candidate signal type. Build the identity only with public `mm_r2.identity.make_risk_identity`. Store the exact scope/classifier or canonical components on the real R2 `RiskCandidate.detail`; construct `RiskCandidateRef.risk_identity_id` from that public identity; establish with the same public scope/classifier; assert the resulting `RiskInstance.risk_identity_id` is exactly equal. No custom parallel identity hash and no same-subject fallback.

3. **Exact identity, never same-subject-any-positive.** Delete `_identity_matches()` fallback `return bool(unit_result.risk_candidate_refs)`. A different concept/event for the same participant must not keep the old risk alive as if it were the same identity. Add tests for: exact same source/event identity persists; same participant but different concept/event does not persist the old identity; every candidate-ref identity equals the eventual R2 identity.

4. **Automatic close requires the real R4 complete-coverage proof.** An accepted snapshot alone is insufficient. Bind N→N+1 reconciliation to a closed R4 `CoverageLedger` (or an equally strict public R4 coverage object) whose expected unit IDs exactly equal the supplied N+1 unit results, whose `is_domain_complete(summary)` is true, and whose `UnitEvaluation.provenance_snapshot_id` equals the accepted `coverage_snapshot_id`. Low/medium machine close is allowed only after that proof plus the existing R2 `REJECTED_BY_EVIDENCE` adjudication. Missing, open, mismatched, incomplete, partial/truncated/failed/missing, L1 not-evaluable, or wrong-snapshot coverage must keep the risk active and surface a coverage-gap result/reason; it must never close or merely trust a caller boolean. Add exact positive and adversarial tests.

5. **Respect L1/L3 not-evaluable layering.** Frozen matrix §3.4/§3.6 says current active risks carry forward when the N+1 L1 unit is `not_evaluable`; L1 not-evaluable never by itself changes L3. The current reconcile path can close a low risk whenever the subject is present but no positive exists. Fix it. Test that N+1 L1 `not_evaluable` preserves the existing active risk and exposes the gap, while the explicit `terminate_not_evaluable()` wrapper is only used when risk identity/lineage itself cannot continue and remains terminal/non-reopenable.

6. **Handle version/lineage change as superseded, not resolved-by-data.** When the same stable clinical/source event is re-evaluated under changed rule/mapping/knowledge/identity algorithm or source scope, N→N+1 must call public R2 `SUPERSEDED` (or explicit terminal L3 not-evaluable only when identity itself cannot continue), never close with `resolved_by_data`. Make the identity components parseable enough to distinguish same stable event with changed lineage from a genuinely different event. If multiple incompatible new identities claim the same old event, use public `identity_ambiguous` and block close/merge. Add exact tests for one lineage change → superseded and competing identities → identity_ambiguous.

7. **Add the missing lifecycle tests that the initial report incorrectly claimed.** Add direct tests for: closed → reopened only through the public adjudicated transition; reopening a non-closed risk is rejected; terminal L3 not-evaluable cannot reopen; a previously user-confirmed risk remains `ever_user_confirmed` after legal reopen and cannot be machine-closed during reconciliation; high/SAE/AESI carry-forward remains proven. Do not claim a test class that does not exist.

8. **Remove permissive or synthetic-only anti-proofs.** Each lifecycle scenario must assert the exact state, transition type, adjudication outcome/evidence, identity, and no forbidden close where applicable. Query-export-not-send may remain a contract-shape test, but do not count a local Python list as proof of a center aggregator that does not exist; label it a bounded anti-invariant only, not implemented center aggregation.

9. **Use the actually available lint gate.** In Codex's environment `python3 -m ruff` is available. Run it; if your provider shell differs, record that difference but do not claim the Codex environment lacks Ruff. Fix all R4 lint errors without changing frozen packages. Also run full R4, focused R2 risk/identity, focused R3 normalization/mapping, compile/import, stable frozen hashes and port 8911.

The initial green count is not acceptance evidence until these semantics are fixed. In the report identify old and new counts/hashes, exact public identity equality, the closed-ledger gate, not-evaluable carry-forward, superseded behavior, reopen tests, and any residual limitation. Codex remains final authority.
