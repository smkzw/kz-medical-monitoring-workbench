You are continuing the SAME Pi execution session for `medical_monitoring_r4_aemh_slice_20260810`, role `worker_03`, requested effort `high`. This is the final narrow recovery pass for worker_03. Codex rejects the round-2 snapshot only for the remaining concrete gaps below. Do not broaden design or touch any non-R4/frozen/product/real-project path.

Runner-managed report path: `runs/execution/medical_monitoring_r4_aemh_slice_20260810/worker_03_round3.md`.
Never write/edit the report path through tools. Return the complete seven-heading report in the final response; the runner persists it.

## Required final corrections

1. **Stable event identity must survive snapshot/revision changes.** `_r4_risk_classifier()` currently embeds `record.locator.locator_id()`, which includes `snapshot_id` and `source_revision_id`. This makes the same stable row/event become a different risk identity in every N→N+1 export. Replace that component with a stable source/event key that excludes snapshot and revision but still binds at least the semantic source role plus stable record/event id. Keep the full locator separately in evidence/candidate detail for provenance. Add an exact test where N and N+1 use different snapshot/revision locators but the same semantic role + stable record/event id, concept, time window and lineage; the public R2 `risk_identity_id` must remain exactly equal. A changed record/event id must remain a different identity.

2. **No runtime `sys.path` mutation or dependency on R2 test helpers.** `src/mm_r4/fixtures.py::_make_accepted_snapshot()` dynamically inserts the R2 tests directory into `sys.path` and imports `helpers_b`. This violates the R4 runtime boundary and repeats a test helper as authority. Build the synthetic accepted snapshot directly from the public R2 APIs (`SourceRevision`, `ListingSnapshot`, `IdentityAlgorithm`, `MappingDefinition`, `MappingResult`, `IdentityResolution`, `make_record_identity`, `SnapshotBinding`, `AcceptanceService.register/evidence/advance`, and public acceptance constants/states). Runtime modules must contain no `sys.path` mutation and no import from any tests/helper module. Extend the source-level regression accordingly.

3. **Require complete identity metadata before any side effect.** `candidate_identity_classifier()` currently falls back to generic `candidate.signal_type`; `establish_positive_candidate()` permits scope/classifier overrides and validates identity only after R2 establishment, which could leave an incorrectly established instance before raising. For D01 adapter registration/establishment, fail closed before `register_candidate`/adjudication/establish unless the candidate carries complete scope, classifier, stable core, lineage fingerprint and exact public `risk_identity_id`, and the corresponding `RiskCandidateRef` is one-to-one with the candidate and exact identity. Precompute/verify the public `make_risk_identity` from stored dimensions before side effects. Remove arbitrary identity overrides. Add tests that stripped/tampered identity metadata is rejected and leaves `RiskLifecycle.instances_for_project(...)` empty.

4. **A complete domain ledger is necessary but not sufficient to close one historical risk.** Frozen matrix §3.4 says a negative unit may link a closed historical risk. Require at least one N+1 `NEGATIVE` unit whose `risk_instance_refs` explicitly links the exact prior `risk_instance_id` and `risk_identity_id`, in addition to the closed, complete, snapshot-matched CoverageLedger and R2 adjudication. Without that exact negative-to-history link, keep the old risk active and surface a gap; do not infer resolution from another positive event or a generic same-subject result. Update synthetic helpers/tests to create this explicit historical link.

5. **Uncertainty never closes.** A same-subject `BOUNDARY` or `NOT_APPLICABLE` result without an exact positive identity must carry the active risk forward (with a reason), never pass into machine close. A different-event positive for the same subject must neither persist the old identity nor close it; it keeps the old risk active until an exact linked negative exists. Add exact tests for boundary, not-applicable, and different-event positive. Keep L1 not-evaluable behavior from round 2.

6. **Reject duplicate N+1 unit results.** Before comparing the closed ledger, reject/surface a coverage gap when `next_unit_results` contains duplicate `unit_id` values; do not hide duplicates by converting IDs to a set. Add an exact test.

7. Run `python3 -m ruff`, full R4, focused R2 risk/identity, focused R3 normalization/mapping, compile/import, frozen hashes, a source scan proving no runtime `sys.path` mutation or tests-helper import, and the port 8911 check. Preserve all accepted round-1/round-2 behavior: public R2 identity equality, no private APIs, exact persistence, lineage superseded, competing identity ambiguity, L1/L3 separation, legal reopen, user-confirmed/high/SAE/AESI carry-forward, Query-not-send and count/join invariants.

In the report correct the final test counts and hashes, state the stable-across-revision identity proof, exact linked-negative close proof, fail-before-side-effect proof, and no-runtime-sys.path proof. Codex remains final authority.
