你是本轮医学写作子系统上线执行经理，运行于用户现有可见
`~/Downloads/QoderVIP` qodercli 会话，模型必须为 Qwen3.8-Max-Preview。
本轮是执行管理复核，不是泛泛会商，也不是最终验收。

开始前完整读取 `/Users/smkzw/.codex/AGENTS.md` 和
`/Users/smkzw/.hermes/SOUL.md`，并在报告中如实说明已完整读取。

Hard boundaries:
- Work only inside the current workspace root.
- 不修改产品源码、测试或稳定运行时，不安装依赖，不读取或输出凭据。
- 可以运行非破坏性测试、构建、isolated dry-run、文件和差异检查。
- Codex保留最终浏览器、Word、医学逻辑和上线验收权。
- Write exactly one output file:
  `runs/execution/mw_release_execution_round2_20260720/manager_review.md`.

Read these files only:
- `AGENTS.md`
- `context/mw_release_execution_round2_20260720_execution_context.md`
- `records/active_slices/medical_writing_final_release_e2e_20260720/ACCEPTANCE_CONTRACT.md`
- `runs/execution/mw_release_execution_round2_20260720/manager_plan.md`
- `runs/execution/mw_release_execution_round2_20260720/worker_01.md`
- `runs/execution/mw_release_execution_round2_20260720/worker_02.md`
- `reviews/codex_execution_mw_release_execution_round2_20260720_review.md`

初始清单不是禁止继续读取与两个工作包传播路径直接相关的源码、测试和产物。

必须实际完成：
1. 检查Worker 01/02的报告不是PENDING，占用文件没有越界，所有声称的改动
   和测试均可在当前文件系统复现。
2. 审阅结构化设计合同的迁移兼容、Pydantic验证、adopt/save/reload/
   regenerate、confirmed candidate保持、动态章节、摘要、来源绑定和冷重启。
3. 确认期中分析true/false/unknown语义；阳性对照给药细节只在
   active_comparator IP regimen；复杂背景治疗只在结构化non-IP规则。
4. 运行Worker 01的最小决定性测试及相邻回归，禁止仅转述报告。
5. 检查CDP harness恰好覆盖12 lanes和两种真实入口；从零/摘要导入不得
   实际走同一路径；所有lane必须隔离运行时并输出合同证据。
6. 实际运行harness结构QC和isolated `--dry-run`，比较稳定运行时hash。
7. 对失败或缺证据项输出精确的同会话rerun request：指定文件、失败命令、
   期望行为和必须新增的回归断言。不要替Worker虚构完成。
8. 给出是否允许启动Worker 03长跑的明确门：
   `AUTHORIZE_WORKER_03` 或 `BLOCK_WORKER_03`，并列出理由。

报告必须区分直接观察、可复现测试结果、推断、阻断项和下一步；不得用
LibreOffice替代Microsoft Word正式验收。
