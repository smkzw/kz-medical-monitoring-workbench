# Kimi Code/k3 same-session continuation: finish stages 1-3

You are Kimi Code continuing the existing high-reasoning k3 session.

Continue the existing session
`session_d9b3b3d3-7d20-4591-a061-904b23923b1c`. Do not restart the audit from
scratch. You already completed 133 tool steps and captured `s01` through `s47`
under the task record. Use that evidence.

Read these files only:

- `context/medical_writing_frontend_kimi_progressive_20260719_context.md`

The context names the global, project, workbench, and frontend `AGENTS.md`
authorities plus the current task record. Reread all of them before acting.

Current facts:

- Stable `5174/8911` is online and currently same-source compatible:
  frontend runtime build expects backend `api-d9293c1afc46b0ce`; API readiness
  returns the same build and independent
  `deepseek/deepseek-v4-pro ready=true`.
- Qoder PID `39908` is defunct. Do not wait for or invoke it. Existing Qoder
  screenshots/demo remain a negative comparator only.
- F001 sourceBlock Enter mapping and AI-first prefill/PICOS implementation are
  already owned and accepted by Codex. Do not modify production source or
  reimplement them.
- The blank-section route contract is implemented, but real production-AI
  quality is being tested separately by Codex. Do not claim it from the old
  fake-provider screenshots.

Your remaining work is stages 1-3 only:

1. Convert the existing real-browser evidence into the complete function/API/
   persistence map and lazy senior medical-writer audit.
2. Build and run the independent Kimi desktop demo under
   `records/active_slices/medical_writing_frontend_kimi_progressive_20260719/kimi_demo/`.
3. Visually inspect and iterate the demo at 1440x900, 1920x1080, 2048x1024,
   and 2560x1440. Store screenshots and machine-readable geometry/state
   evidence under the same task directory.
4. Produce all required deliverables:
   - `KIMI_ROUTE_AND_SOURCE_LOG.md`
   - `CURRENT_WORKBENCH_FUNCTION_INTERFACE_MAP.md`
   - `KIMI_PRODUCT_INTERACTION_SPEC.md`
   - `KIMI_VISUAL_DESIGN_SPEC.md`
   - `KIMI_DEMO_IMPLEMENTATION_GUIDE.md`
   - `CURRENT_VS_KIMI_GAP_REPORT.md`
   - `PROGRESSIVE_INTEGRATION_BACKLOG.md`
   - `KIMI_BROWSER_VISUAL_QC.md`
5. The implementation guide must provide exact integration points for the
   current React/Vite/TipTap code, token/class mappings, tests, rollback, and
   small merge slices. The backlog must separate CSS-only, local interaction,
   existing-API, new-backend-contract, and rejected items.

Hard boundaries:

- Do not modify production frontend or backend files.
- Write only under the task record directory above.
- Do not stop stable `5174/8911`.
- Do not alter real clinical source files or stable project data.
- Do not claim visual or product acceptance. Return evidence and
  recommendations for Codex review.
- Use Kimi Code/k3 high reasoning and the existing session.

Finish the files and demo in this pass. Return a compact loop trace naming all
created files, demo run command, browser/QC evidence, critical findings,
uncertainties, and the recommended first low-risk production integration
slice.

Runner-managed output file: `runs/execution/medical_writing_frontend_kimi_progressive_20260719/kimi_stage_1_3_continuation_20260720.md`.
Do not write this runner-owned report path yourself; return the report in the
final response so the runner can persist it.
