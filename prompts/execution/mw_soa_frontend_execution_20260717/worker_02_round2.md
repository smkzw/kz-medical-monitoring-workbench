You are Kimi Code continuing the same `worker_02` session. Read and comply with workspace `AGENTS.md`.

Read these files only:
- `AGENTS.md`
- `context/mw_soa_frontend_execution_20260717_execution_context.md`

Hard boundaries:
- Work only inside the current workspace `.`.
- Do not edit product code or tests.
- Do not read additional local files.
- Live web research is allowed for the evidence completion requested below.

Write exactly one output file: `runs/execution/mw_soa_frontend_execution_20260717/worker_02_round2.md`.

The first pass was usable but its persisted final response omitted the detailed external evidence. Do not edit product code or tests. Do not use a file-writing tool for the report; return the complete report in your final response so the session runner persists it without truncating the evidence.

Provide the missing packet:
1. Every external source actually consulted: exact title, publisher, direct URL, access date `2026-07-17`, source tier, the specific relevant interaction rule, and limitations.
2. Separate direct observation from inference and recommendation.
3. Compare direct-open, chooser-dialog, and controlled-create patterns against the existing workbench state contract.
4. Resolve your two open recommendations: use a normal dialog with a button/radio list rather than listbox semantics unless selection itself is the only action; and choose whether zero-candidate creation should be direct or explicitly confirmed, considering accidental mutation and the existing duplicate-confirmation flow.
5. Return exact accessibility requirements for initial focus, focus trap, Escape, focus return, labels/descriptions, keyboard order, and non-destructive close.
6. State the smallest implementation that needs no new dependency.

Keep the final response auditable and detailed. Do not claim final UX acceptance.
