# Same-session follow-up: worker_01

First, fully read and comply with `/Users/smkzw/.hermes/SOUL.md`, and state
honestly in the report whether you read it.

## Hard boundaries

- Work only inside the current workspace (`.`), except authorized read-only
  clinical sources already named in the execution context.
- Keep the previously authorized backend/test/evidence write set.
- Do not touch stable ports, stable databases, credentials, or clinical source
  files.
- Tools remain available and must not be disabled.
- Write exactly one output file:
  `runs/execution/medical_writing_prelaunch_release_20260717_v2/worker_01.md`.
  Source/test edits explicitly authorized by the execution context are product
  work, not additional execution reports.

Read these files only for initial context:
- `/Users/smkzw/.hermes/SOUL.md`
- `runs/execution/medical_writing_prelaunch_release_20260717_v2/manager.md`

The initial list does not prohibit later evidence reads required by the task;
record every additional target and reason.

Continue the existing `Hermes/aishuo/cms-model` worker_01 session. Do not
repeat broad inventory. Read the current manager ledger:

- `runs/execution/medical_writing_prelaunch_release_20260717_v2/manager.md`

Use disposable runtimes for every write test.

Close these items with actual reruns:

1. Fix the disposable RA fixture using the complete PICOS pattern in
   `tests/test_medical_writing_authoring_journey.py`. Use clinically meaningful
   values, not empty placeholders. Complete framing, PICOS, corpus gate,
   greenfield document creation, working copy, literature, structured table,
   Word export, save/reload, backend restart, and SQLite integrity.
2. Complete a second real-project path from the CMS-D017 synopsis. Supply the
   required multipart `expected_revision`, confirm the import, and verify
   framing/PICOS prefill. If the product frontend omits the required field,
   reproduce and make the smallest bounded backend/client-contract fix with a
   regression test. Do not weaken optimistic concurrency.
3. Invoke the workbench's direct DeepSeek `deepseek-v4-pro` route on a
   multi-sentence protocol paragraph. Require 3-5 distinct, medically usable
   candidates. Apply one, undo or restore, save, reload, restart, and verify
   stale revision rejection. Record only redacted excerpts and lengths.
4. Verify the required competitor plan-to-execute sequence and whether the
   second revision-thread 409 is expected busy-thread behavior. Add no feature
   unless a reproduced product defect exists.
5. Run focused backend tests for every changed path.

If a product defect is reproduced, research official documentation or a mature
maintained implementation before editing; record the source, fit, and rejected
alternatives. Do not call a fixture failure a product pass.

Overwrite the existing report with the same required execution-report headings
and include a defect/rerun ledger. The final report must state which manager
items are closed, still open, or unverified.
