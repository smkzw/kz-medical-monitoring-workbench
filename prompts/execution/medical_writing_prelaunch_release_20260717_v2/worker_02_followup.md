# Same-session follow-up: worker_02

First, fully read and comply with `/Users/smkzw/.hermes/SOUL.md`, and state
honestly in the report whether you read it.

## Hard boundaries

- Work only inside the current workspace (`.`), except authorized read-only
  company documents already named in the execution context.
- Keep the previously authorized exporter/test/evidence write set.
- Do not touch stable ports, stable databases, credentials, or clinical source
  files.
- Tools remain available and must not be disabled.
- Write exactly one output file:
  `runs/execution/medical_writing_prelaunch_release_20260717_v2/worker_02.md`.
  Source/test edits explicitly authorized by the execution context are product
  work, not additional execution reports.

Read these files only for initial context:
- `/Users/smkzw/.hermes/SOUL.md`
- `runs/execution/medical_writing_prelaunch_release_20260717_v2/manager.md`
- `records/active_slices/medical_writing_prelaunch_acceptance_20260717/ACCEPTANCE_CONTRACT.md`

The initial list does not prohibit later evidence reads required by the task;
record every additional target and reason.

Continue the existing `Hermes/aishuo/cms-model` worker_02 session. Do not
repeat the imported-document Word work that already passed. Read:

- `runs/execution/medical_writing_prelaunch_release_20260717_v2/manager.md`
- `records/active_slices/medical_writing_prelaunch_acceptance_20260717/ACCEPTANCE_CONTRACT.md`

External company documents remain read-only evidence.

Close the remaining Word gates:

1. Assemble a complete greenfield RA document through the real service path,
   using the same complete PICOS and working-copy/approval flow as production.
   Export a real DOCX artifact under
   `records/active_slices/medical_writing_prelaunch_acceptance_20260717/word_final_qc/greenfield_ra/`.
2. Compare its cover, synopsis table, TOC, heading styles, sections, margins,
   headers, footers, page numbering, SoA/table rendering, table/figure lists,
   references, fonts, and pagination against the company style authority in the
   execution context. ICH M11 controls structure only.
3. The current independent greenfield draft has no footer parts. Reproduce on
   the current product path. If confirmed, make the smallest exporter/template
   fix and add regression tests. Do not fabricate company content to make the
   document look complete.
4. Repair the synthetic source-preserving API test setup that currently returns
   422, without adding a permissive production fallback that weakens baseline
   matching.
5. Use the pinned CJK renderer. Produce OOXML inventory, rendered PDF/pages, a
   style-difference checklist, and exact pass/fail evidence. Do not describe a
   LibreOffice font substitution as Microsoft Word acceptance.

If a build issue is reproduced, research official OOXML/Microsoft documentation
or a mature maintained implementation before editing and record the decision.

Overwrite the existing worker_02 report with the same required headings. Mark
Microsoft Word GUI opening as unverified if you cannot execute it; Codex owns
that final gate.
