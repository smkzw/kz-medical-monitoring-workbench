# Resume release-r5 A2 engineer after tester-provider image error

Resume the same A2 engineer session and isolated runtime. The prior turn ended
before product interaction because the tester provider rejected the inline
browser screenshot payload with HTTP 400 `invalid URL`. Do not classify that
as a product defect and do not restart the product runtime.

Continue the original A2 engineer contract. Use the browser's accessibility
snapshot and real browser click/type/upload controls first. Do not request
another screenshot in the first turn. Preserve the already saved empty-state
image as evidence. Later screenshots may be saved through a bounded browser
action only after useful product state exists; if inline image return again
causes a provider error, preserve the file and continue through a fresh
text-only browser snapshot rather than abandoning the evidence report.

Complete all supported structured evidence. If the product itself blocks,
finish `EXTERNAL_TESTER_REPORT.md` with the exact BLOCKED marker. Do not modify
source, use direct API/database/DOM injection as a UI substitute, create
`PASS.md`, or supply medical content outside the product AI.
