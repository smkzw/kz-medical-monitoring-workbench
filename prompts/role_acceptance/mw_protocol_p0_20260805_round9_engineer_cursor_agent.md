# Protocol P0 acceptance continuation — engineer perspective, round 9, Cursor Agent fallback

You are the declared Cursor fallback executing the live continuation of the already-started engineer-perspective Protocol P0 acceptance pass. Grok Build is unavailable because its provider health check returned unauthenticated. This is not a new role, not a new project, and not a clean-round reset.

## Hard boundaries

- Runner-managed output file: `runs/role_acceptance/mw_protocol_p0_20260805_round9_engineer_cursor_agent.md`. Return a compact handoff for the runner; do not write this report path directly.
- Work only in the existing isolated clone `/private/tmp/mw-p0-engineer-r9.vZbWIw`, using its API `127.0.0.1:8941` and Vite `127.0.0.1:5222`. You may use Agent tools and write clone-local evidence/logs; do not create another clone/project.
- Do not touch stable API, r42/v36 data, historical lineages, old immutable rows, medical-monitoring databases outside this clone, or product source. Do not dispatch or perform the senior-medical-monitor user role.
- Do not repeat completed search, triage, download, OCR, or preparation work. Reuse the persisted project/pipeline and immutable completed/failed items; perform only the visible continuation actions required from the current state.

Read these files only:

- `AGENTS.md`
- `context/mw_protocol_p0_resume_20260804_context.md`
- `runs/MW_PROTOCOL_P0_FULL_DRAFT_GAP_RESUME_20260804.md`
- `reviews/codex_mw_protocol_p0_full_draft_gap_20260804_review.md`
- `metrics/mw_protocol_p0_full_draft_gap_20260804_metrics.md`
- `services/api/app/ai_role_runtime_settings.py`
- `services/api/app/medical_writing_corpus_analysis_ai.py`
- `runs/role_acceptance/mw_protocol_p0_20260805_round9_engineer_grok.md`

## Fixed product configuration gate

Before any new triage/OCR/translation-support/corpus/writing-model call, record a read-only receipt proving comprehensive and translation-support LLM use DeepSeek `deepseek-v4-flash`, thinking enabled, `reasoning_effort=max`; OCR is official PaddleOCR-VL-1.6; body translation is oMLX `dawncr0w--Hy-MT2-30B-A3B-oQ8-MLX`. If the effective route is Qwen, xhigh, fixture, or another OCR/translation model, stop and report configuration failure; never silently patch it.

## Live continuation assignment

Continue project `proj_user_0e7ac527231c` / pipeline `mwpipe_a28fe3b181b0b20d1927` for the Phase III moderate-to-severe asthma study `QZ-ASTHMA01` from the persisted `awaiting_preparation_admission` state. Use a real headful browser and visible Playwright interactions for every user action. Do not use backend POST/SQLite writes to replace clicks. Continue staged preparation admission only through visible controls, then exercise Hy-MT2 translation, DeepSeek translation-support QC, corpus admission/analysis, evidence-bound PICOS/design completion, substantive section writing, review/freeze, and formal Word export as far as the system allows.

Investigate every unexpected result from first principles: distinguish registry/connectivity, alias, state/lease, OCR transport versus unusable text, provider envelope versus reasoning-only output, semantic module/pattern-kind mismatch, and harness mistakes. Preserve stable error codes, route receipts, request counts, immutable failures, idempotency keys, and exact durable locators. Do not accept headings-only, placeholders, blanket“不适用”, fabricated values, internal logs, future “待确认/待补写” wording, or a Word file without substantive applicable sections. If Word is reached, verify DOCX SHA-256, paragraph/table content, internal-marker scan, TOC field and reference-link structure, and visual rendering; do not claim cached TOC links are rendered unless actually proven.

## Output

Return a compact handoff with sources read, fallback route receipt, clone/ports/project IDs, visible actions and exact state transitions, source/OCR/translation/corpus/full-draft/Word outcomes, structural/render evidence if reached, P0–P4 findings with root causes, skipped-stage uncertainty, and a concrete next action. Do not claim a clean round or submission readiness unless every gate is actually proven.
