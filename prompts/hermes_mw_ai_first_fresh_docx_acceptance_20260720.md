You are Hermes running inside a Codex-controlled workflow.

First, fully read and comply with `/Users/smkzw/.hermes/SOUL.md`. In your output, include one sentence saying whether you read the full file. Do not claim this unless you actually read it.

Hard boundaries:
- Work only inside the runner's current workspace.
- The real source DOCX paths listed in the context are read-only authorities.
- Do not read or modify stable runtime databases or stable 5174/8911 project data.
- Do not edit product source or tests in this pass.
- You are authorized to create all replayable harnesses, isolated databases,
  generated artifacts, validation evidence and reports only under:
  `records/active_slices/medical_writing_ai_first_docx_release_20260720/`.
    - Tools are available and must not be disabled. Use read/search/terminal/browser/web/visual tools when the assigned role or a blocker requires them, within the workspace and risk boundaries, and record the observation.
    - Do not perform final visual/PPT/browser acceptance unless explicitly assigned; Codex remains the final authority.
- Runner-managed output path: `runs/hermes_mw_ai_first_fresh_docx_acceptance_20260720.md`. Never invoke a
  write/edit tool on this report path; return the complete report in your final
  response and let the runner persist it.

Read these files only:
- `context/mw_ai_first_fresh_docx_acceptance_20260720_context.md`

After reading the context, you may read the local source, test, review and real
authority files it explicitly authorizes. You may inspect an adjacent product
file only when required to execute the current public service contract; record
each additional file and why it was needed.

Task:
Execute the bounded two-project fresh DOCX acceptance preparation.

1. Build project A through the actual synopsis-import journey from the D017 PNH
   source. Parse first, surface extracted facts, then simulate explicit user
   confirmation. Do not hand-construct a replacement from an old snapshot.
2. Build project B from only `磷酸芦可替尼乳膏`, `特应性皮炎`, `III期`.
   Use the current direct production `deepseek-v4-pro` prefill, adopt the
   English ClinicalTrials.gov condition term, verify the search plan changes,
   execute the current product discovery service, bind its immutable snapshot,
   and force-regenerate the evidence-aware package. Preserve the adopted
   condition as `user_confirmed`.
3. Parse the supplied original RUX protocol from zero with current product
   import/extraction surfaces. Exact dose, endpoint, AESI, sample-size, washout,
   threshold and visit-time facts may enter the project only when bound to this
   source with locators. Shared corpus and competitor protocols may shape
   wording but are not current-project fact authority.
4. Complete the minimum confirmed project facts needed by the current document
   creator without creating a second medical-approval gate. Generate two fresh
   editable DOCX files through the current exporter.
5. Validate both files for OOXML integrity, editable Word styles, fonts,
   numbering, TOC/bookmarks/hyperlinks, synopsis object structure, first-line
   indentation, black run/table presentation, headers/footers and fields. Run
   the bundled Open XML SDK validator with Microsoft365 rules.
6. Do not open or automate Microsoft Word as final acceptance. Record the exact
   two file paths and the field-update/visual checks Codex must perform.
7. If any public service or direct AI call fails, preserve the partial evidence
   and report the exact failure. Do not substitute fake provider output or old
   generated artifacts.

Output schema:
1. `# Execution Output: mw_ai_first_fresh_docx_acceptance_20260720`
2. `## Boundary Check`
3. `## Work Performed`
4. `## Project A Evidence`
5. `## Project B Evidence`
6. `## DOCX And OpenXML Evidence`
7. `## Commands And Observations`
8. `## Failed Paths And Remediation`
9. `## Codex-Owned Word Acceptance`
10. `## Residual Uncertainty And Next Step`

Quality gates:
- Return a compact loop trace: sources read, iterations, observations, failed
  paths, evidence, uncertainty and next action.
- Do not expose credentials or secrets.
- Do not claim final clinical/regulatory/visual/Word acceptance.
- Do not mark success merely because files exist; validate structure and the
  actual AI/research state transitions.
