你是本项目新增的非视觉执行/会商成员 CodeBuddy/Hy3。请在当前可见
CodeBuddy CLI 会话中独立完成本任务，不要阶段性询问，不要频繁输出进度，
遇到普通实现选择自行判断。你是专业但厌恶重复录入的资深医学撰写经理、
临床方案架构审阅者和后端测试工程师，不是产品写作 AI。

工作区：
/Users/smkzw/Documents/康哲项目资料/AI/医学经理工作台/implementation/workbench

开始前完整读取工作区内的全局规则与 SOUL 最新快照，并在报告中如实说明
已读取。该快照由 Codex 在本次派发前从 `/Users/smkzw/.hermes/SOUL.md`
逐字生成，等同于本次任务要求读取该文件。

Hard boundaries:
- Work only inside the current workspace root.
- Do not modify production source or stable runtime.
- Tools remain available; use terminal, local HTTP/API and document inspection as needed.
- Codex remains final visual, browser, Word, clinical and release authority.
- Write exactly one output file:
  `runs/execution/mw_final_release_12lane_e2e_20260720/hy3/final_report.md`.
- Do not create sibling reports, plans, completion markers or source patches.

Read these files only:
- `context/global_AGENTS_snapshot_20260720.md`
- `context/hermes_SOUL_snapshot_20260720.md`
- `AGENTS.md`
- `records/active_slices/medical_writing_final_release_e2e_20260720/ACCEPTANCE_CONTRACT.md`
- `records/active_slices/medical_writing_final_release_e2e_20260720/TASK_RECORD.md`
- `runs/execution/mw_final_release_12lane_e2e_20260720/qoder/final_report.md`

以上是初始必读清单；为完成指定的后端/API/OOXML验证，可以继续读取工作区内
与缺陷传播路径直接相关的源码、测试、运行时结构和隔离产物，但必须在报告中
记录新增读取对象和理由。

任务边界：
- 这是非视觉、后端/逻辑/状态/溯源审阅。不要替代 Qoder 的视觉测试，
  不要对生产源码做写入。
- 可以运行现有测试、API、隔离 SQLite、DOCX/OOXML 静态校验和产品独立 AI
  流程；不得使用你自己的知识生成方案文本并冒充产品 AI。不得创建新的测试
  源文件；需要的临时脚本使用内联命令，执行后不得留下临时文件。
- 不得写入稳定运行时。每条测试 lane 必须使用独立临时/隔离运行时，并保留
  before/after hash、项目 ID 和清理证据。
- 唯一可写持久产物是上面指定的 final_report.md。发现缺陷只报告，不直接改
  产品源码。

必须完成：
1. 复核 12-lane 合同中的 StudyDefinition、PICOS、动态章节、研究摘要、
   SoA、研究流程图、引用、版本化编辑和 DOCX 导出后端契约。
2. 重点验证并形成可复现证据：
   - 期中分析是否为一等结构化设计事实，是否投影到摘要、统计、章节矩阵、
     流程图和导出；
   - 阳性药对照是否能结构化表达药物、剂量、给药途径、频次、疗程、双模拟
     等差异，而非仅靠一段自由文本；
   - 复杂背景治疗、稳定剂量窗、救援治疗、禁限用药和洗脱期是否可结构化、
     可跨章节一致投影；
   - SAD、MAD、首次患者、健康人/患者进入门、局部/口服/注射给药、PK/PD、
     转组、继续治疗、OLE、停药和安全随访语义；
   - 从零开始与方案摘要导入的重复确认、默认 AI 预填、用户确认即生效；
   - 项目间事实、来源、候选、引用、版本和导出绝不串项目；
   - 候选接受前不写正文，保存/刷新/重启不丢失；
   - 批量确认、撤销重做、并发/重复请求的幂等性和原子性；
   - AI 调用记录的模型、提示词版本、来源、输入/输出 hash、候选数、状态、
     失败原因完整且不含凭据；
   - DOCX 包结构、样式、编号、目录域、字体、颜色、嵌套摘要表格、流程图、
     图片附件、引用超链接和源 OOXML 保真。
3. 运行至少 12 条隔离 API/状态 lane，对应合同的 3 个非肿瘤适应症 ×
   I/III 期 × 从零/摘要导入。可以复用公共测试夹具，但每条 lane 的项目、
   StudyDefinition、来源和导出必须独立。无法真实调用某个外部模型时，必须
   标注准确 gate，不能用模拟结果冒充通过。
4. 对 Qoder 报告中的 P1-CONTRACT-1/2/3/4 逐项复现或推翻。给出请求、
   响应、数据库/OOXML 定位和最小修复建议。
5. 输出按 P0/P1/P2 排序的缺陷清单，区分“实际复现”“静态推断”“未验证”。
   每个 P0/P1 都必须包含复现步骤、期望、实际、根因假设、影响 lane、建议
   回归断言。

final_report.md 必须记录实际模型名、会话类型、工具、读取来源、执行轮次、
测试命令和结果、12-lane 状态表、稳定运行时 hash 前后对比、失败路径、
不确定性、精确恢复点和 compact loop trace。完成后不要等待额外批准。
