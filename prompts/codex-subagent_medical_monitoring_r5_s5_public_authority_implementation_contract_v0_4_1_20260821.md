You are continuing the same Codex author session for the append-only R5-S5 public-authority implementation-contract v0.4.1 revision. Use `gpt-5.6-sol` with reasoning effort `high` in the current medical-workbench workspace.

Hard boundaries:

- Read and preserve the blocked v0.4 snapshot; never modify it.
- Write only the exact nine v0.4.1 paths below. Everything else is read-only.
- Runner-managed output path: `runs/codex-subagent_medical_monitoring_r5_s5_public_authority_implementation_contract_v0_4_1_20260821.md`. Do not write it with tools.
- Do not create producer/runtime/tests/evidence/acceptance files or edit root/frontend/medical-writing/real-project surfaces.
- Do not start 8911, services, browser, real projects or models. Do not self-accept.

Read these files only, fully before editing:

- latest `AGENTS.md` and mw-workbench-wave-audit Skill;
- original v0.4 author prompt `prompts/codex-subagent_medical_monitoring_r5_s5_public_authority_implementation_contract_v0_4_20260821.md`, treating all unchanged requirements as binding;
- blocked v0.4 nine files and fresh reviewer findings as immutable negative evidence;
- accepted public parent, semantic v0.1, temporal v0.1, temporal v0.2 and their acceptance records;
- accepted error replay coverage delta v0.1 nine files and acceptance record;
- parent 236 challenges/validators and all pinned typed/protected sources.

## Exact append-only nine-file allowlist

Create only:

1. `context/medical_monitoring_r5_s5_public_authority_implementation_contract_v0_4_1_20260821_context.md`
2. `reviews/medical_monitoring_r5_s5_public_authority_implementation_contract_v0_4_1_20260821.md`
3. `artifacts/medical_monitoring_r5_s5_public_authority_implementation_contract_v0_4_1/public_api.json`
4. `artifacts/medical_monitoring_r5_s5_public_authority_implementation_contract_v0_4_1/source_join_matrix.json`
5. `artifacts/medical_monitoring_r5_s5_public_authority_implementation_contract_v0_4_1/invariant_error_matrix.json`
6. `artifacts/medical_monitoring_r5_s5_public_authority_implementation_contract_v0_4_1/test_matrix.json`
7. `artifacts/medical_monitoring_r5_s5_public_authority_implementation_contract_v0_4_1/manifest.json`
8. `tools/generate_medical_monitoring_r5_s5_public_authority_implementation_contract_v0_4_1.py`
9. `tools/verify_medical_monitoring_r5_s5_public_authority_implementation_contract_v0_4_1.py`

Manifest status is `candidate_unaccepted`; acceptance token is forbidden.

## Additional accepted authority

Pin and consume:

- error replay delta manifest raw SHA `0dbc0763b9c282c5e2363707a1f6ea99ae8e730e00acb716dcfc61bf9cd3186b`;
- acceptance record `context/medical_monitoring_r5_s5_public_authority_error_replay_coverage_delta_v0_1_acceptance_record_20260821.md` and its actual raw SHA;
- all nine delta file raw SHAs and its immutable external/protected pin maps.

Pin the blocked v0.4 nine-file SHA map exactly as negative evidence. v0.4.1 must not overwrite, import authority from, or use expected planes from the blocked snapshot.

## Mandatory corrections to blocked v0.4

All original v0.4 requirements remain binding, with these corrections:

### A. Executable 272 source joins

Every row must contain concrete, resolvable exact selectors, instance keys/cardinality, accepted reducer/op, accepted temporal-v0.2 recipe/node output and a real construction-graph target. Delete broad `D08TypedInput.scope_binding`, bare `/source`, field-name heuristics and `$common` magic.

Define an exact internal `ConstructionGraph` contract holding every constructed typed object instance plus final packet. The public builder still returns only the packet. The join verifier independently builds this graph by executing accepted v0.2 closed handlers and resolves all 272 pointers.

Subject shared `PublicCutoffEndpoint` four leaves must resolve through an exact Subject common-object instance path keyed by type+instance, constructed from Subject cutoff binding and Subject recipe stage. It may not resolve through AEMH, a target mirror or a nonexistent packet pointer.

For each of 272 rows, independently execute both real typed-source and v0.2 authority selectors, cross-check exact values/identity/cardinality, execute reducer/recipe node, resolve the target and compare the actual leaf. Verify 156+116 unique leaves, unique target pointers, aliases=0, all pointers resolve, DAG prior-only. Fully resealed changes to reducer, recipe/node hash, cross-target dependency, expected plane, fail codes, instance selector or target must be rejected by the real join gate.

### B. Executable 236 specs

The v0.4.1 replay function may only consume each frozen spec's own base reference, `instance_selector`, primary operation, exact linked operations and reseal mode/order. It must actually mutate accepted v0.2 pre authority input, construct the post graph and call the parent validator. It may not select an accepted transform by case ID or ignore modified spec fields.

Identity includes pre authority identities, instance selector, primary+linked operations, lane, reseal mode/order; excludes case ID, label, sentinel, expected code/output and observed result/hash.

Prove 236/236 distinct specs, 0 aliases, 10 accepts, 226 rejects. Ten accepts are exact v0.2 R5C-109/110/116/157-163 transforms and full parent-valid post graphs. All 226 rejects compare the complete actual ordered issue objects. Fully resealed wrong primary, missing/fake/hidden linked op, wrong instance, `reseal=none`, wrong order or identity omission must fail the real replay gate.

### C. Executable 192 error coverage

Do not claim 236 runtime traces cover all errors. Freeze three separate planes:

- runtime specs: 236;
- original accepted challenge execution: pre-delta 170 unique public codes;
- accepted error replay delta: exact missing 22.

The independent verifier must execute accepted entrypoints and the accepted delta gates to prove union 192/192. For every error row, freeze actual accepted source challenge/replay ref, gate entrypoint, mutation, reseal and complete observed ordered issues. Replacing any of the 192 rows with a fabricated consumer error or altering priority/path/origin/message must fail actual replay comparison.

### D. Real active checks and protected pins

Every governance/consistency check must call a real gate with base object, mutation, reseal and expected exact rejection; comparing only object/hash inequality is forbidden.

Use immutable external root raw pins for parent/semantic/temporal/delta manifests and acceptance records. Bind all duplicate/negative/protected scalar surfaces to hard expected values and actual filesystem raw SHA/path. Fully coordinated resealing of nested accepted manifests plus v0.4.1 duplicates must fail before semantic use.

### E. Machine-executable future producer gate specification

Keep `producer_executed=false`, but freeze exact machine-readable future specs:

- exact 11 paths/modules/public signatures/dataclass constructors;
- exact AST node/import/call/symbol allow/deny rules and reachable-call closure;
- exact pytest node IDs and normal/`-O`/`-OO` commands;
- 10 exact sensitivity vectors with source/candidate/previous path, mutation and expected changed issue/hash;
- exact `python3 -I -B <allowed test path> --isolation-probe` argv, bootstrap/import-before-hook sequence, four public API call windows and filesystem/network/subprocess/dynamic-code deny families with positive and negative controls.

At contract stage only validate this specification's exact schema/path/node coverage. Do not claim future tests, AST, sensitivity, isolation or evidence ran.

## Independent verifier

The v0.4.1 verifier must not import its generator or share expected helpers. It independently executes accepted v0.2 closed op handlers/DAG, constructs `ConstructionGraph`, runs 272 joins, replays 236 specs, invokes parent validators, executes pre-delta challenges plus accepted 22 replay gates, verifies all 192 error rows, and runs all active consistency checks.

Generator/verifier AST must contain no case/label/sentinel/expected-result dispatch, mutual imports or assert-only gates.

Run normal, `-O`, `-OO` × `PYTHONHASHSEED=0,1,777`; two isolated byte-identical generations; generator `--check`; exact offline Ruff; all accepted/rejected/delta/blocked-v0.4/typed/root/S4/542 pins; exact artifact/path sets; future producer/S5/bytecode/cache absence; 8911 stopped.

STOP on any unresolved join, missing real challenge entrypoint, inability to execute 236 specs or 192 coverage, accepted pin drift, need for a new authority/error/clinical rule, shared/self oracle, unexecutable future spec, boundary write, optimization/determinism/Ruff failure or 8911 listener.

Only a fresh isolated reviewer may return `ACCEPT_R5_S5_PUBLIC_AUTHORITY_IMPLEMENTATION_CONTRACT_V0_4_1` for one immutable manifest SHA. It only unlocks the exact 11-file producer create-only stage. It does not accept producer/tests/evidence/S5/runtime/UI/browser/real project/model/clinical authority/product/production/medical-writing/8911.

Return exact files/SHA, 272 resolved joins, 236/10/226 execution, 170+22=192 error replay, active gate execution, machine future-spec coverage, normal/O/O2/hashseed/double-generation/Ruff/pin/absence/8911 evidence and residual uncertainty. Do not self-accept.
