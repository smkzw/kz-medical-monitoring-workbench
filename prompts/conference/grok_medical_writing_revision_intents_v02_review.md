你是Grok Build，本次仅进行一轮只读产品与提示词审阅，不编辑任何文件，不调用网络。

任务背景：面向中国创新药I/II/III期临床试验医学经理的方案写作工作台，独立生产AI为DeepSeek deepseek-v4-pro。用户要求四种AI修订意图“改写、监管语气、查一致性、补证据”各有清晰且AI友好的方向性目标，每次返回3-5个可直接写入正文的候选。Codex已完成v0_1基线和v0_2首轮生产调用，请从高级临床医学撰写专家、提示词工程师和真实桌面端用户三个视角审阅。

请只读以下文件：
1. services/api/app/medical_writing_revision_prompts.py
2. services/api/app/ai_gateway.py 中 MEDICAL_WRITING_REVISION system prompt 和输出校验
3. frontend/src/App.jsx 中 revisionIntentOptions、AI候选卡片与修订处置

v0_1基线观察：四个intent未进入prompt，四类调用全部退化为通用润色；曾将“确证性”改为“确认性”，并把普通“研究目的”擅自升级为“主要目的”。

v0_2对同一RUX原始方案段落的真实DeepSeek调用观察：
- 改写：4个候选均能区分标准、精炼、逻辑展开、保守，医学事实基本保留。
- 补证据：4个候选能在没有外部证据时保留原文并说明缺口，但部分版本把“受试者”换成“患者”。
- 监管语气：4个候选保留“确证性”，但审慎版无来源地新增“初步计划”和“待伦理/监管批准”的语义；交叉引用版输出“第X章”“第Y章”占位符，不可直接写入正式正文。
- 查一致性：provider输出被服务器拒绝，错误为 needs_medical_confirmation=false、一个diff_patch为空、两个候选正文重复。说明模型在“没有足够对照来源/没有发现明确冲突”时不知道如何生成4个既不同又不改变事实的版本。

请输出：
1. 四种意图各自的最小不重叠边界，指出当前profile哪里混淆、过度或不足。
2. 针对上述每个v0_2失败，给出可直接落入system/task context的具体中文约束，不要泛泛建议。
3. “查一致性”在仅有一个来源、多个一致来源、来源冲突三种情况下，候选生成与rationale/uncertainties应如何稳定输出并满足3-5候选。
4. 如何要求候选真正差异化，同时禁止通过新增事实、占位符或改变义务强度制造差异。
5. 前端候选选择、接受、重写、写入正文是否还存在会误导医学用户的关键问题。
6. 最终给出一份按P0/P1排序的修改清单，并明确哪些建议不应采纳。

请保持短而具体，引用文件和函数名，区分观察、推断和建议。最终结论仍由Codex决定。
