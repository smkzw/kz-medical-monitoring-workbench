你是额外的 direct Grok 4.5 医学写作产品审阅者。本次只允许一个完整回答：不要调用工具、不要制定稍后执行的计划、不要提问、不要请求第二轮。你不是 Hermes，也不替代正式视觉会商参与者；Codex 保留最终决定权。

目标：从医学经理日常撰写中国 I/II/III 期临床试验方案的真实桌面工作流出发，批判性审阅并给出可执行的信息架构。

已观察到的当前界面证据：
- 1920x1080 桌面页面常驻左侧全系统导航；主区又分为正文编辑器、AI栏、文档目录三列。正文约占一半宽度，AI和目录均很窄；151或159个章节纵向平铺，目录标题截断。
- 页面下方还有“研究方案写作资料包”等大卡片；AI栏常驻修订线程、证据包、导出质量门、来源状态等过程信息。用户认为原文、写作质量门、方案章节候选、表格与来源定位等均属于日志型信息，不应常驻。
- 绿地项目编辑器可进入 contenteditable，但首章正文为空，用户感知为“不能编辑”；曾发生前端已更新而后端仍是旧版本，模板端点404，导致创建与编辑契约错配。重启同步后浏览器探针暂未复现白屏。
- 项目选择器只能选择预置项目，没有“新建项目”。实际需要从零两阶段反问或导入方案摘要两种入口，二者最终进入同一 StudyDefinition 和写作工作台。
- 竞品研究检索、Protocol/SAP 下载、解析、翻译和审核目前混入写作相关区域。用户要求它们成为独立工作流；用户批准后可以一键批量由AI处理，不逐文件审批。主写作台只显示与当前章节匹配、可直接采用的证据，并由AI提供3-5个可填入版本。
- 当前源 DOCX 后端模型主要保留段落纯文本、表格结构和粗粒度 style_id/style_name，未保留 Word run 级字体、字号、加粗、下划线、颜色、标黄、上/下标等，因此编辑器无法真实呈现源文档格式。Tiptap 工具栏虽有格式按钮，但不是格式保真证明。
- 桌面端优先，移动端可放弃；系统用于专业医学经理，不需要营销式介绍页。

请用中文一次性输出：
1. 当前 P0 可用性根因，区分运行态、信息架构、数据合同。
2. 一张文字化桌面布局规格，给出各区相对宽度、展开/收起规则和滚动边界。
3. 新建项目到写作的最短完整主路径。
4. 独立竞品语料工作流与主写作台 AI-证据卡片的明确边界。
5. 3-5个候选版本一键采用、撤销、来源追溯和工作副本保存的交互顺序。
6. 必须移除、按需抽屉、必须常驻的现有信息清单。
7. 富文本格式保真的最小前后端合同与验收测试。
8. 最可能失败的5个点及一个推荐实施顺序。

明确分开“证据”“推断”“建议”“不确定性”。
