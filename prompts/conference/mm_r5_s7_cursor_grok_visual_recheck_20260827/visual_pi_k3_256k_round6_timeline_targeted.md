同一会话最终针对性复核，只核对上一轮 P1-01、P1-02、P2-02/P2-03 和八域首屏认知，不重跑其他矩阵，不读源码，不改文件。

当前服务仍为 5174/8911。使用上一轮两条普通/高密度 Patient Journey 深链，真实 Playwright 复核：

1. 普通页点无风险合并用药、访视间操作、跨访视试验药、日期待确认实验室检查：事件详情不得残留上一风险，不得显示 `s7-source-*`，访视应分别呈现合理的“跨访视期间/访视间/访视未定”等中文语义。
2. 点 `+` 进入详细档：正文不得出现 `s7-source-`，来源只应为“已定位到 N 条原始记录”等用户文案。
3. 高密度点 `-` 进入概览档：每个中高风险点仍应保留可见的“高/中”文字标记，不能只靠颜色；无需在正文重复完整“高风险”四字 100 次。
4. 时间轴上方应在首屏出现 8 个泳道概览条目（AE、MH、合并用药、试验药、检验/检查、住院/操作、症状/疗效、方案符合性），让用户在尚未纵向滚动完八轨时也明确全部域均存在。
5. 第八轨应具备可访问名称“方案符合性事件泳道”。

只按 P0-P4 报告上述 5 项。若这些项均已纠正，明确写“本次针对项未发现 P0-P4”；不得代表 Codex 最终验收。
