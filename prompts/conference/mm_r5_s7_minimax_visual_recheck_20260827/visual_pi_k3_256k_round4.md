同一会话继续，不得重开任务。产品已完成上一轮保留问题的聚焦纠偏，当前合成 overview 已由 Codex 验证为 HTTP 200。请用新的 Playwright context 重新读取当前页面，不得沿用旧截图、旧字符切片或旧性能数据。

只复测 B02、B08、B09、B10、B12：
1. B02 是否已出现“升级”；
2. B08 首屏是否直接显示高/中风险徽标、八域是否按实际日期顺序、低风险是否聚合且“+”能展开全部；
3. B09 指标趋势是否保留“共享时间轴”和完整起止日期；
4. B10 是否有部分日期/日期冲突/日期缺失短标签；
5. B12 是否显示具体 Data Listing 行、单元格和原始值，且普通页不再出现 synthetic://、lineage:、s7-run/snapshot/source 等内部标识。

同时检查双视口、console/pageerror/failed request、中文自然度和首屏可读性。性能以当前运行重测，不引用上一轮 1873 ms。输出完整更新报告，逐项说明 P0-P4；若没有任何 P0-P4，明确写“P0/P1/P2/P3/P4 均为 0”。不改源码、不启停服务。
