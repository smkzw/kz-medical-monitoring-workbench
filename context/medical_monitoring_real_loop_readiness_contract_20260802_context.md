# Context: medical_monitoring_real_loop_readiness_contract_20260802

## Goal

Prepare one fail-closed manifest contract for the eventual real-project LOOP
required by the medical-monitoring Goal, without pretending that offline
fixtures or prior browser runs are current acceptance.

## Source of truth

- `records/active_slices/medical_monitoring_goal_p0_20260729/REAL_PROJECT_TEST_MATRIX.md`
- current project source manifest and source-revalidation records;
- current B6 formal package, approved-input dry-run and aggregate/CAS evidence;
- current product `AGENTS.md` and global execution/routing rules.

## Contract requirements

- canonical projects: RUX-03-002, MG-K10-SAR, MY009-UC;
- protocol/listing references are opaque IDs plus SHA-256, never public local paths;
- confirmed raw full/locked listing and at least two traceable full batches;
- two distinct roles (engineer and senior medical monitor) × four AI tasks;
- an exact 24-row prompt manifest with distinct prompt references and exact
  UTF-8 prompt hashes, plus approved day/night Pi routes;
- explicit B6, approved-input, source-token, CAS and runtime-identity gates;
- browser and scientific acceptance are recorded after execution, never inferred.

## Current result

The planning fixture is blocked. RUX is a raw candidate with a known OOXML
dimension defect, MY009 is restored/transitional, MG-K10 has only a confirmed
single locked snapshot, B6/CAS/source-token/runtime gates are false, and no
browser/scientific acceptance exists. No real project or shared runtime changed.
The exact prompt rows are frozen in the prompt-manifest slice with manifest SHA
`e42fa4f8d579f59654bf8dfe2db185d493bb6a02e1a2d591c7794bc9a2bc3281`; they are
planning identities only and do not represent executed AI output.
