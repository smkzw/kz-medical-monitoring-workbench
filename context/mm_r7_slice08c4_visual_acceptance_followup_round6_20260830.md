# 08C-4 visual acceptance follow-up — round 6

Continue the same independent visual-conference session. Reopen the newest R8 ego-only packet and actual originals/collages. Do not reuse the R7 verdict.

Round 8 claims: the 1280 table edge now ends after whole `风险变化`/`新增`; native rails are hidden so exactly one linked accessible hrail/vrail paints; phase-flow whitespace is reduced and responsive; lower cards enter the 1280/1440 first screen; cadence is a single `日常监查（本次）`; MANIFEST notes/mtimes are current.

Independently verify every frozen-contract P0-P4 and the ego-only packet identity. Closely inspect whether the single rails are proportional and genuinely linked/discoverable, whether the table edge is whole, whether first-screen density remains calm/readable, and whether the widened flow improves rather than distorts hierarchy. Recheck previously accepted drawer/tamper/site/first-token semantics for regressions.

Return `ACCEPT_VISUAL` only if P0-P4 are all zero. Otherwise provide the smallest exact remaining repair with evidence paths. Do not modify product files or claim Codex final acceptance.
