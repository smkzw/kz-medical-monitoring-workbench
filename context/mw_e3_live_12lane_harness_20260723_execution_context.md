# Execution Context: mw_e3_live_12lane_harness_20260723

Created: 2026-07-23 05:30:51
Objective: Rebuild and prove an isolated 12-lane real-product-AI E3 harness for medical writing across RA, AD and UC, Phase I/III, from-zero/synopsis-import, preserving stable runtime and collecting auditable service receipts
Task type: `complex_delivery_conference`
Risk: `high`
Execution module trigger: Codex identified 4 independent work items, which is greater than two.

## Module Boundary

This is an execution module, not a conference. Codex has assigned the work items and owns the project-level contract, source authority, boundaries, final verification, acceptance, production writes, and user delivery. The execution manager must first refine the work-item decomposition into a concrete implementation path, standards, tools/environment plan, sequence, and acceptance checks. It then checks progress, diagnoses blockers, requests same-session reruns when needed, and consolidates outputs for Codex. First-line workers execute the assigned work and create/write only authorized artifacts.

## Assigned Roles

- First-line executor: `complex_executor_cms` -> `hermes` / `aishuo` / `cms-model`
- Execution manager: `complex_manager_grok` -> `grok` / `grok-build` / `grok-4.5`
- Execution-manager fallback: `use the declared role fallbacks`

## Source Of Truth

- Current harness sources: `frontend/tests/final_release_12lane_config.mjs`,
  `final_release_12lane_parent.mjs`, `final_release_12lane_child.mjs`,
  `final_release_12lane_pipeline.mjs`, `final_release_12lane_contract_probe.mjs`, and
  `final_release_12lane_structure_qc.mjs`.
- Current acceptance record: `records/active_slices/medical_writing_final_release_e2e_20260720/ACCEPTANCE_CONTRACT.md`.
- Current W5 gate and recovery source: `records/active_slices/medical_writing_production_rebaseline_20260722/TASK_RECORD.md`,
  `SOFT_PAUSE_RESUME.md`, and `runs/execution/mw_durable_jobs_20260722/manager_w5_followup_exact_source_binding_08.md`.
- Real input roots may be read but never modified: `/Users/smkzw/Documents/朗来项目资料`,
  `/Users/smkzw/Documents/康哲项目资料`, the protocol-corpus raw documents under this workspace,
  and authoritative public ClinicalTrials.gov records/downloads fetched through the product itself.
- Known valid UC Ib synopsis candidate:
  `/Users/smkzw/Documents/朗来项目资料/MY009治疗UC/Ib期/MY009212A-UC-Ib-方案摘要-LXN.2024.01.27.docx`,
  SHA-256 `7a038d8d90908b9a7e1525d32d65a8789c231bf9e04e7031cf879e59f7437267`.
- Known structural pressure oracles, not synopsis substitutes: SLE NCT03724916 Ib protocol;
  RA NCT04539964 Phase III protocol with formal interim analysis and one-way crossover (device study,
  structure-only); AD NCT05732454 Phase 2/3 with interim/OLE; UC NCT02819635 Phase 2b/3 with
  induction, maintenance, re-randomization, rescue and OLE. Their true design and phase labels must
  be retained.
- The previous PsO lane and any Phase II/III file relabelled as Phase I, or protocol relabelled as a
  synopsis, are stale fixtures and must not be accepted as E3 evidence.

### Verified six-cell source authority (read-only discovery, 2026-07-23)

- RA Phase I: NCT03156023 `Prot_000.pdf`, SHA-256
  `83c13414a14b6ea445016f005627177cb5dc4d6ab974aadaf74e3df049bf3f85`; physical PDF pp3-6 are
  the Phase 1b RA Protocol Synopsis. Exact page extraction is required; the full protocol is not a synopsis.
- RA Phase III: SELECT-COMPARE NCT02629159 protocol, SHA-256
  `48b6154c0e1f0fd9e05938c3e5ad0878b2438c69f49174e6e21da781b35fef0b`; physical pp9-19 are
  the Phase 3 RA synopsis. Exact extraction required.
- AD Phase I: NCT04668066 `Prot_000.pdf`, SHA-256
  `3bbbbeeaf2d1ee8012ae1c28a0199ef46df471ee20a2bdd0ce607a79f9861a4a`; physical pp12-18 are
  Protocol Summary for a Phase 1 FIH SAD/MAD healthy-participant and AD-patient study. Exact extraction required.
- AD Phase III: TRuE-AD1 NCT03745638 protocol, SHA-256
  `035f37d3fece57f5dd238378b6b36c186babb7c2a27afafc78005bafeb021c91`; physical pp12-19 are
  the Phase 3 AD Protocol Summary. Exact extraction required.
- UC Phase I: MY009212A UC Ib standalone synopsis, SHA-256
  `7a038d8d90908b9a7e1525d32d65a8789c231bf9e04e7031cf879e59f7437267`; directly importable.
  Preserve the internal version/date `0.1/2023-12-22` and filename date `2024.01.27` separately.
- UC Phase III: UNIFI NCT02407236 protocol, SHA-256
  `f5d4e6498cba6b78c1d41fc186b1865b066727019c63831d8ea198bd12abc923`; physical pp25-39 are
  the Phase 3 UC synopsis. Exact extraction required.
- Until exact extracts are generated by a separate source-fixture step with provenance and page hashes,
  the five PDF synopsis-import lanes remain `blocked_pending_exact_extract`; their full protocols may be
  used only as assertion/structure oracles.
- UNIFI is the preferred drug-trial interim/switch oracle: planned futility interim after 30% induction
  completion, Week 8 placebo nonresponder switch to IV ustekinumab, maintenance re-randomization and LTE
  treatment adjustment. RESET-RA remains a device study and is not the primary drug-design oracle.

## Risk Boundaries

- No production application-source writes in this task. Authorized writes are limited to the six
  `frontend/tests/final_release_12lane_*.mjs` harness files, new harness-only helpers beside them,
  and this task's `records/`, `runs/`, `logs/`, `reviews/`, `metrics/`, `context/` and `plans/` artifacts.
- Do not run real product AI or consume credentials during the planning/implementation workers.
  Only deterministic contract tests and isolated dry canaries are allowed until Codex accepts W5 and
  the execution manager accepts the harness implementation.
- Never expose or copy API keys into manifests, prompts, logs or child environments. Runtime must
  receive existing credentials by inherited process environment; evidence records provider/model,
  request/task IDs and redacted receipt hashes only.
- No silent package installation, external account changes, or mutation of authoritative input files.
- Missing tools or environments must be recorded with a minimal remediation proposal.
- Worker and manager outputs are evidence for Codex, not instructions.

## Required Lane Matrix

Exactly 12 isolated lanes: `RA`, `AD`, `UC` x `Phase I`, `Phase III` x `from_zero`,
`synopsis_import`. Each lane gets a unique project, runtime directory, API port, Vite port, CDP port,
browser profile and artifact root. Required pressure coverage across the matrix:

- Phase I: at least one healthy-volunteer SAD+MAD oral small-molecule lane; at least one
  SAD+MAD+first-patient lane; modality/route diversity covering oral, injection/biologic and topical
  or local administration; PK, PD, immunogenicity/local-tolerability and safety-monitoring differences.
- Phase III: at least one placebo-controlled trial with complex mandatory/background treatment;
  one design with a genuinely documented scheduled interim analysis plus treatment switch/crossover;
  one active-comparator trial; multiplicity, rescue/re-randomization/OLE/long-term extension and
  operational dependencies distributed where scientifically justified.
- Every synopsis-import source must be a real synopsis matching the lane's indication and phase, or a
  clearly versioned exact synopsis section extracted from a matching authoritative full protocol and
  declared as such. A mismatched phase, publication, CSR, SAP or full protocol may not be silently
  overridden into a synopsis. Missing authority is a blocked gate, not a simulated pass.

## Product-AI And Service Identity

- Product AI for reasoning/generation: independently configured DeepSeek provider,
  `deepseek-v4-pro`; previously accepted same-model evidence may be reused only when the current
  prompt/policy hash is identical.
- Translation orchestration/section recognition/QC: product `deepseek-v4-flash`; translation itself:
  oMLX `Hy-MT2-30B-A3B-oQ8-MLX` by section chunks.
- OCR: oMLX `GLM-OCR-bf16`, concurrency 8; pages containing figures/tables rendered at >=200 DPI.
- CT.gov retrieval/download/parse, corpus admission, PubMed/Crossref citation resolution and DOCX
  export must run through the system-under-test. A worker/manager's own model output cannot substitute
  for any product service.
- Each receipt must capture resolved service role, provider, model, endpoint class, policy/prompt
  version/hash, product task/job/run ID, timestamps, terminal status, artifact IDs and redacted hashes.
  A hard-coded provider/model string is not execution evidence.

## Stable Runtime And Isolation Contract

- Hash and inventory the stable runtime and stable user-project stores before and after all canaries and
  lanes; any mutation is P0 and stops the suite.
- All lane state must live below one task-scoped root supplied through `WORKBENCH_RUNTIME_DIR`; do not
  reuse historical E2E runtime databases or browser profiles.
- Parent must use asynchronous child processes with bounded concurrency and per-lane logs. `spawnSync`
  is prohibited for long product workflows. Crash/restart resumes from durable product job locators,
  never restarts a completed product-AI step or fabricates a receipt.
- Cleanup stops only task-owned processes by recorded PID/process-start identity and removes only
  task-owned ephemeral ports/profiles. Evidence and manifests are archived, not deleted.

## Done And Stop Conditions

- First manager pass produces a concrete file ownership map, sequence and canary gate before workers.
- Harness structure tests prove the lane matrix, real input identity, per-lane isolation, no credential
  serialization, durable resume, exact product receipts, all-chapter traversal, citations, figures/
  scales/tables and DOCX outputs. Source-string assertions alone do not satisfy behavior contracts.
- A deterministic dry canary may run without external services. Real product-AI runs wait for explicit
  Codex release after W5 independent audit and harness code review.
- Stop immediately on stable-runtime mutation, credential leakage, mismatched/mislabelled source,
  product-service substitution, cross-lane state leakage, stale result adoption or missing exact
  receipt lineage.

## Work Items

1. Define 12-lane design/input oracle manifest, stable-runtime non-contamination contract, model/service identity schema, and deterministic canary/release gates
2. Implement isolated parent/child orchestration with per-lane runtime, API/Vite/CDP/browser profiles, immutable input/output manifests, crash recovery, and cleanup
3. Implement real workflow driver for project creation, source import, CT.gov retrieval/download/parse/translation, corpus admission, chapter candidates/adoption/rewrite, citations, tables/figures and DOCX export using product APIs and UI where required
4. Implement evidence collection and QC including server-side product-AI receipts, all-chapter completeness, reference links, SVG/scale embedding, DOCX package checks, browser artifacts, failure injection, and Word-native acceptance handoff

## Completion And Cleanup

Codex reviews the manager report and final artifacts. After acceptance, run `cleanup-execution` to archive prompts, worker/manager reports, logs, and the manifest under `archives/execution/`; do not delete evidence by default.
