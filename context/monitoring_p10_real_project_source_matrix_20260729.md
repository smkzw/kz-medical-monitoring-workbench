# 医学监查 P10 真实项目原始来源矩阵

**状态：** P10 前置盘点，尚未代表发布验收通过  
**目的：** 固定“从原始方案与原始 listing 起步”的测试边界，禁止从既有
Timeline、Patient Profile、风险清单或已解构中间文件进入工作流后半段。

## 1. 权威性分级

| 级别 | 定义 | 可承担的验收 |
|---|---|---|
| A | 方案原文和项目级全量 EDC data listing 均可追溯，内容/项目/文件角色校验通过 | 真实全流程、规则金标准、发布门 |
| B | 真实项目文件，但经过解密、还原、人工比较或其他加工；转换前后关系可说明 | 兼容性、解析、失败恢复、开发回归 |
| C | 已解构、已筛选、既有技能过程文件、Timeline/Profile 成品或演示数据 | 仅单元/视觉夹具，不得作为真实 E2E 起点 |

## 2. 三项目矩阵

### 2.1 RUX-03-002

- 方案：V1.3 清洁版方案原文；Source Registry 已解析。
- 当前项目级全量 listing：
  - 原始 Excel 版本已登记并形成批次
    `monbatch_dffbfdc95d1e4ae7bbac6abb1227dbcb`；
  - 处理后 Excel 版本已登记并形成批次
    `monbatch_83f5d7adf70f485d98c762bb3b970169`。
- 当前画像规模：52 个域、180113 行。
- 历史定期 listing 已完成只读核验；首选 2025-04-16 快照
  （SHA-256 `376a299e8c01617371b26d9d6dd59bd42cbb642304b2c1d440faedd9fbfc2ea8`）：
  53 个核心 EDC sheets、179565 行、240 名受试者、20 个中心。它与 2025-06-12
  当前快照存在真实新增、删除/撤回、同键修改和 schema 扩展，可用于增量 LOOP。
- 该历史快照必须标记为 B 级 `processed_full_snapshot`，并通过独立的历史批次日期
  与来源说明表达其历史角色：虽没有人工比较
  sheet，但存在 Apache POI 创建和人员重存痕迹；本地没有同日期未经重存的原始导出，
  不得升级为 A 级来源权威。
- 当前定位：
  - 原始 Excel + 方案可作为 A 级全流程主项目；
  - 处理后版本用于同源解析差异、兼容性和防回归，不替代原始版本。

### 2.2 MG-K10-SAR-001

- 方案：V2.1 清洁版方案原文；Source Registry 已解析 2762 个内容片段。
- 项目级锁库后全量 EDC listing 已登记并形成批次
  `monbatch_075b99489baf448a88b83c483ecf8eab`。
- 当前画像规模：61 个域、148727 行。
- 当前定位：可作为 A 级跨适应症、跨数据结构的第二权威项目。
- 禁止以既有 Subject Timeline、Patient Profile、评分 SDV、自查结果或技能过程文件作为
  起点；这些只用于最终输出形态和医学结果交叉核对。

### 2.3 MY009-UC-2-01

- 方案：真实 IIa 期方案原文已登记并解析。
- 当前 listing 为“已自动还原”版本，已形成批次
  `monbatch_71a1d2023a4b4432b954ffc74934a60e`。
- 当前画像规模：56 个域、4023 行。
- 本地还存在不同日期的医学比较/Comparison listing，可用于跨批次兼容性和增量差异
  研究，但均带有还原或人工比较语义。
- 当前定位：B 级开发项目。原始导出、还原过程和完整性未形成可追溯证明前，不用于规则
  家族第二项目权威、金标准发布或最终上线门。

## 3. P10 最低真实验证组合

1. RUX 原始全量批次：主路径、真实独立 AI 字段映射、Timeline、Profile、统一风险、
   日常监查、锁库/核查前汇总。
2. MG-K10 全量批次：跨项目泛化、吸入/鼻用等治疗相关规则、复杂合并用药与量表结构、
   同一规则族第二项目验证。
3. MY009 当前批次：不同数据规模和字段结构、加工来源告知、override/拒绝路径、失败重试、
   不得越权升级为发布权威。
4. 增量差异必须至少选取一组经过内容核对的真实不同时点全量 listing；同一快照的
   “原始/处理后”只能测试解析一致性，不能伪装成时间增量。
5. 已选定 RUX 2025-04-16（B 级历史全量）→ 2025-06-12（A 级当前原始）作为
   第一组真实增量对。该组合可证明产品增量链路，但“来源权威通过”只能分别按各批次
   的实际等级表述。

## 4. 放行前必须补齐

- 为每个入选 listing 固定来源条目、内容哈希、批次、快照日期和文件角色。
- 排除 Office 临时文件、中心筛选版、结果比较版和局部 listing 被误识别为全项目快照。
- 为增量对固定基线/当前批次顺序，证明新增、修改、消失与持续风险来自真实快照差异。
- 三项目均从 Source Registry 导入、批次解析、字段画像、规则/AI 运行开始，禁止跳步。
- 最终报告分别标识“医学结果正确”“产品链路通过”“来源权威通过”，三者不得相互替代。
