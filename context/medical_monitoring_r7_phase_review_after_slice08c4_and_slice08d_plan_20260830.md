# R7 大阶段复盘（Slice-08C-4 后）与 Slice-08D 计划

日期：2026-08-30  
状态：`R7_IN_PROGRESS_SLICE_08D_NEXT`

## 1. 面向医学监察员的阶段判断

08C 已把跨轮连续性真正带到项目、中心、风险和受试者访视轴：用户能先看到本轮新增/升级/重开/需重新判断及中高风险，再沿同一身份进入中心、受试者、风险详情和来源。08C-4 经过十轮执行纠偏与八轮独立视觉复核，1920×1080 宽屏首屏已没有半字、半行或内部工程术语。

当前缺口不再是“页面能否看懂”，而是同一套事实在三种监查模式、不同 hash seed、进程中断和失败恢复后是否仍保持完全一致。只有 08D 把这些综合回归闭合，Slice-08 才能整体复盘；之后还要完成 Slice-09 的备份/迁移/审计/容量等本地产品韧性，R7 才能总体评估。

## 2. Slice-08 对账

| 子切片 | 已完成 | 仍不外推 |
|---|---|---|
| 08A | continuity 领域事实、七类变化、三模式基线、registry v3 与 publication CAS | 真实 R5/R6 authority、UI、真实项目 |
| 08B | R5 authority、R6 四输出、artifact member/bytes SHA、registry v4 原子发布 | 中文 UI、综合回归、真实医学质量 |
| 08C | 项目/中心连续性看板、唯一访视轴 Journey、详情抽屉、ego(lite) 桌面视觉 | 三模式全矩阵、长稳、真实项目/模型 |
| 08D | 未开始 | 三模式综合回归与 Slice-08 总体接受 |

## 3. 08D 用户价值目标

- 日常监查：full 与 incremental 使用同项目、同模式、已发布且兼容的基线；新增、升级、持续、降级、关闭、重开、需重新判断在重复运行和恢复后不漂移。
- 锁库前：每轮都输入新的完整 listing；Query 后修订只有证据完整时才显示相应归因，普通数据修订不被误称为 Query 结果。
- 核查前：固定总量、截止点、规则和决定版本共同绑定；任一变化形成新运行，旧看板不被污染。
- 用户不需要理解 run/hash/artifact/CAS；综合回归只验证这些内部事实是否稳定支持现有中文看板。

## 4. 08D 实施步骤

1. 冻结 08D 验收矩阵，不改写 Slice-08 v0.2 语义。
2. 建立 synthetic 日常 full、日常 incremental、锁库前 full 修订、核查前 fixed-total 四类场景；每类覆盖 compatible baseline、无基线、错误基线、同值重放和冲突。
3. 对七类变化、五项首屏摘要、continuity plan/item、R5/R6/publication/member-set/bytes SHA 做逐对象对账。
4. 以不同 `PYTHONHASHSEED`、优化级别、SQLite 关闭重开和相同请求重放验证字节/摘要/顺序确定性。
5. 覆盖 staging→verified→published、publication publishing→available 的失败注入、迟到回调、恢复和回滚；不得出现半发布。
6. 复跑 08A/08B/08C、R7、产品医学监查、R5/R6/R2 共享消费者的最小相邻回归。
7. 做独立工程/医学语义会商；只有 P0-P4 为零才接受 08D，并随后进行 Slice-08 总体复盘。

## 5. 非目标与停止线

- 不新增页面、动画、移动端适配或视觉风格；08C-4 已冻结当前桌面视觉。
- 不运行五个真实项目，不读取真实 listing，不调用真实模型，不输出医学结论。
- 不修改医学写作，不扩展系统安全设计/测试。
- 8911/5174 保持停止；仅运行 synthetic/offline 单元、属性、故障和产品接口测试。
- 08D 通过后先做 Slice-08 总体复盘，再进入 Slice-09；不得直接跳到 R8。
