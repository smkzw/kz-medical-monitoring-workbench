# R7 Slice-07A 复盘与 Slice-07B 阶段前计划

日期：2026-08-28  
状态：`R7_SLICE_07B_PHASE_BEFORE_REVIEW`

## 1. Slice-07A 已证明什么

真实进度页已在 synthetic 范围内完成受限验收：页面数字来自 manifest/work-unit，支持
离页后台运行、刷新恢复、停止确认和终态回看；医学监察员视图不暴露停止等管理动作，失败
状态不再造成“100% 即完成”的错觉。R5 默认页与 R7 进度页可在同一产品页面正常共存。

最终视觉会商对修订后的失败态给出 `accept_limited`。当前接受边界、测试数量、ego(lite)
截图和服务停止证据以
`context/medical_monitoring_r7_slice07a_progress_ui_acceptance_record_20260828.md` 为准。

## 2. 尚未证明什么

Slice-07A 没有证明项目/中心受试者阶段流向、图表与受试者明细对账、跨运行变化、真实项目
医学正确性、Patient Journey 或 R7 总体完成。现有 R5 权威包只有受试者身份、事件、访视和
风险，没有可作为流向依据的阶段与转移记录；不得从风险、访视名称或事件标签猜测研究状态。

## 3. Slice-07B 用户任务

项目或中心默认页打开后，资深医学监察员应无需配置即可在一屏内看清：受试者从知情同意、
筛选、入组/随机、治疗到随访/研究结束的主流程；筛选失败、已入组未治疗、永久停药、退出、
失访等关键分流；每个节点与流向的人数；相伴的中高风险；以及可逐例核对的数据依据。

图不是装饰。点击节点或连线必须筛选同一份下方明细；图、表、风险数和覆盖说明必须同源且
精确对账。用户看到的是中文研究阶段和医学含义，不看到内部 ref、哈希、模型、日志或研发
状态词。

## 4. Slice-07B 分步计划

1. 冻结权威数据合同：阶段目录、受试者阶段记录、转移、日期状态、原因、完整性、来源定位
   和前次运行变化；阶段由项目方案/数据解析结果提供，产品层不写死。
2. 扩展 R5 authority packet 与 overview projection；项目和中心继续复用同一只读 overview
   路由及严格 project/run/snapshot/cutoff/site 身份。
3. 用一份规范化 `subject_flow` 投影生成横向流向图、节点/连线筛选和下方受试者表；建立
   双向对账门禁，任何人数不一致均拒绝展示为正常结果。
4. 在现有 R5 默认页中加入摘要优先的全宽流向区；风险采用独立徽标叠加，不改变流向颜色
   语义；桌面宽屏 1280/1440/1920 默认完整显示主流程。
5. 保留项目/中心/阶段/风险筛选上下文，一跳进入同一运行与截止点下的受试者医学旅程；
   返回后恢复此前看板位置与筛选。
6. 使用 synthetic 数据覆盖正常主链、关键分流、未知/缺失、不适用、日期冲突、更正、空态
   和小样本；运行确定性回归、构建、ego(lite) 交互与独立视觉会商。

## 5. 停止线

不运行五个真实项目，不把 synthetic 对账外推为医学质量，不重构 Patient Journey，不修改
医学写作子系统，不使用 Playwright。本切不设计、实现或测试系统安全功能。只有流向图、
明细表、风险叠加、中心范围和跳转上下文全部通过同源对账及 ego(lite) 验收后，才可形成
Slice-07B 范围受限接受。

