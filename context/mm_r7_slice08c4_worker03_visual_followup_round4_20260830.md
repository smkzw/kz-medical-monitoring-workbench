# 08C-4 worker_03 same-session visual remediation round 4

Continue the same bounded execution session. Read independent conference Round 2 at `runs/conference/mm_r7_slice08c4_visual_acceptance_20260830/visual_single_object_round2.md` and close the remaining evidence/product gaps. The frozen contract is authoritative.

Required repairs:

1. `result-context:s08c4-current` is comparable incremental: remove `当前为首个监查版本`; retain prior comparison. Restore the required first-screen five KPIs `新增 / 升级 / 重开 / 需重新判断 / 中高风险` above the four-stage flow+homologous table on current project and site pages. First token remains clearly “当前完整数据（不使用比较基线）”.
2. Risk entry `risk_instance_ref=risk-multi-001` must expose both aggregated continuity rows with a 2-row switcher and exactly one pressed row, not merely a distinct title. Fix product/fixture mapping as needed and recapture all viewport risk drawers.
3. Recreate the complete 1920 evidence folder at exactly 1920×1080, including overview, site, Journey, event/risk drawer, host1196 push, host1195 overlay, keyboard/reduced-motion, first/tamper states and structured JSON.
4. Tamper: keep exact `本轮变化暂不可查看`, and hide/neutralize all incremental change KPIs, summaries and table change cells. It may show stable subject flow/current status, but no unavailable delta values.
5. Ensure risk overlay uses the same visible 0.28 scrim as event overlay and capture both at 1280.
6. Fix the homologous table last-column clipping and the `研究状态` node badge/connector collision across viewports.
7. Polish subsystem-native wording and scan quality: remove duplicated `日常监查 日常监查`; replace `一跳进入医学旅程` with `查看受试者医学旅程`; remove duplicated `不随图上筛选变化`; make site return control explicitly `返回项目风险概览`; make the Journey axis-local scrollbar/scroll affordance visible when lanes extend below the still.
8. Set FINDINGS/manifest non-clear before recapture and only return to `p0_to_p4_clear=true` when new PNG, JSON and collage agree. Refresh all counts/mtimes/must-open paths.

Use no new dependencies/assets, preserve current design system and legitimate clinical abbreviations. Rebuild, focused tests, run only 8984, use ego(lite), open all repaired originals/proofs, stop 8984 and clear task spaces. Return exact residual P0-P4; do not claim Codex final acceptance.
