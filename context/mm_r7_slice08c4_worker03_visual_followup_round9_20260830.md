# 08C-4 worker_03 same-session visual remediation round 9

Continue the same bounded execution session. Read visual-conference Round 6 at `runs/conference/mm_r7_slice08c4_visual_acceptance_20260830/visual_single_object_round6.md`. Close the final P2-P4 without waivers.

Required work:

1. At 1280 Journey, keep `按缩放级别聚合` together: widen/rebalance the banner columns or place a deliberate line break before `按`, never split `级别`.
2. Ensure every 1280/1440 domain chip avoids single-character orphan wrapping; compact two-by-two wrapping such as `检验` / `检查` is acceptable, but 3+1 is not.
3. Thin the single table hrail and Journey vrail to a refined native-weight treatment while keeping them persistently visible, linked, accessible and discoverable. Do not reintroduce native duplicate rails.
4. Adjust overview density by a few pixels so the first visible risk item's supporting meta line is either fully readable or wholly below the 900px fold; never slice glyphs at the viewport edge.
5. Recapture tamper in the R9 ego packet so it uses the current cadence line, table cue/rail, full-word edge, and still preserves exact `本轮变化暂不可查看` with no aggregate/item delta leakage.
6. Refresh all MANIFEST nested `file_mtimes`, task-space metadata and notes to current R9 evidence.
7. Set structured clear false before capture. Use ego(lite) native `captureScreenshot()` only. Recapture affected 1280/1440 Journey, overview, site, first-token, tamper and any 1920 state changed by rail styling. Rebuild relevant collages and open the original PNGs at full resolution.
8. Re-run focused tests/build. Start only 8984; stop it and complete all ego task spaces with `keep:false`.

Keep the accepted whole table edge, single linked rails, cadence wording, flow spacing, study-status risk label, site return, aligned transition reason and drawer semantics. No other browser, no new dependency/asset, no medical-writing edits. Return exact residual P0-P4; do not claim Codex final acceptance.
