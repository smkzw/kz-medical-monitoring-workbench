# R7 Slice-07A 真实进度界面受限验收记录

日期：2026-08-28  
决定：`ACCEPT_R7_SLICE_07A_PROGRESS_UI_SYNTHETIC_LIMITED` +
`ACCEPT_R7_SLICE_07A_PROGRESS_UI_VISUAL_SYNTHETIC_LIMITED`

## 已接受范围

R7 持久化 Run/work-unit 进度已最小接入现有医学监查页面。用户可看到服务器权威的
总项数、已处理数、阶段分布、当前工作与最近进展；支持等待开始、运行中、正在停止、
已停止可继续、失败、结束但有未完成项、完成七态，并支持刷新/离页返回、停止确认、
继续入口与中文失败/空态。

本次同时关闭：Vite dev React StrictMode 生命周期死锁、运行中文案重复、医学监察员首屏
无权动作、终态历史记录层级、失败 100% 的完成错觉、R5 不可用占位、同项目多 Run
Profile bridge 幂等冲突、未准备错误码被其他运行 identity 遮蔽及 FastAPI 0.137 路由
内省测试漂移。

## 决定性证据

- Codex 独立回归：R7 + 产品路由 `195 passed`；医学监查前端相邻 `49/49` test files；
  隔离夹具 `16 passed`；失败态聚焦渲染合同 `61 passed`；Vite build 通过。
- ego(lite) 修复后实测：1280×800 无加载死锁或横向溢出；running 中文案无重复；
  monitor 首屏无动作；admin 停止确认正确获得焦点并在 9 秒检查时自动恢复。
- worker_02 已用 ego(lite) 覆盖七态、1366/1440/1920、轮询/刷新、键盘停止确认、403、
  未准备/无运行与读取中断恢复；其截图由 Playwright 对同源同 URL 代拍，未冒充 ego 截图。
- Codex 重新打开 v2 1920×1080 全页渲染；失败态为红底白字“本项未完成”、
  降权的深红数字进度和弱化错误轨道，下方 R5 合成看板正常呈现。
- 独立视觉会商在同一 session 中两次要求修订，最终对 v2 给出 `accept_limited`；
  未将旧图、模型自评或通过的测试数当作视觉验收替代品。
- 8911、5174 以及本轮临时 5176、5177、8978 均已停止；未读取真实临床项目，未修改
  医学写作产品源码或权限模型。

## 未接受范围

不接受真实项目、医学发现质量、三模式端到端、R7 总体、安装/升级、Patient Journey、
项目/中心受试者流向看板或医学写作。ego 当前实例的 `Page.captureScreenshot` 仍会 CDP
超时，这是工具环境缺口；不影响已完成的 ego DOM/键盘/网络实测，但后续视觉切片仍应
保留可见截图与 Codex 复核。

## 下一阶段

进入 Slice-07B：先冻结项目/中心“受试者阶段流向”看板合同，再实现同一截止点、同一
范围、同一运行版本下的 Sankey 与下方受试者表格双向对账。先用独立 synthetic 数据证明
阶段/转移/分母与风险叠加，不提前接真实项目，也不同时重构 Subject Journey。
