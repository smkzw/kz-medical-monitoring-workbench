Delegated mode. Continue the same bounded execution session for task `mm_r7_slice09d_implementation_20260831`, role `worker_03`.

Stay inside `artifacts/mm_r7_slice09d_implementation_20260831/`; do not write runner reports, product source, real projects, services, browser/model paths, or medical-writing files. You may read/import accepted 09A-09C seams and use isolated temporary directories/databases only. Codex conference confirmed that the current recovery JSONL is self-simulated and several semantic seam names are wrong. Apply a focused remediation:

1. Rename the current self-generated rows/status/schema unambiguously as simulated contract examples; they must not say independent observed evidence or `pass` for real recovery behavior.
2. Correct every semantic seam address against actual R1/R7 symbols and expand `_SEAM_REQUIREMENTS` so all named semantic seams are existence-validated. Missing seams must fail closed.
3. Add observed recovery evidence for the real 42 backup and 42 migration `FAILURE_HOOK_POINTS` by invoking the accepted hooks on synthetic temp workspaces or by a deterministic structured runner over the existing accepted exhaustive hook tests. Each JSONL row must record the actual hook hit, actual terminal/recovery observations, oracle result, and originating test/runner identity; it may not copy expected fields as observed values.
4. Keep other semantic cases explicitly `not_executed` unless you implement a real isolated injection. Do not use a named-oracle string as proof.
5. Extend DTO leakage validation to Chinese technical labels such as 表名/错误码/堆栈/技术日志 and regenerate scoped artifacts/manifest. Run focused tests plus the relevant real backup/migration exhaustive-hook tests and the full R1/R7 adjacent suites if time permits.

Return a concise execution handoff with files changed, observed-vs-simulated evidence counts, exact test commands/results, remaining unexecuted cases, and next step. Do not claim §5 closure unless all frozen cases have real observed evidence.
