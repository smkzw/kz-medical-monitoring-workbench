Delegated mode. Continue the same bounded execution session for task `mm_r7_slice09d_implementation_20260831`, role `worker_03`.

Stay inside `artifacts/mm_r7_slice09d_implementation_20260831/`; do not write runner reports, product source, real projects, services, browser/model paths, or medical-writing files. You may read/import the accepted R1/R7 seams and tests and use isolated temporary directories, SQLite databases, subprocesses, monkeypatches, fake clocks, and deterministic synthetic fixtures only. Codex has already corrected the overdeclared launch-registry backfill hook list in product source; the current 82 source-enumerated backup/migration hook rows all report real `observed_pass`.

The remaining blocker is the 18 semantic rows currently labelled `simulated_contract_example` / `not_executed`. Implement the smallest truthful observed semantic runner needed to close as many frozen §5 cases as the actual seams support:

1. Execute each semantic case through its real named R1/R7/09D seam, not by mapping to a test name and not by copying the matrix's expected terminal/user/oracle fields into observations. The runner must directly cause the stated interruption/fault in an isolated synthetic setup and read actual persisted state, return/exception, recovery result, forbidden side-effect absence, and relevant public DTO projection.
2. Cover, where supported by the actual code: claim/begin/attempt interruption; async cancel; non-cancellable sync transport; expired lease/heartbeat; generation fence; late callback; dependency failure; audit append and audit-head CAS conflict; technical-log lock/ENOSPC/fsync/rotation; resource low-disk/watchdog/sustained-RSS. Use deterministic barriers/fake clocks/patches and bounded waits; do not rely on real disk exhaustion or high real memory pressure.
3. Add a distinct evidence kind/schema for actually executed semantic cases and a validator that fails closed on missing origin, missing actual injection observation, missing persisted-state/recovery/public-projection observations, expected-field leakage, or inconsistent oracle result. Do not relax validation merely to make rows pass.
4. Update `build_validation_evidence()` so each semantic case is either a genuine observed semantic row or remains explicitly `not_executed`; counts/status must truthfully reflect both. §5-complete status is allowed only when all 18 semantic cases plus all source hook cases have observed passing evidence and validators are clean.
5. Add focused negative/mutation tests proving fabricated observations, missing injection hits, missing persisted evidence, unexpected side effects, and wrong public projection are rejected. Regenerate JSON/JSONL/manifest artifacts deterministically.
6. Run the full artifact suite and the smallest directly relevant R1/R7 tests. Report exact executed/pass/fail counts and any genuinely unsupported semantic case. Do not change production code to manufacture a seam and do not claim §5 closure if any case remains simulated, unexecuted, or failed.

Return the standard concise execution handoff. Include every changed artifact file, exact commands/results, observed semantic count, remaining unexecuted count, observed hook count, and the precise blocker for any remaining case.
