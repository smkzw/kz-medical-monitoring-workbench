Delegated mode. Continue the same bounded execution session for task `mm_r7_slice09d_implementation_20260831`, role `worker_02`.

Stay inside `artifacts/mm_r7_slice09d_implementation_20260831/`; do not write runner reports, product source, real projects, services, browser/model paths, or medical-writing files. Codex conference found runner-contract gaps. Apply a focused remediation:

1. Use the current profile's event count when applying the T0-derived balanced watchdog; add a T2/T3/T4 test proving tier scaling before the 3600 s cap.
2. Replace hardcoded source revision with a truthful, recomputable source-revision input: auto-detect when possible and allow explicit commit/dirty-tree evidence for a non-git copied workspace. Missing evidence must remain `inconclusive_environment_drift`; never convert unknown to clean.
3. Make profile order dependency-safe: C04 must be evaluated before any C05 gate; C05 skip must not truncate unrelated profiles. A red/yellow/inconclusive result may stop only the contract-defined higher tiers, not same/lower-tier shape/fanout cells.
4. Implement an explicit confirmation/bisection planner matching the frozen contract instead of uniform 30 trials for all 15 profiles. It may be tested without running the 24-hour matrix.
5. Compute last-all-green balanced and adjacent non-green from available records; bounded proof must remain explicitly incomplete. Rename/qualify the JSONL-only backend as `synthetic_fixture_io` and prevent its output from being presented as accepted 09A-09C product capacity.
6. Regenerate only the final bounded proof directory (do not preserve new v5-style trial debris), run focused tests, and record remaining gap that an accepted-seam backend/full 30-cell run is still required if not implemented.

Return a concise execution handoff with files changed, evidence, test counts, remaining limits, and exact next step. Do not claim full capacity closure.
