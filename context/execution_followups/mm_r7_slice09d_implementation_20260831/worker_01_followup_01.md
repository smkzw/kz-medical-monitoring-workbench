Delegated mode. Continue the same bounded execution session for task `mm_r7_slice09d_implementation_20260831`, role `worker_01`.

Stay inside `artifacts/mm_r7_slice09d_implementation_20260831/`; do not write runner reports, product source, real projects, services, browser/model paths, or medical-writing files. Codex conference found anti-overfit and oracle gaps. Apply a focused remediation:

1. Extend `scan_source_text` to catch identity-specific branches when identity is an Attribute or Subscript, when the string literal is on the left, and for membership against tuple/list/set literals. Keep generic AST logic; do not enumerate project IDs beyond test fixtures.
2. Make the static evidence scan every package `.py` implementation/generator/schema file while excluding tests and generated evidence. Document the explicit domain-token policy.
3. Expand corpus mutation verification beyond C01 to at least balanced, wide, deep, sparse, high_entropy, and fanout representatives; keep evidence compact and deterministic.
4. Remove the shared-function self-oracle loop: implement an independently coded input-side gold reconstruction used by `build_input_oracle`, distinct from the verifier's observed-stat path, and add a mutation/test that would survive a defect in the verifier-side stats helper but be rejected by the independent gold.
5. Regenerate fixed corpus/static/mutation artifacts and run focused tests. Preserve the current bounded-proof conclusion; do not claim final acceptance.

Return a concise execution handoff with files changed, evidence, test counts, remaining limits, and exact next step. Do not merely describe a patch; implement and verify it.
