Delegated mode. Continue the same bounded execution session for task `mm_r7_slice09d_implementation_20260831`, role `worker_02`.

Stay inside `artifacts/mm_r7_slice09d_implementation_20260831/`; do not write product source, runner-managed reports, real projects, services, browser/model paths, or medical-writing files. You may read/import accepted R7 Slice-09A–09C product seams and use isolated synthetic temporary workspaces/SQLite only.

The recovery/bounded checkpoint is accepted, but frozen contract §3/§4 remain open because the existing backend is only `synthetic_fixture_io`. Implement the smallest accepted-seam measurement adapter and a bounded T0 proof:

1. Add a versioned backend/adapter that executes the seven frozen workloads through real accepted 09A–09C seams on an isolated synthetic project workspace: first backup, same-value backup, first restore into fresh target, same-value restore, public read/verification, close-and-reopen/recovery-ready, and progress observation. Reuse `ProjectBackupManager`/accepted restore and 09C verifier/background/progress public reads rather than duplicating file-copy behavior.
2. Keep `synthetic_fixture_io` as runner self-test only. Accepted-seam records must identify a distinct backend (for example `accepted_09a_09c_seams`) and retain product ledger/result states, correctness observations, package/workspace inventory, and exact seam identities. Do not label an adapter as accepted if it silently falls back to JSONL copy/hash.
3. Map opaque synthetic corpus objects into generic workspace content without disease/drug/scale/risk/listing-column constants. No project-specific branches or hard-coded clinical semantics.
4. Preserve the frozen cold/warm, calibration, watchdog, stop, environment, confirmation and capacity-statement contracts. The adapter must be selectable without changing the existing schema truthfully; update schema/tests/manifest as needed.
5. Run only a bounded T0 accepted-seam proof (cold+warm, seven workloads, enough repetitions to validate schema/correctness but not the full 30-cell/24h run). Its report must say `bounded_proof_incomplete=true` and `accepted_product_capacity=false`; it may establish adapter readiness, not §3/§4 capacity closure.
6. Add focused tests that fail if any accepted-seam workload routes to the old synthetic file-I/O implementation, if fresh restore is not fresh, if same-value behavior is confused with full operation, or if public read/reopen/progress observations are absent.

Return exact files, commands/results, bounded sample counts, seam identities, failures, and the next safe full-run command. Do not start services/models/browser or the full 30-cell run, and do not claim 09D acceptance.
