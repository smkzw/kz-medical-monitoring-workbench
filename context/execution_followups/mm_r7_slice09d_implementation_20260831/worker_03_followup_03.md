Delegated mode. Continue the same bounded execution session for task `mm_r7_slice09d_implementation_20260831`, role `worker_03`.

Stay inside `artifacts/mm_r7_slice09d_implementation_20260831/`; do not write product source, runner-managed reports, real projects, services, browser/model paths, or medical-writing files. Make only the narrow post-conference repair below.

Authoritative decision: the frozen contract §6 `project_public_status(case.user_status)` 12-row Chinese mapping is the contractual user projection for §5. A real product overlay, where available, is a separate runtime observation and must not be substituted for or conflated with that contract projection. Resource cases are deterministic 09D guard injections/rechecks, not product recovery; preserve this explicit label.

Required changes:

1. Remove runner-chosen DTO strings. For every semantic case, build the contractual DTO from the case's `user_status` through the frozen §6 mapping and the real `ProjectVerificationDTO(...).as_dict()` renderer. Store it under an explicitly named contract-projection section. Where a real adapter/product overlay exists, retain it separately as `runtime_overlay_observation`; do not claim it equals the contract DTO unless it actually does.
2. Make `validate_observed_semantic_evidence` independently compare the contractual DTO to `project_public_status(case.user_status)` exactly. Reject vocabulary drift, missing projection source, wrong result/message/nextAction, or an overlay disguised as the contract projection.
3. Add an independent, per-case raw-observation oracle in `fault_recovery_matrix.py` (separate from runner booleans). Recompute the critical invariant for each of the 18 case IDs from nested raw fields: actual exception/hook/clock/return observation, persisted before/after or journal contents, recovery result/state, forbidden side-effect absence, and terminal state where applicable. `oracle_result=observed_pass` must require both structural checks and this independent raw oracle. Do not merely re-evaluate `hit/read/invoked/observed/forbidden_absent` booleans.
4. Add boolean-preserving nested mutations for all major domains (interrupt/cancel/lease-generation/callback-dependency/audit/log/resource), such as false exception with hit=true, forbidden persisted transition with forbidden_absent=true, wrong recovery state, wrong contract DTO, and fake healthy resource recheck. All must be killed.
5. Keep §3/§4 status unchanged and explicitly incomplete; do not claim product capacity. Generate a consolidated §8 total manifest covering corpus, measurement-final, fault/recovery, DTO, static/mutation, regression-evidence references, and the current contract hash, without copying large payloads. The manifest must hash only existing files and validate itself deterministically.
6. Regenerate artifacts and run the full artifact suite plus directly relevant R1/R7 tests. Return exact results and any remaining gap. Do not claim final acceptance.

Use the smallest stdlib-first change. If a per-case raw invariant cannot be independently recomputed from current evidence, leave that case non-passing and report it rather than weakening validation.
