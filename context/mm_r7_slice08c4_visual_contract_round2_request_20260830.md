# 08C-4 visual contract same-session round 2

Continue the same bounded read-only acceptance session. Re-open the revised contract, the new synthetic fixture spec, and the frozen Sankey asset. Verify whether every Round-1 P0/P1/P2 is now closed without starting services or judging runtime pixels.

Explicit Codex decisions now on disk:

1. Current live `DOMAIN_LABELS` is the eight-domain Chinese authority; old screenshots are structure-only.
2. Flow badges are in scope and must use the seven current change labels; old aliases are banned.
3. Live/envelope split, deterministic identities, multi-row event/risk cases, continuity route, and browser-reachable states are frozen in the fixture spec.
4. `.r5-subject-columns` and `host_width=1195|1196` are the deterministic overlay/push boundary method.
5. The P0-P4 rubric is frozen and no old O3 exemption carries forward.

Return only: boundary statement; P0-P4 findings on the revised contract/spec/assets; exact residual edits if any; and verdict `ACCEPT_CONTRACT` only if P0-P2 are zero. Do not start a service, invoke ego(lite), edit files, or claim visual acceptance.
