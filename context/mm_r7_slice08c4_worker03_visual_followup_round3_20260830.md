# 08C-4 worker_03 same-session visual remediation round 3

Continue the same bounded execution session. Independent visual conference rejected ACCEPT. Read its report at `runs/conference/mm_r7_slice08c4_visual_acceptance_20260830/visual_single_object.md`, but use the frozen contract and current pixels as authority.

Implement and ego(lite)-verify these open items:

1. 1440×900 push drawer: `查看来源证据` must be visibly present in the screenshot without requiring the page itself to scroll. Use an internal scroll area + sticky footer inside the 420px aside; close/title/switcher/current summary/source all remain in viewport. Recapture 1440 event, 1440 risk and 1440 host1196 push, then rebuild the collage.
2. Risk-entry evidence at 1440 and 1920 must be opened from `risk_instance_ref=risk-multi-001`, not stale `event_ref`; screenshot title/content must differ from the event entry and DOM/URL evidence must prove the row set.
3. Tamper view: show exact `本轮变化暂不可查看` and no “当前为首个监查版本” or prior-result comparison claim. Preserve fail-closed behavior.
4. Current comparable project and site default pages must include a real four-stage `知情同意→筛选→治疗→研究状态` Sankey/flow plus a homologous table on the default dashboard. Reuse the existing subject-flow component/data if available; do not clone the English reference or add a dependency. The same source data must drive both chart and table. Capture current 1280/1440/1920 project and site first screens.
5. 1280 third risk card: domain/severity badge must be fully visible within 900px in the captured default Journey state. Reduce vertical card waste without lowering the 14/12px hierarchy or hiding clinical identity.
6. Eliminate the observed orphan wraps for `仅看需重新判断` and `精确日期` at the affected viewports. Use intrinsic sizing/grid hierarchy, not smaller text.
7. Replace fixture-facing `多行事件示范` with a realistic Chinese clinical-monitoring title while preserving multi-row test semantics. `Query 草稿`, TFL, PV and the corporate English subtitle are legitimate domain/brand abbreviations and need not be removed.
8. The computed overlay alpha 0.28 is contract-compliant and Codex visually confirmed the scrim; retain it, but capture a still where the dim layer is unambiguous.

Before editing, set the current evidence gate to non-clear or avoid relying on it. Then rebuild, run focused tests, restart only 8984, use ego(lite) for repaired captures and structured evidence, open every new original/collage, update pack manifest, stop 8984 and clear task spaces. Return exact P0-P4 residuals. Do not claim Codex final acceptance.
