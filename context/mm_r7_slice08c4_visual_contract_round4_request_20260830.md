# 08C-4 visual contract same-session round 4

Continue the same bounded read-only acceptance session. Re-open the contract and fixture spec after the result-context prefix correction. Confirm all named result tokens satisfy the live `result-context:` prefix gate and all Round-1 through Round-3 P0-P2 are zero. The contract now also freezes numeric backdrop/shadow polish bands.

Return only boundary statement, residual P0-P4, and verdict `ACCEPT_CONTRACT` if and only if P0-P2 are zero. Do not start services, use ego(lite), edit files, or claim runtime visual acceptance.
