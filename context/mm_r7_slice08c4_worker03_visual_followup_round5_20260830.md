# 08C-4 worker_03 same-session visual remediation round 5

Continue the same bounded execution session. Read the independent Round 3 rejection at `runs/conference/mm_r7_slice08c4_visual_acceptance_20260830/visual_single_object_round3.md`. Close every remaining P2-P4 item rather than only the two blocking examples.

Required repairs and recapture:

1. At 1280 and 1440, the homologous table must have complete headers and no orphan `对`. Preserve all medically useful columns. A contained table-local horizontal scroll is acceptable only if its affordance is visibly obvious within the card; prefer responsive column sizing/wrapping that keeps the common first-screen scan simple. No page-level horizontal overflow.
2. Move the red `中高 1` annotation fully off the treatment-to-study-status connector at 1280/1440/1920. Keep the study-status node's `中高风险 1` clear of its border and connector.
3. Remove the duplicate site chrome label so there is exactly one clear `返回项目风险概览` navigation control.
4. Remove the inspector meta dangling separator/ellipsis. Ensure text wraps cleanly.
5. Make the Patient Journey axis-local vertical/horizontal scroll affordance visible when lanes continue below the fold, without creating page-level overflow.
6. In tamper state, hide the item-level `变化原因：新增或修订数据` and `重开` continuity label as well as aggregate deltas.
7. Replace check-state chips such as `路径完整 1` / `阶段人数已核对一致` with concise medical-monitor-facing wording, or remove them if redundant.
8. Use `数据完整性` consistently at all viewports.
9. Set FINDINGS/MANIFEST non-clear before capture; recapture new overview and site originals at 1280/1440/1920 plus Journey/tamper as affected; rebuild `overview_flow_strip`, `site_flow_strip`, `1280_overview_side_by_side`, and matched reference/prototype comparison evidence. Only set structured clear after PNG/JSON/collage agree.

Re-run focused tests/build, start only 8984, use ego(lite), open the original screenshots and matched comparison evidence, then stop 8984 and clear task spaces. No new dependencies or assets. Preserve medical-writing. Return exact residual P0-P4; do not claim Codex final acceptance.
