# R5-S5 temporal-projection authority delta context (2026-08-20)

## Goal and stop rule

Freeze an append-only, synthetic/non-clinical authority delta that closes the documented 169-leaf construction gap without rewriting the accepted 17-object subject-temporal schema, 13-object AE/MH history schema, accepted parent contract, accepted semantic delta, accepted R5/S4 artifacts, or either rejected implementation snapshot. If any accepted output leaf cannot be mapped to an independently supplied authority root or an exact executable recipe, generation and verification fail; mirroring an output, target, fixture, case, sentinel, or example is forbidden.

## Read and preservation boundary

Before the first edit, the worker fully read 75 specified files (32,660,155 bytes): current `AGENTS.md`; System Design v1.1; the R0-R8 implementation plan; the accepted S5 plan/context; the accepted parent contract, all of its artifacts/tools and acceptance record; the accepted semantic delta, all artifacts/tools and acceptance record; rejected implementation-contract v0.1/v0.2 prose, artifacts and tools as negative evidence; accepted R5 v0.3/S4 contract records/artifacts/tools; and every typed R1/R2/R4/R5 file pinned by the accepted parent manifest. Thirty-one JSON files parsed fully, 25 Python files parsed fully through AST, and all prose decoded as UTF-8. The accepted parent 24 artifact/source pins and accepted semantic delta seven artifact pins matched before writing.

Only the nine paths named by the author prompt are writable. Parent, semantic, S4/R5, rejected v0.1/v0.2, root init, all product/runtime/test/evidence/acceptance surfaces, and the 542-file medical-writing aggregate remain byte-preserved. Port 8911 must remain stopped. No service, browser, real project, model, producer, runtime, test, evidence or acceptance record is authorized.

## Fresh 272-leaf audit

The ledger is derived from every property of the accepted parent schemas, not from rejected expected values. It contains exactly 156 subject leaves and 116 AE/MH leaves. The frozen before-delta classification is:

- direct typed authority: 53 (subject 28; AE/MH 25);
- deterministic closed derivation: 46 (subject 18; AE/MH 28);
- accepted semantic authority: four subject leaves;
- unconstructible: 169 (subject 106; AE/MH 63).

The classification is field-level: `A` is a typed value that already has an accepted source path; `B` is a closed constant/canonical identity/hash derivation over accepted leaves; `C` is one of the four exact semantic leaves already accepted; `D` lacks either an independently selectable authority record or a closed recipe after dependency propagation. The delta ledger keeps those before labels and assigns every former `D` leaf to one of 18 authority-root families or an exact recipe output. Every one of the 272 rows carries a qualified target leaf, one exact authority/recipe output path, and a non-empty dependency chain. The verifier requires a recipe-mapped target to occur literally in that frozen recipe's `outputs`; exact leaf-output bijection is 272, nominal mappings are zero, and post-delta unexplained count is zero. The 272 deletion probes prove only structural coverage and are never counted as positive constructibility evidence.

## New authority boundary

The schema freezes 25 exact-key authority objects, 37 exact-key named/nested types and 38 closed enums. Its complete manifest-owned canonical baseline is independently hard-pinned as `9f2784b14c7e9f1ac6d151d340f98b834e449185af3c0ffcc0fd32f0a93c197c`. The recursive exact comparator freezes object/named-type/enum sets, required and optional field partitions, properties, types, enum members, cardinality, nullability, constraints and conditional rules. Twenty generic JSON-pointer mutations cover additions, removals and changes across these dimensions; no field name receives a validator special case. Runtime instances are checked against the same closed baseline and reject unknown keys at every depth.

The candidate receipt is never authority. The immutable synthetic registry root is owned only by manifest section `/external_acceptance_registry_root`; its hard-pinned root hash is `580d367783f8a00e68e8779fceec9146ddadd2bc3dd0040d26937d415b0589b3`, and its complete canonical section hash is `65e2d571ba6858c65c2a82957639968b8f00431f2df56549a4c09f55733832cd`. The single accepted record's full canonical payload is manifest-owned and pinned as `295a916100ea53efe816fc79ae818fc4bb0ef38a75eaaf258f3a58e314cbf391`. A runtime candidate must carry an authority record exactly equal to that accepted payload in every field; a changed policy hash payload fails even under legitimate refs/root and a fully resealed chain. Separately typed operational evidence may vary only in `evaluation_instance_ref`, `observed_sequence`, optional `transport_digest`, and its self-hash; these fields cannot carry policy semantics. Real clinical acceptance remains external and absent.

## Recipes and controlled policies

Eighteen executable recipes close cutoff present/absent, visibility, locator/revision, timezone/day-zero, endpoint, visit/event/phase/risk/domain/pending, AE/MH append/evidence/thread/prefix, dependency hashes and immutable pins. Their complete definitions live only in manifest section `/accepted_recipe_registry`, hard-pinned as `91687b6d7aa7acb3d0fa24732e2872d81e7ec597c76a9d4d863dfdab40f84e3a`; all 18 per-recipe hashes are also frozen in manifest and verifier. `recipe_registry.json` carries references, hashes and closure declarations, while candidate execution supplies only a recipe ID. A study-day recipe changed to count with its fixture resealed to expected output 10 fails the accepted recipe pin before execution. The generator neither interprets recipes nor backfills expected outputs; the independent verifier resolves and executes only frozen definitions.

Controlled packages freeze only policy semantics: phase code to native Chinese label, `Asia/Shanghai` plus day-zero/day-one formulas, exact/partial/conflicted/missing range projection, externally governed empty-domain applicability, and the AE/MH reminder/match/withdraw/reappear transition matrix with lifecycle effect fixed to `none`. They reference the accepted semantic registry for event/risk semantics and do not duplicate its packages.

## Executable authority-output closure

The 119 former-D `authority_root` leaves are separately frozen in manifest section `/accepted_authority_output_registry`, hard-pinned as `2b92462366004c46bd460927514627d209cc608f3cf23688b8501ac03fad3607`. Every mapping declares the exact qualified leaf, principal authority root, ordered fixture identities and their content-hash pins, the exact source field paths actually consumed by that leaf, operation enum, one non-null op-specific parameter object, six-key scope join, output type/cardinality/nullability, canonicalization and hash recipe. The verifier requires the runtime registry to byte-equivalent canonical content with this manifest-owned registry and never treats the mutable ledger declaration as proof.

All 119 mappings have sealed positive fixtures and a sealed execution-trace hash. Six closed branches are exercised: `direct_field=67`, `derive_value=2`, `project_record=13`, `assemble_sorted_records=4`, `canonical_hash=13`, and `project_contract=20`. Direct copy, closed contract-ID selection, exact-one joined record projection, stable-identity sorted assembly, canonical JSON SHA-256, and nested contract projection each enforce distinct required/forbidden parameters and output rules. The executor resolves only each mapping's ordered fixture slice, reads only its declared field slice, and returns the consumed fixture IDs, field paths, verified fixture pins and computed output. The verifier requires the returned trace to equal the declaration exactly; an unused or missing declared dependency fails. There are 91 unique dependency slices; combined fixture-plus-field dependency counts are minimum 9, median 15 and maximum 194; no mapping declares all 19 fixtures, and unused declared dependencies are zero.

The second `EventProjectionBinding` has a distinct fixture identity, binding/activity/event identity, locator source and content identity. Event assembly sorts by stable target identity. Each mapping pins fixture content by fixture ID, so a fully resealed same-type value exchange still fails the accepted authority-output registry's source pins. Counts remain `authority_mapping_count=119`, `executable_authority_mapping_count=119`, `unconsumed_authority_fixture_ref_count=0`, and `authority_leaf_without_emitter_count=0`.

The 18 recipe fixtures also have recipe-specific ordered authority bindings frozen in manifest-owned registry `3bce92047ffb6d3173d1e22dbc7c59db922c4a4e75f7cf602c31511f4f71c463`. Every requirement binds exact fixture ID, expected object kind, semantic role, fixture content hash and scope join keys. The recipe executor resolves these bindings before interpreting IR; cyclic reassignment across all 18 recipe fixtures fails rather than passing a set/union check.

## Challenge and acceptance boundary

The challenge registry has 418 distinct one-mutation cases: 272 structural leaf-bijection deletions and 146 typed semantic/contract mutations. Separately, 18 executable recipe fixtures exercise every frozen recipe, and 119 executable authority-mapping fixtures exercise every former-D authority mapping. The semantic typed-probe count is 283; the structural coverage-probe count is 272; generic schema mutation coverage is 20. The 146 active controls include all 30 ordered substitutions among the six projection ops without output edits plus six op-specific negative outputs, so every branch has a positive and a negative fixture. The packet/receipt fixture carries an explicit dependency DAG (`root_hashes -> packet_hash -> receipt_hash` and `external_registry_hash -> receipt_hash`), and the verifier independently proves computed hashes and propagation.

Active attacks retain all prior controls and additionally reject replacement of every mapping slice by the global dependency union, a no-op mapping operation, cyclic reassignment across all 18 recipe authority-ref lists, every ordered cross-op substitution, and one wrong output per op branch. The earlier cyclic root exchange, fully resealed same-type event exchange, wrong scope, field-path drift, member drop/add/reorder and authority-registry reseal controls remain active. Every case contains exactly one typed mutation record and one exact expected error; immutable pins and exact traces are checked before recipe or authority projection acceptance.

This worker does not accept the delta. Only a fresh isolated reviewer may return `ACCEPT_R5_S5_TEMPORAL_PROJECTION_AUTHORITY_DELTA_V0_1` for the exact manifest bytes. That verdict would unlock only implementation-contract v0.3 generation and would not unlock or accept producers, S5, UI/browser, real project/model, clinical authority, medical writing, product or production.
