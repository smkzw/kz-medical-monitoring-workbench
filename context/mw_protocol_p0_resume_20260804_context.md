# Task Context: mw_protocol_p0_resume_20260804

Created: 2026-08-04 18:47:28
Objective: Resume Protocol P0 from the saved PICOS completion blocker; prove substantive full-draft and Word path before serial role acceptance.
Task type: `clinical_document_router`
Risk: `high`
Selected agent route: `codex` / `codex-main` / `high`

## Trigger Reason

This task was initialized through the Codex x Hermes complex-task entrypoint because it is expected to involve more than three execution steps, research/writing/report/code/report-visual work, or source-grounded verification.

## Source Of Truth

- `/Users/smkzw/Documents/康哲项目资料/AI/医学经理工作台/implementation/workbench/context/mw_protocol_p0_full_draft_20260804_context.md`
- `/Users/smkzw/Documents/康哲项目资料/AI/医学经理工作台/implementation/workbench/runs/MW_PROTOCOL_P0_FULL_DRAFT_GAP_RESUME_20260804.md`
- `/Users/smkzw/Documents/康哲项目资料/AI/医学经理工作台/implementation/workbench/reviews/codex_mw_protocol_p0_full_draft_gap_20260804_review.md`
- `/Users/smkzw/Documents/康哲项目资料/AI/医学经理工作台/implementation/workbench/metrics/mw_protocol_p0_full_draft_gap_20260804_metrics.md`
- Current source under `services/api/app`, `frontend/src`, and focused tests under `tests`; current filesystem and isolated runtime are authoritative.
- Stable API `127.0.0.1:8900`/PID 33143 and frozen r42/v36/ready/excluded/history remain outside the write/test scope.

## Scope

- In scope: read-only diagnosis of the saved PICOS completion blocker; minimal source/test repair if required; isolated API/Vite and real headful Playwright proof through substantive Protocol full draft, candidate adoption, DOCX unzip/read/render/link checks; independent role configuration contract; later strictly separated serial tester work.
- Out of scope: Synopsis/CSR, stable runtime, r42/v36 immutable rows, old retries, triage/download/OCR/retranslation/attempt-2/3/ready/excluded reruns, and any API/backend click in user-perspective acceptance.

## Success Criteria

- The current saved journey reaches a truthful, auditable PICOS-complete state without mutating old lineages.
- The isolated visible flow produces substantive source-bound prose in every applicable Protocol section and a DOCX that passes unzip/paragraph/table/TOC/reference-link/render checks; heading-only/placeholder output remains blocked.
- LLM, OCR, translation, and OCR/translation-support roles remain independently configured; only the requested thinking controls are exposed and the effective provider/model/reasoning settings are auditable.
- Engineer and senior-medical-monitor user tests are separate tasks, use real visible clicks for user perspective, start from clean projects, and do not count a round unless the complete Word path is substantive. P0-P4 claims require two consecutive clean rounds per role/tester.
- Every material result is recorded in this task's context/run/review/metrics and the prior lossless records.

## Risk Boundaries

- Do not write to production paths until Codex review gate passes and writable paths are explicit.
- The delegated agent is not final authority; Codex owns verification and acceptance.
- Keep the current test inputs `systemic` exposure, `注射剂` dosage form, and `多中心` center model labeled as user-entered test facts, not external clinical evidence.
- Do not silently coerce unsupported AI response shapes; preserve fail-closed error, observed provider/model, idempotency, and audit evidence.
- All external tester prompts must be role-specific, concise/open-ended, preflighted, and use declared route policy; do not mix engineer and user roles or dispatch duplicate sessions.
- The first live Word proof is the gate before broad tester matrix; any blank/headings-only Word is P0 and cannot count.

## Timeout Policy

- Do not mark the delegated agent failed for slow response alone.
- For complex or artifact-heavy work, wait and poll generously; use conference mode when multiple independent model perspectives are needed.
- Failure requires terminal error, provider exhaustion/rate limit after controlled retry, empty/truncated retry output, or no progress after hard wait plus one retry.

## Loop Log

- 2026-08-04 18:47:28: Task initialized by `tools/hermes_workflow_guard.py init-task`.
- 2026-08-04 19:05-19:18: Isolated API `8913` was corrected to load the source tree via explicit `PYTHONPATH`; runtime readiness and Vite build IDs matched. Headful Chrome/Playwright selected `proj_user_414af710ad8e` and navigated through the visible 医学写作 workspace.
- 2026-08-04 19:18: The saved journey was read through the UI as revision 33, `stage1_complete`, `current_stage=picos`, `framing_complete=true`, `picos_complete=false`, despite every required PICOS value being present. The source repair in `services/api/app/medical_writing_authoring_journey.py` was exercised by a real visible completion flow: after a visible draft/revert setup to obtain a fresh idempotency key, “完成第二步” returned revision 38, `status=corpus_not_ready`, `current_stage=corpus`, `picos_complete=true`, `picos_draft=null`. Screenshot evidence: `/private/tmp/mw_p0_picos_reconciled.png`.
- 2026-08-04 19:20: An upstream framing commit caused the UI's existing automatic search path to create `wref_search_7ab493c1f6ef7bf2eaef`. The terminal historical batch `wref_translation_batch_8488879d054d781b86ae1916` remains immutably bound to `wref_search_9e730dd999fdc1d63b81`; the two snapshot payloads contain the same 49 candidate identities/26 public documents, but no alias/rebind route is authorized. No old triage/download/OCR/translation/retry/ready/excluded path was started or rewritten after this observation. This is a lineage boundary, not a reason to reuse the old batch.
- 2026-08-04 19:20: Current safe next action is an isolated substantive full-draft proof using a clean, already-prepared corpus/runtime or a new snapshot lineage; do not click “重新检索竞品”, “启动AI分诊”, preparation, translation, retry, or exception override on the 7ab snapshot until the lineage plan is explicitly verified. Broad external tester dispatch remains gated on a non-placeholder Word proof.

## 2026-08-04 current continuation record

- The current bounded work moved to a new isolated rich clone `/private/tmp/mw-p0-rich-proof-20260804`, API `8916`, Vite `5190`; stable API `8900`, r42/v36 immutable rows, old batches, and medical-monitoring files remain outside scope.
- Visible Playwright generated a 9/9 substantive full-draft candidate, adopted it once, and froze nine sections. The candidate UI contained no internal transport markers. The first rich DOCX rendered with 12 headings, 15 body paragraphs, 3,379 body characters, one structured table, and an update-on-open TOC field, but is diagnostic only because the synthetic source packet still contains unresolved confirmation language and legacy internal markers.
- A strict content-quality gate now detects internal transport/test vocabulary and unresolved drafting markers in any section or table cell. After hot-loading the fix, a bounded final export request failed closed at assembly with 15 open blocking findings. This is the current expected outcome; do not dismiss or confirm these findings just to force an artifact.
- The rich role status endpoint reports independent `current_runnable=true` bindings for DeepSeek LLM/translation-support, official PaddleOCR-VL-1.6, and oMLX Hy-MT2. Encrypted credential material is confined to the isolated temp runtime and not copied into durable records as plaintext. This is readiness evidence, not proof of a live OCR call.
- Next safe action: resolve the missing factual inputs through an explicit source-bound fact packet or maintain the actionable blocker; then rerun only the isolated full-draft/adopt/freeze/export path. The external serial engineer/user matrix is still not authorized by the evidence gate and remains unstarted.
- Candidate validation now mirrors the final export gate: AiTaskRunner rejects unresolved confirmation/evidence wording before adoption, while the repair prompt directs the provider to return complete native regulatory prose. Focused candidate/content-quality tests are 12/12; no bypass or sanitization is permitted.
- Visible recheck after the hot-loaded backend clicked 正式 Word once and displayed the content-quality export error with no download; screenshot /private/tmp/mw-p0-rich-proof-20260804/final_export_blocked.png is retained as user-surface evidence.
- Adoption also rechecks pre-existing full-draft artifacts; a focused test proves internal or unresolved wording cannot be replay-adopted into any working copy.

## 2026-08-04 isolated substantive Word proof (current continuation)

- A fresh copy of the prepared greenfield fixture was isolated at `/private/tmp/mw-p0-word-proof.Q2YFFQ`; API `8915` was started with a valid process-level DeepSeek route, and Vite `5189` was rebuilt so the frontend/runtime build IDs match (`api-503b80992ae58dc3`, `web-efbcb0328119fade`). Stable API `8900`, prior `8913/8914` fixtures, old snapshots/batches, and medical-monitoring files were not changed.
- Runtime readiness and role binding remained independently auditable: comprehensive LLM and translation-support use DeepSeek V4 Flash with thinking controls; OCR is pinned to official PaddleOCR-VL-1.6; body translation is pinned to oMLX `dawncr0w--Hy-MT2-30B-A3B-oQ8-MLX`. No secret value was copied into task records.
- Headful Playwright selected the isolated project and clicked the visible `生成全文初稿` once. Durable job `mwjob_260415de8fc35bd4e625d4fc` completed in one chunk with `2/2` candidate sections, provider `deepseek`, model `deepseek-v4-flash`, and a persisted candidate artifact. The browser then clicked `整体审核后采纳并写入` once, created/saved/froze the three current section working copies through visible controls, and clicked `正式 Word` once. Download: `/var/folders/yb/31r9763x6_54mdxswxk36c4w0000gn/T/playwright-artifacts-tY7Kxr/6792f469-a920-4091-9746-5d24656170cc` (`SYN-PICOS-401-202_V0.1_方案终稿.docx`).
- Structural DOCX check: 9 paragraphs, 4 non-heading body paragraphs, 745 body characters, 3 applicable sections represented, no placeholder-regex hits, OOXML TOC field with hyperlink switch, one section property, and no tables/references in this deliberately minimal fixture. The Word exporter emitted the expected update-on-open TOC field; the cached field text is `更新域后显示目录`, so cached TOC hyperlinks were not claimed as rendered evidence.
- Visual QA first used the generic renderer and showed missing CJK glyphs because the runtime environment maps `宋体` poorly. The workbench's deterministic `scripts/render_docx_visual_qc.py` was then used with its explicit CJK alias configuration: 3 pages, 54 Chinese spans, all 54 with visible pixels, CJK font embedded, expected heading present, report `/private/tmp/mw-p0-word-proof-visual-qc/visual_qc_report.json`, `passed=true`. Page images were inspected; layout is readable. This is render evidence for the isolated fixture, not a claim of submission readiness.
- The generated prose is substantive and source-bound but intentionally carries test-only wording such as `隔离fixture`/`ProtocolAssemblyPlan`, and the fixture has only three sections and no references/tables. Therefore this proof closes the prior headings-only/empty-body gate for the isolated path, but does **not** yet satisfy the user's final real-protocol quality bar or count as a tester round. Broad role matrix remains gated.
- Next safe action: preserve this positive end-to-end artifact and add a bounded content-quality guard/prompt rule preventing internal fixture/platform tokens from entering user-facing full-draft prose, then obtain a richer applicable-section fixture (with synopsis/reference/index objects) before serial engineer/user acceptance. Do not touch old r42/v36 records or rerun old preparation/translation/triage paths.

## 2026-08-04 continuation — user-surface export blocker repair

- The isolated rich runtime remains the only active proof surface (`8916` API, `5190` Vite, `/private/tmp/mw-p0-rich-proof-20260804`). Stable `8900`, frozen r42/v36 rows, old preparation/translation lineages, and medical-monitoring files remain outside scope.
- A real headful Playwright click on `正式 Word` now surfaces: `终稿暂不能导出：仍有章节含待确认事实或内部标记。请点击“内容核查”逐项修订并重新冻结。` No artifact is downloaded. Screenshot `/private/tmp/mw-p0-rich-proof-20260804/final_export_blocked_user_plain_v2.png` is current visual evidence.
- Source repair is limited to `frontend/src/App.jsx` error mapping plus a frontend contract assertion. Raw backend error detail is retained for diagnostics; no content-quality finding is dismissed or sanitized.
- Verification: frontend contract 120/120; full-draft/content-quality/greenfield 32/32; DOCX exporter 24/24; `npm run build` pass, 1,952 modules, existing chunk warning only. The strict blocker remains intentional.
- Current safe next action: build an explicit source-bound fact packet/missing-input review gate for the isolated rich clone, then rerun only full-draft → adopt → freeze → formal Word. Do not dispatch the serial engineer/user matrix until the resulting Word has substantive applicable sections, references/tables, and no blocking content-quality findings.

## 2026-08-04 continuation — source-bound complete-content rerun

- Product change: MedicalWritingRevisionService._current_project_study_definition_source now exposes every confirmed, section-relevant StudyDefinition fact needed for a substantive full draft (structured design, product profile, intervention/regimen, concomitant-treatment rules, endpoints, visit schedule, estimand, sample-size and statistical strategy). Typed booleans render as 是/否; unresolved/manual-candidate paths remain omitted. The full-draft prompt and repair prompt explicitly require confirmed numeric/parameter facts to be written directly and prohibit future补写 language.
- Content-quality change: UNRESOLVED_DRAFT_MARKER_RE now blocks explicit future-formal-text, missing-fact, post-confirmation, and unlisted-reference wording in addition to prior confirmation/evidence markers. Unit/runtime focus passed 33/33 after the change.
- A new isolated clone /private/tmp/mw-p0-source-bound-completeness.ZY1mL6 was created from the prepared source-bound fixture. Only its temp SQLite working copies were reset to empty body text with monotone fixture revisions 5→6; source facts, StudyDefinition revision 9, plan, and historical rows were preserved. Reset audit: /private/tmp/mw-p0-source-bound-completeness.ZY1mL6/source_bound_completeness_fixture_reset.audit.json. No stable API, r42/v36 rows, or medical-monitoring files were touched.
- Headful Playwright on http://127.0.0.1:5192/ selected the visible SYN-PICOS-401 project, opened 医学写作, clicked visible 生成全文初稿 once, and waited for durable job mwjob_0d887129c14c9a3a6bc47ea8 to complete. Provider/model were DeepSeek / deepseek-v4-flash; artifact coverage is 12/12, AI run IDs airun_20260804140659_9b6cd373 and airun_20260804140944_988ccfcc, candidate artifact SHA-256 02185ea311c93b3655c7e9f6a686837ed0b095e7854ac789f3d4995e6811a4f6. All 12 candidate sections are substantive (228–455 Chinese characters) and contain no internal or unresolved-marker hits. Confirmed facts visibly appear in the candidate (240 participants, 18–75 years, daily topical dosing for 12 weeks, EASI/IGA/DLQI, weeks 2/4/8/12, and specified statistical methods).
- The same visible page clicked 整体审核后采纳并写入 once and then visibly froze all 12 sections (研究设计, 研究干预, 安全性评价, 研究背景与依据, 研究目的与估计目标, 研究人群与入排标准, 访视与研究流程, 有效性评价, 安全性评价方法, 统计分析考虑, 伦理与受试者保护, 参考文献). API freeze-readiness is ready=true, current_frozen_section_count=12, gaps empty; content-quality is zero findings/open/blocking; study-consistency is current; document-index returns 200 with empty tables/figures.
- The same visible page clicked 正式 Word once. Download: /private/tmp/mw-p0-source-bound-completeness.ZY1mL6/SYN-PICOS-401-202_V0.1_方案终稿.docx. DOCX SHA-256 6e5204a4115e803de6068dc76bba2c9ae3f8f9a59d5d8e32d2c2c242a3d0da2a, size 47,724 bytes; export job mwjob_0b065609bb68a96b20cae152 completed on first attempt. OOXML has 21 parts, 3,988 extracted chars, 8 tables, a TOC field with w:updateFields, and no unresolved/internal marker hits. The generic deterministic visual QC passed: 9 pages, 296 visible Chinese spans, embedded CJK fonts, expected text layer; report /private/tmp/mw-p0-source-bound-completeness.ZY1mL6/word_render_qc/visual_qc_report.json. Representative pages 1, 2 and 6 were visually inspected. Cached TOC text remains 更新域后显示目录 by design; the Word field is configured to update on open, so cached TOC links are not overstated as rendered evidence.
- Verification after source expansion: frontend contract + full-draft/content-quality/greenfield focus 153 passed; DOCX exporter 24 passed; npm run build passed with 1,953 transformed modules and only the existing large-chunk warning.
- Acceptance boundary: the blank/heading-only P0 is closed for this isolated source-bound path, and a substantive no-marker 12-section Word proof now exists. This is not a production-ready clinical submission: the fact packet is explicitly synthetic/isolation-only, independent engineer/user rounds are still unstarted, and final Word acceptance for real clinical inputs remains open.
- Next safe action: preserve this evidence, then validate the requested independent-role route/configuration and begin separated serial engineer/user acceptance only after the workflow guard manifest is valid. Do not reuse the synthetic Word as a clinical deliverable or alter old r42/v36/medical-monitoring records.

## 2026-08-04 route/configuration gate

- Isolated API `8918` read-only readiness: `ready=true`, build `api-a5f27716129356fd`; all four independently configured roles report `current_runnable=true`. Comprehensive LLM and translation-support are DeepSeek `deepseek-v4-flash` with thinking enabled and selectable intensity; OCR is official `PaddleOCR-VL-1.6`; body translation is oMLX `dawncr0w--Hy-MT2-30B-A3B-oQ8-MLX` and the shared gate is ready with 0 active OCR/translation leases. No secret value was read into this record.
- Executable route policy was consulted, not hand-copied. At `2026-08-04T22:21:08+08:00`, the Beijing night policy `beijing-deepseek-v4-flash-night` was active; exact Pi/Alibaba Qwen Preview nodes are replaced by `pi/alibaba/qwen3.8-max:xhigh`, and DeepSeek V4 Flash primary nodes are similarly replaced with the same effective primary while the declared DeepSeek node becomes fallback 1.
- Requested `pi/xai-oauth/grok-4.5` and `pi/cms-router/minimax-m3` fail closed because neither triple is in `/Users/smkzw/.codex/tools/route_policy.py` allowlist. No route-policy edit or unvalidated dispatch is authorized by this checkpoint. Allowed substitutes for later serial tests are `grok/grok-build/grok-4.5` and `codebuddy/codebuddy-cli/minimax-m3`; `pi/opencode-go/deepseek-v4-flash`, `codebuddy/codebuddy-cli/hy3`, and `codebuddy/codebuddy-cli/glm-5.2` are allowlisted subject to the night-window rewrite and availability.
- This is a route-readiness result only. No tester task, login, project cleanup, OCR/download/translation run, or P0–P4 round has started. Before dispatch, create two separate role-specific prompt/manifests, preflight each, and validate the route chain through the guard; never combine engineer and senior-medical-monitor roles in one session.

## 2026-08-04 engineer role round 1 — Grok Build (completed)

- Policy-valid route: `grok/grok-build/grok-4.5`, health check passed, one same-session pass, no fallback. Runner session `819ed436-aa17-480e-9a93-7d7b45da80c1`; runner duration 1,814.619 s; report `runs/role_acceptance/mw_protocol_p0_20260804_round1_engineer_grok.md`.
- The agent created its own isolated clone `/private/tmp/mw-p0-engineer-r1.HcoIX1`, API `8920`, Vite `5200`, and used headful Playwright. It created a genuinely new non-oncology Phase I chronic spontaneous urticaria project (`proj_user_b469452350fc`, SYN-CSU-101, randomized double-blind placebo-controlled SAD/MAD design). It did not reuse the synthetic source-bound Word or stable runtime.
- Visible path reached public search (22 candidates), DeepSeek triage 2/2, and the explicit visible `确认全部排除并继续` action after a 10+ character no-suitable reason. Read-only pipeline evidence confirms `confirmed` triage, retained=0, no preparation batches/artifacts, no translation batches, and terminal `failed` with `no_retainable_candidates_after_confirm`; framing/PICOS remained incomplete and no Word was produced. Role status independently showed DeepSeek LLM/support with thinking, PaddleOCR-VL-1.6, and oMLX Hy-MT2 with `current_runnable=true`.
- Reproducible findings: P0 empty-retain basket was a terminal dead end even when no public Protocol/SAP was suitable; P0 full-draft/Word unreachable; P1 weakly signaled 10-character reason and CTA label mismatch; P1 new session defaulted to the first demo project; P1 drug technology field was hard to locate; P1 corpus gate offered no visible shared-corpus/manual-upload continuation; P2 AI-settings entry discoverability; P2 ChinaDrugTrials `reachable_no_api`; P2 durable parent job remained `completed` while pipeline projection later failed. Evidence root `/private/tmp/mw-p0-engineer-r1.HcoIX1/evidence` contains screenshots, prompt hashes, JSON state, and job IDs.
- Bounded repair now in source: an empty confirmed basket transitions to `awaiting_corpus_admission` with an auditable `fallback_mode=shared_corpus_or_manual_upload`, exact error code, and explicit message that no download/OCR/translation was started. It no longer terminal-fails or triggers heavy work. The banner surfaces the shared-corpus/manual-upload or explicit exception path. No stable runtime or old lineages were touched.
- Verification after repair: research-pipeline validation + frontend contract focus 143/143, py_compile pass, Vite build pass (1,953 modules; existing large-chunk warning). Next safe action is to restart only a fresh isolated runtime with this repair and run the separate senior-medical-monitor user role; do not count the engineer pass as clean and do not dispatch the user role against the old failed clone.

## 2026-08-04 user role round 1 — Grok Build (completed; defect finding)

- Separate policy-valid Grok Build pass used headful Playwright against clean isolated runtime `/private/tmp/mw-p0-user-r1.d5bf40` (API 8921, Vite 5201), with Phase III ulcerative-colitis project `proj_user_fbf06e400317`.
- The visible path reached framing and public search but stopped at `failed/search_no_results`, `returned_count=0`; no download, OCR, translation, full draft, or Word evidence exists. Screenshots and JSON are under `/private/tmp/mw-p0-user-r1.d5bf40/evidence/`; the report explicitly gives no clean-round credit.
- Codex traced the P0 to the zero-result branch bypassing the earlier empty-confirmed-basket fix. It now routes to `awaiting_corpus_admission/no_public_protocol_results`, preserves the search/registry audit, and explicitly records that heavy stages did not start. Frontend fallback detection and compact copy are aligned and no longer say “公开研究检索已保留” for zero results.
- Post-repair checks: 163 focused tests passed, backend py_compile passed, and Vite build passed with only the existing chunk-size warning. The stale user clone cannot validate the patch; next safe action is a fresh isolated user-role rerun.

## 2026-08-04 user role follow-up — Grok Build (same session; repair pass not clean)

- Same Grok user session `c623a8f7-0550-4078-898b-1faec560d9c1` completed one targeted continuation (runner pass 2, no fallback). It created a new clean runtime `/private/tmp/mw-p0-user-r1-fu.4c301f` (API 8922, Vite 5202), zero projects, and a distinct Phase II Crohn disease project `proj_user_0239b0c720cb` using only natural product/indication/phase facts.
- Fresh visible Playwright evidence confirms the repair: `stage=awaiting_corpus_admission`, `error_summary=no_public_protocol_results`, `returned_count=0`, HTTP 200 registry reachability, zero preparation/translation/artifact rows, and explicit UI copy “公开检索未发现可保留Protocol；未启动下载、OCR或翻译…”. The previous false “已保留”/terminal failure is closed for this branch.
- The follow-up correctly diagnosed why the result was still not a usable end-to-end path: the visible `语料准入` action opened a long raw-JSON design form with “13项待决定”, not a clear shared-corpus/manual-upload/exception chooser; the tech-type gate still blocked “完成第一步”; stale copy still implied active AI work (`0/4 收集`) while waiting. It produced no PICOS, OCR/translation, full draft, or Word, so the pass is not clean and gives zero streak credit.
- Independent connectivity/strategy check: official ClinicalTrials.gov API returned HTTP 200 for both queries; Chinese qualified labels returned zero studies while English `Crohn's disease` and `ulcerative colitis` returned studies. The root cause is deterministic alias matching, not outage. `medical_writing_condition_term_resolver.py` now performs longest-known-Chinese-alias containment matching for severity/activity-qualified labels; resolver regression tests cover both IBD variants.
- Post-repair verification: 168 focused tests passed (resolver + pipeline progress/validation + frontend contract), py_compile passed. Next safe action is a fresh engineer/user role round from current source after validating the resolver query contract; do not count this follow-up as clean.

## 2026-08-04 current-source UX/root-cause repair before next user rerun

- Backend zero-result fallback now explicitly resets the child projection to `phase=corpus_admission`, `total=0`, label `等待医学经理选择语料来源`; stale `正在收集第一轮语料分析的准入来源 0/4` cannot survive the soft-wait transition. The fallback context still records no downstream heavy stage.
- Frontend framing now follows the backend's actual minimum contract: unknown product technology type/route does not block the initial framing submission; the UI keeps the fact visibly pending for IB/evidence confirmation instead of forcing an arbitrary user selection. Full first-step submission remains explicit.
- CorpusGate now permits an already version-bound StudyDefinition to enter the existing auditable exception-writing route (`access_permitted=true`, `readiness_status!=ready`) instead of permanently disabling the writing CTA; normal ready-state assembly-plan checks remain unchanged.
- The empty-result banner has a direct visible “查看语料准入” action. It names the current evidence boundary honestly; it does not promise a nonfunctional upload route when no candidate/NCT exists.
- Composite AI design defaults are rendered as human-readable Chinese (`启用状态：否；具体特征：未设置` etc.) rather than raw JSON scaffolding. Backend fail-closed candidate/path gates are unchanged.
- Role prompts now require first-principles root-cause analysis for every unexpected result, including zero-result connectivity vs alias/filter/query diagnosis and completion/button/state contradiction checks. All three prompts preflight successfully.
- Verification after repair: 168 Python focused checks, backend `py_compile`, and Vite build (1,953 modules; existing large-chunk warning only). No service was started by Codex, no old frozen lineage was touched.
- Next safe action: launch one fresh, separate senior-medical-monitor Grok user pass on a clean isolated clone from this source; inspect whether the visible fallback reaches PICOS/exception/full draft/Word and whether the previous raw-JSON/technology blocker is actually gone. Keep zero clean-round streak at 0 until a complete role pass is evidenced.

## 2026-08-05 user role round 2 — Grok Build and current-source response-shape repair

- A fresh policy-valid Grok Build senior-medical-monitor pass used headful Playwright against clean isolated runtime `/private/tmp/mw-p0-user-r1b.1fd879` (API `8923`, Vite `5203`), with zero projects at start and a distinct IgA nephropathy Phase III project `proj_user_8b21b1f5ccdb`. The route completed one runner pass with no fallback; this is a defect-finding round and contributes zero clean-streak credit.
- First-principles evidence: `IgA肾病` was resolved to `IgA nephropathy`; ClinicalTrials.gov returned HTTP 200 and 51 Phase III interventional candidates. DeepSeek triage retained 1 (`NCT03608033`) and excluded 50. Visible Playwright then reached real Protocol download, native extraction (~147 pages), OCR on 7 pages, and Hy-MT2 translation of key sections. The failure occurred only at round-1 corpus analysis, so this is not a registry connectivity or download/OCR/translation failure.
- Durable failure evidence is `/private/tmp/mw-p0-user-r1b.1fd879/evidence/pipeline_terminal_fail.json`: stage `failed`, prep `wref_prep_cf64f732a494303480fb3af6`, translation batch `wref_translation_batch_f28af813aee5cacc4cf1d2f7`, error `CorpusAnalysisAiError` with root keys equal to the nine finding keys and all six analysis-envelope keys missing. No round-1 analysis, PICOS completion, full draft, freeze, or Word artifact exists. The child stage and 0/4 wording observed before terminal projection were traced to the user-facing stale/hidden retry state, not accepted as progress evidence.
- Root cause is a provider response-shape mismatch: the independent AI returned one otherwise-shaped `finding` object as the top-level JSON object, while the server requires the analysis envelope. The strict finding-level evidence, numeric and indication guards were not at fault. The prior durable retry consumed three attempts with a second provider-level JSON/SSE error and left the user without an immediately visible retry action; no raw backend response was treated as valid evidence.
- Minimal source repair: `medical_writing_corpus_analysis_ai.py` bumps the prompt version to `competitor_protocol_corpus_analysis_v10`, explicitly forbids a standalone finding root, and permits only the exact nine-key single-finding shape to be wrapped with server-derived `schema_version`, `analysis_id`, `project_context_hash`, `findings`, `evidence_gaps`, and `global_conflicts`. The wrapper is marked `single_finding_root_repaired` under `single_finding_root_repair_v1`; all other partial/unknown shapes remain fail-closed. The original provider object and normalized envelope are retained separately in the result for auditability.
- Minimal UI repair: failed-but-retryable pipeline states now expose `重试后续语料分析` in the compact competitor toolbar, not only inside a collapsed progress detail. The failure copy maps the observed schema/JSON failures to a task-facing explanation and states that completed source/OCR/translation can be reused. Existing narrow `/research-pipeline/resume` idempotency and no-repeat download/extraction route is reused.
- Verification after repair: corpus-analysis plus pipeline/frontend waiting focus `104 passed`; frontend Vite build passed (`1,953` modules, pre-existing large-chunk warning only). No service was started by Codex and no stable/r42/v36/medical-monitoring rows were changed. The current user clean streak remains `0`; the next safe action is a fresh isolated user rerun from current source, followed by read-only confirmation of the response-normalization audit and substantive full-draft/Word evidence if the pipeline advances.

## 2026-08-05 user role round 3 — preparation-stage admission CTA defect

- A fresh policy-valid Grok Build user pass used headful Playwright against `/private/tmp/mw-p0-user-r1c.p5NNG4` (API `8924`, Vite `5204`), zero user projects at start, and a distinct SLE Phase II project `proj_user_807ea9fea588`. It reached 343 ClinicalTrials.gov candidates, DeepSeek triage retained 55/excluded 288, and real Protocol download, native extraction, and official PaddleOCR-VL-1.6 OCR. It did not reach translation/full-draft/Word; the round is not clean and gives zero streak credit.
- Persisted evidence `/private/tmp/mw-p0-user-r1c.p5NNG4/evidence/pipeline_final_status.json` records `awaiting_preparation_admission`, `preparation_stage_admission_required`, prep batch `wref_prep_154ede83adfb4ca5aa223deb`, 8 completed documents and 49 deferred documents. One OCR item (`NCT02265744`) failed with unusable text; that issue is visible in child state but was not the reason to bypass the admission gate.
- Root cause: the backend already had a narrow, idempotent `resume_waiting` → `_continue_from_prepared_batch` route that advances exactly one next preparation stage without re-downloading/OCRing completed items, and `ResearchPipelineBanner` rendered `准入下一批原文`; however the banner lived in a collapsed progress `<details>`, while the compact medical-writing toolbar had no action for `awaiting_preparation_admission`. A real user therefore saw a waiting state with no visible next action. This is a UI reachability defect, not a missing backend transition.
- Minimal repair: the compact toolbar now shows `准入下一阶段原文` for `awaiting_preparation_admission`, reusing the existing idempotent resume route and explaining that completed source/OCR work is not repeated. The existing banner and backend semantics are unchanged. A deterministic test proves one stage advance, one pending run, a truthful re-pause when a deferred item remains, and no translation batch creation.
- Verification after repair: preparation-resume plus pipeline/frontend waiting focus `32 passed`; Vite build passed (`1,953` modules, existing chunk warning). The next safe action is a fresh isolated user rerun from current source to verify visible CTA reachability and then inspect OCR-exception handling, translation, substantive full-draft, Word structure, and link evidence. Clean streak remains `0`.

## 2026-08-05 user role round 4 — partial OCR failure and status-race root cause

- A fresh user pass used clean isolated runtime `/private/tmp/mw-p0-user-r1d.smdDTE` (API `8925`, Vite `5205`), zero projects at start, and a distinct plaque-psoriasis Phase III project `SYN-PSO-301` / `proj_user_a91ff9605285`. Headful Playwright reached 313 studies, recovered a failed triage chunk to 20/20, retained 57 studies, downloaded/extracted real Protocols, and ran official PaddleOCR-VL-1.6. The visible preparation CTA repair worked; no translation, full draft, or Word was produced, so this is a defect round with zero clean-streak credit.
- The observed path was: `awaiting_preparation_admission` (8/58, 50 deferred) → visible `准入下一阶段原文` → valid one-stage admission and pending work → one file (`NCT02742441/Prot_SAP_000.pdf`) failed closed at Paddle OCR (`completed with unusable text`, 63/64 pages) while the batch retained 23 `prepared` and 34 `deferred` items. SQLite evidence `/private/tmp/mw-p0-user-r1d.smdDTE/writing_reference.sqlite3` is unchanged after the run and records exactly these counts.
- First-principles root cause: the batch's `partial_failure` status was truthful, but `resume_waiting` is synchronous and releases the service lock while processing OCR. Concurrent UI status polling entered `_reconcile_stale_preparation_state` while the valid resume request was still processing; its durable-job check only saw the old initial job as terminal, so it wrote the parent from `preparing` to terminal `failed` (`stale_preparation_state:partial_failure`). API evidence shows the `POST /research-pipeline/resume` completed 200 while repeated `GET /status` calls were in flight; the final parent projection retained 34 deferred and 23 prepared items, proving this was a parent-state race rather than loss of usable evidence.
- Minimal source repair: `MedicalWritingResearchPipelineService` now holds a process-local active-resume lease while the narrow continuation runs; status reconciliation returns without writing during that lease and clears it only after the waiting/terminal result is persisted. After a process restart (no in-memory lease), a `partial_failure` batch with reusable prepared/deferred evidence reconciles to explicit `awaiting_preparation_admission` or `awaiting_translation_scope` with counts and a new error code; only a partial batch with no reusable evidence remains terminally failed. Failed OCR items remain failed/immutable and are never silently admitted as evidence.
- Deterministic verification: validation-gate suite `27 passed` (including active-resume race and both restart-reconciliation branches), py_compile passed, and the combined corpus/pipeline/frontend/condition-resolver focus `234 passed`. No service was started by Codex and no stable API, old r42/v36 lineage, or medical-monitoring files were touched.
- Next safe action: run a fresh isolated user pass from this source to verify that status polling cannot clobber the active resume, that failed OCR remains visibly reviewable while clean sources continue, and then inspect translation, evidence-bound analysis, substantive full draft, and Word/reference-link acceptance. Clean streak remains `0`.

## 2026-08-05 user role round 5 — confirmed basket with missing parent pipeline

- A fresh Codex user pass used `/private/tmp/mw-p0-user-r5.LnL6TK` (API `8927`, Vite `5207`), zero projects at start, and a distinct multiple-sclerosis Phase II project `RZ-202` / `proj_user_dd36dccc25ea`. The PAH alias probe was a bounded pre-reset diagnostic and is not a role-round credit.
- Visible Playwright reached 575 public studies, 81 reported Protocol references, DeepSeek triage 19/19, retained 43 indirect references and excluded 532. The manager clicked the one-click basket confirmation; the confirmation POST returned HTTP 200. No source preparation/OCR/translation/full draft/Word was reached.
- First-principles evidence: the captured access log contains `competitor-search` 200 at `2026-08-04T21:01:49Z` but no `research-pipeline/start` request or response for this project; the API log is empty; the UI showed no `pipeline_start_failed` message. After confirmation, `research_pipeline={}`, pipeline IDs/stage/job/prep/triage IDs were empty, durable jobs contained only the completed `competitor_triage` job, and the only audit event was `search_snapshot_saved`. The green UI text claiming that source download was continuing was therefore a P0 false-success/dead-end.
- Root-cause inference is bounded by the trace: the early same-project `automaticResearchTokenRef` stale guard in `runPublicSearch` could return after the search response when a component remounted/rotated the UI token, skipping the required idempotent pipeline-start POST. The trace proves the missing request; the remount/token cause is an inference, not a claimed HTTP error.
- Minimal source repair: the start request is now suppressed only by an actual project switch, not by same-project token rotation. The confirm handler now retains the backend response and branches on `pipeline_advanced`; it says “已开始继续处理原文” only when the backend returns true and explicitly reports an unestablished/failed pipeline otherwise. The old unconditional “正在继续下载方案原文” copy is removed.
- Verification: frontend contract suite `122 passed`; Vite build passed (`1,953` modules; existing large-chunk warning only). No service was started by Codex and no stable/r42/v36/medical-monitoring rows were touched. This round is non-clean and clean streak remains `0`.
- Next safe action: same-session fresh user rerun (round 6) must verify the search→start request reaches the API, the parent pipeline remains durable through triage confirmation, and then continue through real preparation/OCR/translation, evidence-bound analysis, substantive full draft, Word/render/link checks. Do not credit any round without full evidence and P0–P4 review.

## 2026-08-05 user role round 6 — start repair verified; night provider HTTP 403

- A fresh isolated Codex user pass used `/private/tmp/mw-p0-user-r6.Ab4RxY` (API `8929`, Vite `5210`), zero projects at start, and a distinct rheumatoid-arthritis Phase II project `RZ-203` / `proj_user_7598282cfa08`. The user used visible Playwright and the intended randomized double-blind placebo parallel design was supplied in the prompt, although the minimum framing form does not persist that design field. This is a defect/provider-blocked round and contributes zero clean-streak credit.
- First-principles transition evidence: the captured network log contains `competitor-search` HTTP 200 followed by exactly one `research-pipeline/start` HTTP 200, with job `mwjob_075cdc829b16ac369c552aa4` and pipeline `mwpipe_4f5f92bdfe7691b054a2`. The search snapshot contains 659 studies and 94 Protocol references. This verifies the round-5 missing-start repair; the parent pipeline was durable and entered triage.
- Triage reached 22 chunks: 4 succeeded and 18 failed. The user clicked the visible retry twice and refreshed; both retries preserved the same durable partial state. Final pipeline evidence is `/private/tmp/mw-p0-user-r6.Ab4RxY/evidence/r6_pipeline_status_final.json`; SQLite shows zero competitor confirmations, zero preparation batches/items, and only one idempotency row for the saved search snapshot. No download, OCR, translation, full draft, or Word artifact exists, so no clean credit is possible.
- Root cause is an external route/runtime blocker, not a skipped transition: the active 05:22 Beijing runtime selected `alibaba_qwen38` / `alibaba_token_plan` with `qwen3.8-max-preview` under the declared night policy; all 18 failed chunks record provider HTTP 403. The provider setting and failure observations are retained without credentials in `/private/tmp/mw-p0-user-r6.Ab4RxY/evidence/r6_provider_failure_observation.txt`. Do not silently switch to DeepSeek before its allowed daytime window, bypass triage, or treat partial results as evidence.
- Safe next action: after 07:30 Beijing, run a new clean clone with the policy-effective DeepSeek daytime route and a new indication/design. Reconfirm one start request, then investigate any new failure causally; preserve the current zero clean streak until a complete substantive Word and P0–P4 review are evidenced.

## 2026-08-05 deterministic regression after round 6

- Read-only source regression across authoring/framing, evidence binding, PICOS, condition-term resolution, content quality, corpus-analysis normalization, full-draft contracts, pipeline minimum/start/progress/validation, and frontend waiting/transition contracts: **869 passed, 17 warnings, 23.11 s**.
- This check used no service, no external provider call, no OCR/download/translation, and no mutation of stable/r42/v36/medical-monitoring data. It confirms the round-5 start repair and round-4 resume lease remain covered by deterministic contracts; it does not substitute for a daytime real-browser acceptance round.

## 2026-08-05 product-test route binding clarification

- Read-only inspection of the shared product runtime shows `active_profile_id=alibaba_qwen38`; the persisted comprehensive binding is `independent_ai__alibaba_qwen38 / alibaba_token_plan / qwen3.8-max-preview`, while the configured DeepSeek Flash profile is `deepseek_translation_support / deepseek / deepseek-v4-flash` and the comprehensive DeepSeek profile is V4 Pro. This explains why the night clone exposed Qwen Preview and must not be conflated with the user-requested test configuration.
- No shared runtime was changed. Before the daytime acceptance pass, the isolated clone must apply a test-only role overlay that explicitly binds both comprehensive AI and translation-support AI to the user-requested DeepSeek V4 Flash (thinking/max), with PaddleOCR-VL-1.6 and Hy-MT2 unchanged. The overlay, effective route receipt, and any provider error must be retained; a source default change is not inferred from this inspection alone.

## 2026-08-05 user role round 7 — translation-boundary and provider-response audit

- Fresh isolated clone `/private/tmp/mw-p0-user-r7.5YLtnw` (API `8930`, Vite `5211`) started with zero projects. The separate senior-medical-monitor user role used headful Playwright and a new Phase II atopic-dermatitis project (`QZ-AD01`); the provider receipt is `evidence/r7_provider_role_receipt.{md,json}`. The isolated overlay was DeepSeek `deepseek-v4-flash` with thinking enabled/max, PaddleOCR-VL-1.6, and oMLX Hy-MT2. No stable API, r42/v36 rows, or medical-monitoring files were touched.
- ClinicalTrials.gov returned HTTP 200 with 455 studies / 27 Protocol chunks. A browser-proxy observer initially listened to API `8930` while the browser correctly used Vite proxy `5211`; this was a harness observation error, not a product failure. After correcting the observer, visible Playwright evidence proved one `competitor-search` POST 200 and one `research-pipeline/start` POST 200 with durable IDs.
- First triage produced two invalid JSON/SSE chunk failures. The user clicked the visible retry once; the retry completed `review_ready` without bypassing the contract. Preparation then used 15 visible `准入下一阶段原文` clicks; 101 documents were prepared and 13 remained immutable failed. Repeated official Paddle OCR outcomes were `paddle_ocr_outcome_unknown` (submit/poll `SSLEOFError`) or unusable text. The fail-closed exclusion was correct, but the adapter does not persist safe HTTP status/content-type/body-hash/poll-attempt diagnostics; this is a follow-up observability gap, not evidence to admit bad OCR.
- Translation produced 20 distinct translation records, each attempt 1, and 20 distinct composite runs. One visible continuation created a second immutable reuse batch with zero chunks/model calls; it did not duplicate translation records. No corpus-analysis row, full draft, or Word artifact was produced.
- Corpus analysis stopped at `awaiting_corpus_analysis` after the provider returned a JSON completion with empty `message.content` while `reasoning_content` existed. The user clicked the visible continuation once; the retry failed at the same parser boundary. Raw response body/status/content-type/SSE termination were not available in the prior runtime. Evidence: `evidence/r7_translation_failure_diagnostics.json`, `evidence/r7_pipeline_status_final.json`, `evidence/r7_translation_sqlite_final.json`, `evidence/r7_translation_final_ui.json` and screenshot. Round verdict: **fail; zero clean-round credit**.
- Source repair now bumps the corpus prompt identity to v11, explicitly requires final JSON in `message.content` (never `reasoning_content`) with a compact envelope example, raises the analysis output cap to 32,768, and propagates only safe provider response-shape diagnostics into the durable child context. Compact toolbar actions now surface `awaiting_translation_scope` and `awaiting_corpus_analysis` continuation without requiring expansion of the progress details. The original response is never persisted as raw text by this diagnostic path.
- Deterministic verification after the repair: 158 focused tests passed; Vite build passed (1,953 modules, existing chunk-size warning). The broader medical-writing suite yielded 2,528 passed / 13 unrelated baseline failures / 1 skipped; those failures concern template/chapter/legacy API baselines and were not masked. Next safe action is a new daytime DeepSeek user-role clone; no clean streak is granted until substantive Word and P0–P4 review pass.

## 2026-08-05 fixed product-model configuration and r8 classification

- The product-level writing configuration is now explicit and independent of the external dispatch day/night policy: comprehensive AI and translation-support LLM bind to the separate profile `independent_ai__deepseek_v4_flash`, model `deepseek-v4-flash`, `thinking=enabled`, `reasoning_effort=max`; OCR binds to official `PaddleOCR-VL-1.6`; body translation binds to oMLX `dawncr0w--Hy-MT2-30B-A3B-oQ8-MLX` with thinking disabled. The shared active provider remains Alibaba Qwen for legacy active-profile consumers; role-bound writing/monitoring resolution uses the explicit independent binding.
- `ai_role_runtime_settings.py` now accepts `max`, exposes it in the API/UI effort list, ships the isolated DeepSeek Flash builtin profile, defaults both LLM roles to thinking/max, and copies the encrypted DeepSeek support credential only into the separate independent profile during migration. No credential value is written to source or durable records. This migration keeps role isolation while avoiding a browser-visible secret.
- Current runtime JSON was updated and syntax-checked. Post-change SHA-256: `runtime/ai_provider_settings.json` `fd42ed4eb4c9e79695bcea282e6351dcf9a70721970b4d8c203c282b3a9be77e`; `runtime/ai_role_bindings.json` `4e41a156649529b5c1f4cd5885ac0d45629ba6ebe9da62ebd5b046f451222f9d`. The encrypted secret store itself was not read into records or directly rewritten; first role-store migration copies the existing local DeepSeek support credential to the new profile.
- Focused verification after the change: role/gateway tests `69 passed`; frontend contract focus `134 passed`; backend `py_compile` passed; Vite build passed (1,953 modules, existing large-chunk warning only). Stable runtime JSON read-only contract check confirms the required four role models and max settings. The previous broader result remains partial (2,528 passed / 13 unrelated baseline failures / 1 skipped).
- r8 was a valid clean-clone user attempt but its provider receipt was created under the prior `xhigh` configuration. It is explicitly **not clean and receives zero streak credit**, even if the isolated clone later reaches a fuller terminal state. The next user clone must carry a receipt proving `max` before any corpus-analysis or full-draft call.

## 2026-08-05 r8 evidence-bound rejection and v12 repair

- Read-only clone evidence proves the r8 terminal cause is a provider semantic-contract violation, not registry connectivity, OCR, translation, an adapter field projection, or a mapper loss. `writing_reference_corpus_analysis_runs` has zero rows because validation rejected every finding before persistence. The exact error is `structure and regulatory_commonality modules require regulatory_common_structure pattern_kind`; the validator can emit this only when the finding has a supported but wrong `pattern_kind` (`wording_convention` or `clinical_design_requirement`) alongside one of those modules.
- `_normalize_response_root` only wraps an exact finding-key singleton and never changes finding fields; `_chat_completion_content` parses the provider JSON directly. The fail-closed boundary is correct and must not silently infer or rewrite the missing semantic classification. R8 had already made upper-layer xhigh-era calls; its later isolated GET readiness check showed `deepseek-v4-flash`, thinking enabled, max, `current_runnable=true`, build `api-85bc3b483d8a483d`, but no post-restart corpus retry occurred.
- Minimal source repair is now v12: the system prompt includes an explicit module→pattern_kind table and forbids omission/guessing; all invalid combinations remain rejected. A no-findings error now carries sanitized diagnostics only (stable failure code, rejection reasons/counts, output hashes, normalization status), so the waiting UI/state can explain the semantic rejection without persisting raw model output. Deterministic focus after the repair: 115 tests passed; backend compile passed. No live retry was run.
- Recheck requirements: fresh max-receipt clone; evidence of request-level max; exact deterministic rejection and no-artifact behavior for wrong module/kind combinations; valid regulatory-structure and clinical-design fakes; then full visible user flow to substantive Word, internal-marker scan, TOC/reference-link checks, and two P0–P4-clean rounds. Clean streak remains 0.

## 2026-08-05 engineer round 9 / user-requested no-loss pause

- Fixed-max Grok engineer pass used clean clone `/private/tmp/mw-p0-engineer-r9.vZbWIw`, project `proj_user_0e7ac527231c`, asthma Phase III. Receipt gate passed with DeepSeek Flash thinking/max, official PaddleOCR-VL-1.6, and Hy-MT2. Search 697; triage recovered to 26/26; 87 retained/610 excluded; preparation ended at `awaiting_preparation_admission` with 44 prepared, 4 fail-closed OCR items, 39 deferred. No translation, corpus, full draft, Word, or clean credit.
- Report `runs/role_acceptance/mw_protocol_p0_20260805_round9_engineer_grok.md` is the detailed source. Findings are P0 no full Word path, P1 triage missing-results schema retry, P1 weak OCR outcome observability, P1/P2 high-friction staged admission, and P2 harness navigation mismatch. No product/stable data mutation.
- Pause control was staged at `prompts/role_acceptance/mw_protocol_p0_20260805_round9_pause_grok.md`. Because the active process had stdin `/dev/null`, an authorized interrupt stopped it; fallback/helper and isolated services were also stopped. Same-session resume on Grok session `a1e31091-cfce-4482-bc95-a0c841f9fbc3` failed before response due three settings-interface network errors. Preserve the session directory, clone, evidence, and report; the pause marker itself was not observed.
- Resume boundary is explicit: after user asks to continue and Grok network is available, recheck max receipt and continue visible preparation admission on this clone; do not dispatch user role or claim clean until substantive Word and P0–P4 evidence pass.

### Pause-network correction

- The blocked Grok resume later produced additional settings retry groups around 05:05 and 05:11, each ending in `Settings fetch failed max_attempts=3`, followed by controller exit code 130. No model response, browser action, or product/stable-data write occurred after round 9; this does not alter the non-clean verdict.
