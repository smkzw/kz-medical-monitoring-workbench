# 08C-4 visual acceptance follow-up — round 3

Continue the same independent visual-conference session. Reopen the frozen contract, the Round 2 report, the user Sankey reference, and the newly refreshed R4 originals/JSON/collages under `artifacts/mm_r7_slice08c4_ego_visual_20260830/evidence_revised/`.

The execution session reports these Round 2 repairs completed:

- comparable current overview no longer shows the first-version notice;
- five incremental KPIs precede the four-stage project/site flow and homologous table;
- `risk-multi-001` exposes two continuity rows with exactly one pressed row;
- the 1920 evidence set is 1920x1080 and complete;
- tamper state retains stable current flow while suppressing incremental KPI, summary, and table deltas;
- event and risk overlays use the same 0.28 scrim;
- table terminal column and study-status node collision were repaired;
- duplicated/engineering copy was replaced with Chinese-native user wording;
- focused tests report 23/23 and 8984 was stopped.

Independently verify rather than trusting that report. Compare reference and prototype at matched states/viewports, inspect the original PNGs and structured JSON, and judge the complete frozen contract. Return `ACCEPT_VISUAL` only if P0-P4 are all zero; otherwise give bounded actionable findings with exact evidence paths. Do not modify product files or claim Codex final acceptance.
