# 07C-4 合同最终同 session 复核请求

保持 session `b50d6481-6614-4fbe-800c-8a3a55530e9c`，只读。

请完整读取 v0.2 最新版本，核对上一轮仅剩的三项 P0：

1. available publication token 不重铸；
2. 删除历史结果页的独立 in-flight 混合提示，只保留次动作“返回正在进行的监查”；
3. v0.1 §9 允许修改路径加入 launch_registry additive token 列/唯一索引/finalize/lookup。

同时核对五项 P1 编辑：精确 in-flight 名称、subject GET 必须 site_ref、公开 identity 提供 mode/site
文案、progress public token 例外、结果内返回与模块重入区分。若均关闭，请明确返回 `ACCEPT` 并给出
冻结状态；若仍有 blocker，只列当前文本仍未关闭的精确条款，不重开已关闭问题。
