# 08C-4 visual acceptance follow-up — round 4

Continue the same independent visual-conference session. Round 6 replaced the inadmissible Round 5 Chromium stills with an ego(lite)-only packet after relaunching ego and using native `captureScreenshot()`.

Reopen the newest Round 6 originals, JSON, `CAPTURE_META_R6_EGO.json`, `EGO_ONLY_R6_PACKET.json`, FINDINGS/MANIFEST, the user Sankey reference, and the matched `1280_overview_vs_sankey_ref.png` comparison. Do not rely on worker prose.

Specifically rejudge every prior P2-P4 item:

- 1280/1440 table headers, orphan wrapping, and whether the table-local horizontal-scroll affordance is genuinely visible and understandable rather than merely present in CSS;
- flow connector and study-status risk-label collision at all three viewports;
- single site return control;
- inspector metadata punctuation/wrap;
- Journey axis-local scrollbar visibility;
- tamper aggregate and item-level delta suppression;
- removal of implementation/check-state chips;
- consistent `数据完整性`.

Also verify the packet is ego-only and the 1920 originals are 1920x1080. Return `ACCEPT_VISUAL` only if P0-P4 are all zero under the frozen contract. If any visible issue remains, give the smallest exact repair with evidence paths. Do not modify product files or claim Codex final acceptance.
